Original prompt: I want to make the 3d world with real world physical rules.

- 2026-03-18: Inspected the repo. It currently contains a single-file Three.js character controller with basic arcade movement and no deterministic test hooks.
- 2026-03-18: Replaced the arcade controller with a fixed-step physics update: gravity, acceleration, drag, slope response, static colliders, and pushable metal spheres.
- 2026-03-18: Added deterministic hooks: `window.render_game_to_text`, `window.advanceTime(ms)`, and `window.resetWorld()`.
- 2026-03-18: Headless validation showed a black canvas capture even though the physics state was advancing, so the renderer now uses `preserveDrawingBuffer: true` for reliable screenshot readback.
- 2026-03-18: Validated from `file:///Users/yifandu/Desktop/3dw/character.html` with the Playwright client. Screenshots and state dumps show the ramp, sloped movement, jumping, and rolling spheres all working without reported console/page errors.
- 2026-03-18: Changed horizontal controls from camera-relative to fixed screen/world axes and made the camera follow position without rotating around facing, so left/right stay literal.
- 2026-03-18: Re-ran right, left, and ramp scenarios. State dumps now show right driving the player to positive X and left to negative X while the ramp sequence still works.
- 2026-03-18: Adjusted facing so backward-dominant input backpedals instead of turning the character around.
- 2026-03-18: Verified backpedal with a dedicated down-key run; the player now moves to negative Z without a turn-around spin, and left/right still regress cleanly.
- 2026-03-18: Flipped the horizontal basis sign. With the camera looking toward +Z, screen-right corresponds to world -X, so the previous literal X mapping was reversed.
- 2026-03-18: Clarified the text-state coordinate note so it no longer claims world +X is screen-right.
- 2026-03-18: Removed the visual yaw offset after confirming the model geometry already faces +Z. Forward motion should now show the character's back, which matches the camera and movement frame.
- 2026-03-18: Re-validated the body orientation: forward now shows the character's back, side movement shows a side profile, and backward preserves the same body facing for backpedal motion.
- 2026-03-18: Tightened the backpedal rule so it only applies to pure backward input. Left/right reversals now rotate the full figure instead of preserving the previous side-facing pose.
- 2026-03-18: Started making the gait more anatomical: knee flexion via shin joints, shoulder/hip counter-rotation, and a separate backpedal pose instead of reusing the forward walk swing.
- 2026-03-18: Validated the updated gait with forward, backward, and left-to-right turn runs. The figure keeps consistent body facing and now uses more human-like flexion/extension with visible knee bend and trunk rotation.
- 2026-03-18: Rebuilt the lower footwear as tall boots and moved the boot toe to +Z so the feet now point the same way as the face/body.
- 2026-03-18: Added a scoreboard HUD, a goal mouth at the plateau end, and score detection that respawns the scored ball back at spawn.
- 2026-03-18: Extended the plateau support through the goal, added a centered scoring lane, moved the side balls off the middle path, and revalidated with Playwright. A straight sprint push now scores and resets `steel-2` while preserving the rest of the physics setup.
- 2026-03-18: Replaced the fixed follow camera with a mouse-drag orbit camera. The camera now tracks yaw/pitch around the player, resets to the default view on `resetWorld()`, and exports camera state in `render_game_to_text`.
- 2026-03-18: Validated the orbit camera with the bundled Playwright client for gameplay regressions and with a direct Playwright drag script using the same SwiftShader flags as the client. A drag changed camera yaw from `0` to `-1.98` and pitch from `0.52` to `1.17`, and scoring still passes with the sprint push flow.
- 2026-03-18: Added a `↺ VIEW` action button that resets the orbit camera to the startup angle. A direct Playwright drag-and-click test confirmed the camera returns from `yaw -1.98 / pitch 1.17` back to `yaw 0 / pitch 0.52`.
- 2026-03-18: Reworked the goal loop into rounds. Scored balls now disappear instead of respawning immediately, the HUD shows level/balls-left, and clearing all 3 balls triggers a level transition.
- 2026-03-18: Added a level-2 rival with its own score, locomotion, collision handling, and debug hooks so the next round becomes a user-vs-AI contest over the same 3 balls.
- 2026-03-18: Tuned rival target selection and steering to prefer makeable shots through the center lane. A 20-second headless level-2 run now shows `aiScore: 1`, `ballsRemaining: 2`, and `statusMessage: "AI SCORED STEEL-2"`.
- 2026-03-18: Upgraded the rival AI with target persistence, stuck detection, plan-based steering (`recover`/`funnel`/`align`/`finish`), and a short-range contact assist so it can keep pressure on one ball instead of spraying multiple balls sideways.
- 2026-03-18: Revalidated the standard harness and player scoring after the AI upgrade. Level 1 still opens cleanly with `ballsRemaining: 3`, and the player sprint push still scores `steel-2` and removes it from play.
- 2026-03-18: Benchmarked the smarter rival. After 20 simulated seconds in level 2, it has `aiScore: 1` with `steel-1` advanced onto the plateau at `z: 18.33`; after 26 seconds it reaches `aiScore: 2`, `ballsRemaining: 1`, and `statusMessage: "AI SCORED STEEL-1"`.
- 2026-03-19: Added a `STYLE MENU` HUD panel for outfit changes. The player can now cycle shirt colors, toggle black shades, and toggle a cap; the selected outfit is included in `render_game_to_text`.
- 2026-03-19: Added actual accessory geometry to the player model and wired shirt themes into the torso and sleeve materials. The chosen outfit persists through `resetWorld()` and normal level resets.
- 2026-03-19: Found and fixed a debug-hook bug where `window.resetWorld()` recursively called itself. The internal reset function now uses a separate name, and the style-menu Playwright test confirmed outfit persistence after reset.
- 2026-03-19: Revalidated gameplay after the wardrobe update. Level 1 still opens with the default outfit, the player sprint push still scores `steel-2`, and a direct front-view screenshot shows `BLACK` shades, `FOREST` shirt, and `CAP` selected together.
- 2026-03-19: Adjusted the shades to follow real-world visibility: the black lenses are now fully opaque and the eye meshes are hidden whenever shades are enabled. A direct front-view screenshot confirms the face no longer shows eyes behind the glasses.
- 2026-03-19: Reworked the face into a softer fantasy-adventure style: flatter eyes instead of protruding eyeballs, subtler brows/nose/mouth, light cheek tint, and slightly pointed ears. Direct front-view screenshots confirm the bare face is less creepy and the shades variant still masks the eyes.
- 2026-03-19: Extended the style system to support boot colors. The `STYLE MENU` now includes a `BOOTS` slot, boot palettes are wired into the leg meshes, and the selected boot color is exposed in `render_game_to_text` and `window.setOutfit(...)`.
- 2026-03-19: Revalidated the wardrobe and gameplay after the boot update. Level 1 still opens with `boots: "OBSIDIAN"`, player scoring still works, and a direct front-view screenshot confirms the `CRIMSON` boots render correctly and persist after `resetWorld()`.
- 2026-03-19: Fixed character flicker caused by near-coplanar clothing layers fighting in the depth buffer. The shirt stripe is now a front panel instead of a full torso-depth block, the boots are spaced more clearly away from the shins, and the hair shell sits farther off the skull.
- 2026-03-19: Revalidated after the flicker fix. Base load still opens cleanly, the player sprint push still scores `steel-2`, and a direct rear-view screenshot shows the back/boot surfaces rendering cleanly without the old overlapping stripe.
- 2026-03-19: Reworked the player arm animation to follow more human shoulder flexion/extension. Arms now use mirrored pose helpers with elbow flexion, slight shoulder abduction, and forearm rotation instead of a single rigid pendulum swing.
- 2026-03-19: Revalidated the arm update. Base load still opens cleanly, the player sprint push still scores `steel-2`, and side-profile captures now show bent-elbow walk/run poses plus a raised bent-elbow wave pose.
- 2026-03-19: Corrected the arm rig's elbow flex direction. The mesh was bending forearms backward relative to the body, so `applyArmPose(...)` now maps positive elbow flexion to the anatomical forward fold direction.
- 2026-03-19: Revalidated the elbow-direction fix. Base load still opens cleanly, the player sprint push still scores `steel-2`, and fresh side-profile screenshots now show the forearm folding forward in idle, walk, and wave.
- 2026-03-19: Added a day/night sky system for the overall UI/UX pass. The HUD now advertises a `T` shortcut, the style menu includes a `SKY` control, and the scene can switch live between `DAY` and `NIGHT`.
- 2026-03-19: Day/night now drives actual environment state rather than just text. The scene updates background/fog, tone mapping exposure, ambient/key/fill/rim lights, lamp glow, ground/grid palette, stars, clouds, and sun/moon visibility, and the current sky mode is exposed in `render_game_to_text`.
- 2026-03-19: Revalidated the sky system. Default load reports `environment.sky: "NIGHT"`, clicking the `SKY` control switches to `environment.sky: "DAY"`, the full-page screenshots show the HUD/state/menu labels updating, and the player sprint push still scores `steel-2` after the environment work.
- 2026-03-19: Added a blocky coffee shop to the left corner of the field. It now sits on the player's left from the default camera frame, includes a sign, awning, windows, patio/menu props, and has matching static colliders so it behaves like real world geometry.
- 2026-03-19: Revalidated the coffee shop addition. Default load still opens cleanly, a dedicated orbit view shows the shop clearly in daylight, and the player sprint push still scores `steel-2` after the new structure was added.
- 2026-03-19: Switched player movement from fixed world axes back to orbit-camera-relative input. `WASD`/arrows/d-pad now derive their forward/right basis from the live camera yaw so the movement directions stay consistent after mouse rotation, while the pure-backward backpedal rule stays intact.
- 2026-03-19: Revalidated camera-relative controls with the Playwright harness and direct orbit tests. At `yaw: -1.57`, `up` now moves the player to `x: 2.07`, `down` to `x: -2.07`, `right` to `z: 4.07`, and `left` to `z: -0.07`; the default sprint push still scores `steel-2` with `score: 1`.
- 2026-03-19: Corrected the coffee shop sign orientation. The global coordinate system was not the problem; the front sign plane was rotated inward, so the camera was seeing the mirrored back face of the texture.
- 2026-03-19: Revalidated the coffee sign fix. Base load still opens cleanly, a daylight orbit screenshot now shows `COFFEE` reading correctly on the facade, and the normal sprint push still scores `steel-2` with `score: 1`.
- 2026-03-19: Added a pointer interaction mode for the playground. `Q` now toggles between `DRAG MODE` and `SELECT MODE`; in select mode, clicking can lock onto dynamic objects like the steel balls or onto terrain areas like the ramp/meadow, and the current selection is shown in the HUD and exported through `render_game_to_text`.
- 2026-03-19: Added selection visuals and debug hooks. Selected objects get a cyan outline, selected areas get a marker ring, and `window.setInteractionMode(...)`, `window.pickSceneAtCanvas(...)`, and `window.projectWorldToCanvas(...)` are now available for deterministic testing.
- 2026-03-19: Revalidated the selection system. A direct Playwright run now shows `Q` switching to `interaction.mode: "select"`, clicking `steel-3` yields `selection.name: "steel-3"`, clicking the ramp yields `selection.type: "area"` with `zone: "ramp"`, switching back to drag keeps orbit rotation working, and the normal sprint push still scores `steel-2` with `score: 1`.
- 2026-03-19: Tightened the mode switch so entering drag mode clears the active selection immediately instead of keeping the old object/area locked.
- 2026-03-19: Revalidated the clear-on-drag behavior. After selecting `steel-3`, pressing `Q` back to drag now resets `interaction.selection` to `null`, removes the cyan highlight, and updates the HUD back to `SELECT: NONE`.
- 2026-03-19: Flipped the horizontal orbit-drag sign so dragging the cursor right now rotates the camera view to the right instead of swinging it left around the player. Vertical drag behavior is unchanged.
- 2026-03-19: Revalidated the drag-direction fix. A direct drag run now changes camera yaw from `0` to `1.75` after a rightward drag, with the screenshot matching the new rightward orbit, and the normal sprint push still scores `steel-2` with `score: 1`.
- 2026-03-19: Superseded the earlier drag-direction note after reproducing a first-drag inconsistency. The orbit path now updates the camera immediately inside `pointermove` instead of waiting for the next animation frame, which removes the old “first drag feels reversed / only settles after direction changes” behavior.
- 2026-03-19: Corrected the remaining orbit-sign bug after rechecking the untouched initial view. The live pointer update stays in place, but horizontal drag now uses the opposite yaw sign so `drag left -> left view` and `drag right -> right view` from the default camera instead of the inverted mapping.
- 2026-03-19: Revalidated the corrected orbit rule with fresh initial-state captures and regressions. In `output/web-game-drag-initial-fixed-2/state-0.json`, dragging left reaches `yaw: 1.76` and dragging right reaches `yaw: -1.76`, matching the visible left/right views in `left.png` and `right.png`; camera-relative movement still passes in `output/web-game-drag-camera-relative-fixed-2/state-0.json`, and score still passes in `output/web-game-drag-score-fixed-2/state-0.json`.
- 2026-03-19: Added a decorative pair of black tall boots beside the coffee shop entrance. The new prop uses its own grouped boot geometry so it reads as a world object rather than the player rig, and it sits on the shop slab near the front menu side.
- 2026-03-19: Revalidated the coffee-shop boots with a daylight orbit capture and standard regressions. `output/web-game-boots-prop-coffee/shot-0.png` shows the pair clearly by the storefront, `output/web-game-boots-prop-base/shot-0.png` confirms the boots are also visible from the default view, and score still passes in `output/web-game-boots-prop-score/state-0.json` with `score: 1`.
- 2026-03-19: Added a touch-triggered boot confirmation near the coffee-shop prop. When the player reaches the black boot pair, a modal asks whether to switch to the black tall boots; `SWITCH` equips `OBSIDIAN`, `KEEP` preserves the current style, and the prompt stays dismissed until the player leaves the touch zone.
- 2026-03-19: Added deterministic helpers for the boot interaction. `render_game_to_text` now reports `interaction.bootsPrompt`, and `window.movePlayerToCoffeeBoots()` plus `window.respondBootPrompt(...)` allow direct prompt testing without hand-driving the player across the map.
- 2026-03-19: Revalidated the boot confirmation flow. `output/web-game-boots-confirm-accept/state-0.json` shows `bootsPrompt.active: true` on contact and `afterYes.outfit.boots: "OBSIDIAN"` after confirmation, `output/web-game-boots-confirm-cancel/state-0.json` shows `afterNo.outfit.boots: "CHESTNUT"` after cancellation, base load still passes in `output/web-game-boots-confirm-base/state-0.json`, and scoring still passes in `output/web-game-boots-confirm-score/state-0.json` with `score: 1`.
- TODO: If you want stronger screen-space motion instead of a centered follow camera, adjust the camera follow amount rather than the input axes.
- 2026-03-20: New request: change the background environment into the working office shown in the provided reference image.
- 2026-03-20: Replaced the outdoor meadow presentation with a stylized office interior pass in `character.html`: darker herringbone wood flooring, office day/night lighting, a pantry island on the screen-right, a lounge cluster on the screen-left, interior planters, glass walls, a feature wall, and visible ceiling beams/pipes while preserving the existing gameplay path, physics, and debug hooks.
- 2026-03-20: Repositioned the pantry/boot interaction zone to the office island side and updated the world labels/text-state from meadow/playground wording to office-floor wording.
- 2026-03-20: Validation: inline script syntax passes via `node --input-type=module -e`, base office capture saved to `output/web-game-office/shot-0.png`, and a direct Playwright check in `output/web-game-office-check/state.json` confirmed scoring still increments to `score: 1` / `ballsRemaining: 2` after `forceScoreBall('steel-2', 'player')` and the pantry boot prompt still activates after `movePlayerToCoffeeBoots()`.
- 2026-03-20: Follow-up office pass: extended the side glazing into more obvious floor-to-ceiling window walls and fixed the pantry/front-desk flicker by recessing the dark kickplate so it no longer shares the same front plane as the desk body. Also added a directional-light normal bias to reduce shadow acne on the desk surfaces.
- 2026-03-20: Revalidated after the window + desk fix. `output/web-game-office/shot-0.png` shows the taller window wall, and `output/web-game-office-check/shot.png` confirms the close pantry view no longer shows the old jagged flicker band while the boot prompt still opens and the score regression still passes in `output/web-game-office-check/state.json`.
- 2026-03-20: Corrected the floor-window interpretation after the user called out that the earlier glazing still did not read as a true floor-to-ceiling window. Replaced the weak side partition with framed full-height window walls using an exterior skyline backdrop so the glazing is obvious from both the default and pantry-adjacent camera views.
- 2026-03-20: Added a branded logo display wall near the pantry side to match the reference office display area. The panel wall now uses a dedicated canvas texture with a bold black `DP` logo in the first panel and a grid of supporting graphic marks across the rest of the wall.
- 2026-03-20: Revalidated after the logo wall addition. Fresh renders in `output/web-game-office/shot-0.png` and `output/web-game-office-check/shot.png` show the branded wall in the requested area, and a direct Playwright check still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Added two more office props: a coffee machine on the pantry island and an upright soda freezer placed next to the branded wall. The freezer has a glass front, shelves, and a `SODA` header; the coffee machine now reads clearly in the pantry-side view.
- 2026-03-20: Revalidated after the pantry/freezer additions. `output/web-game-office/shot-0.png` and `output/web-game-office-check/shot.png` show both props, and the quick Playwright regression in `output/web-game-office-check/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-24: Fixed the `SPECIAL MOVES` UI layout. The panel is now anchored to the action stack as a left-side popout instead of using the old fixed bottom offset, so it no longer overlaps the action rows or the style menu after the extra control rows were added.
- 2026-03-24: Revalidated the moves layout with the required Playwright client in `output/web-game-moves-layout-client/state-0.json` and a focused screenshot/state pass in `output/web-game-moves-layout/shot.png` and `output/web-game-moves-layout/state.json`; the panel opens cleanly with `interaction.specialMoves.expanded: true` and no visible control overlap.
- 2026-03-24: Moved the top-center scoreboard and select-mode HUD into a dedicated upper-right lane. They now sit just left of the existing status/style stack instead of occupying the center of the screen, which keeps them off the left help panel and clears more of the scene.
- 2026-03-24: Revalidated the top-panel repositioning with the required Playwright client in `output/web-game-top-panels-right-client/state-0.json` and a focused screenshot/state pass in `output/web-game-top-panels-right/shot.png` and `output/web-game-top-panels-right/state.json`; the level/select HUD stays visible on the right with no overlap against the left data panel.
- 2026-03-24: Moved the scoreboard and select-mode HUD to the left edge instead, and pushed the directions/help HUD below them so the left-side overlays read as a single vertical stack.
- 2026-03-24: Revalidated the left-edge HUD stack with the required Playwright client in `output/web-game-top-panels-left-client/state-0.json` and a focused screenshot/state pass in `output/web-game-top-panels-left/shot.png` and `output/web-game-top-panels-left/state.json`; the score/select panels stay pinned to the left edge and the directions block sits below them.
- 2026-03-24: Added a dark luxury kitchen zone to the back side of the office. The new set includes a darker stone floor patch, back cabinetry, a stone backsplash/hood wall, a wood-accent island with sink/oven details, and warm pendant lights so the rear of the space reads closer to the provided kitchen reference.
- 2026-03-24: Revalidated the back-kitchen pass with the required Playwright client in `output/web-game-back-kitchen-client/state-0.json` and a focused render/state pass in `output/web-game-back-kitchen/default.png`, `output/web-game-back-kitchen/kitchen-angle.png`, and `output/web-game-back-kitchen/state.json`; the kitchen is now visible in the rear office scene and the normal game state still initializes cleanly.
- 2026-03-24: Added a framed glass front door to the back-left corner area. The first pass put it on the wrong rear wall segment, so it was moved onto the actual rear-left side wall and given a darker floor mat to read more clearly as an entry.
- 2026-03-24: Revalidated the back-corner door pass with the required Playwright client in `output/web-game-back-door-client/state-0.json` and focused camera/state captures in `output/web-game-back-door/default.png`, `back-door-angle.png`, `back-door-corner.png`, `back-door-clean.png`, `state-focused.json`, and `state-corner.json`; base game state still initializes cleanly after the door placement.
- 2026-03-20: Removed the soda freezer from the pantry/logo-wall area while keeping the coffee machine on the island. Fresh renders in `output/web-game-office/shot-0.png` and `output/web-game-office-check/shot.png` show the cleared wall zone, and the quick regression still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Added a pair of glassy meeting rooms on the lounge/window side of the office. Each room now has dark metal framing, translucent glass partitions, an interior meeting table with chairs, wall-mounted display panels, and collision volumes so the rooms behave like built-in architecture.
- 2026-03-20: Revalidated after the meeting-room addition. Inline script syntax passes, `output/web-game-office/shot-0.png` and `output/web-game-office-check/shot.png` show the new rooms from the default and orbit-biased views, and the quick regression in `output/web-game-office-check/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Fixed the meeting rooms so they are actually enterable. The sealed front glass wall was replaced with left/right sidelights, doorway jambs, and an open glass door panel, and the front collider was split into side segments so the center span stays passable for the player capsule.
- 2026-03-20: Revalidated after the meeting-room entry fix. Inline script syntax passes, `output/web-game-office-check/state.json` now includes a doorway probe showing no collision push across the board-room entry line (`x: 13.9 -> 15.5` at `z: 2.7`), the fresh room view is in `output/web-game-office-check/shot.png`, the base render is in `output/web-game-office/shot-0.png`, and the quick regression still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Replaced the old blocky player presentation with a new custom stylized female character built directly in code on the existing animation skeleton. The new model adds layered black hair, a slimmer anime-leaning face, a black leather-jacket silhouette, gray top, black shorts, knee-high boots, and a handheld spray can while preserving movement, animation, and collision behavior.
- 2026-03-20: Updated outfit application for the new character so shirt choices recolor the top/shorts rather than the jacket shell, hair hides correctly when the cap toggle is used, shades still mask the eyes, and the existing boot palettes continue to drive the tall boot materials.
- 2026-03-20: Revalidated after the character-model replacement. Inline script syntax passes, the default office view is in `output/web-game-office/shot-0.png`, front character captures are in `output/web-game-office-character-front/front.png` and `output/web-game-office-character-front/close-front.png`, and the quick regression in `output/web-game-office-character-front/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Fixed the new character’s foot sinking by adding a dedicated visual ground offset for the rebuilt model. The gameplay actor already snapped to terrain correctly; the issue was that the new taller boots extended farther below the old render root, so the model is now rendered slightly higher while physics and collisions remain unchanged.
- 2026-03-20: Revalidated after the foot-alignment fix. Inline script syntax passes, the updated default office view is in `output/web-game-office/shot-0.png`, the close side-foot check is in `output/web-game-office-character-front/side-foot.png`, and the quick regression in `output/web-game-office-character-front/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Softened the custom character face to make it less uncanny. Reduced the eye opening and highlight intensity, switched the irises to a calmer brown palette, lowered the eyelids, eased the eyebrow angle, shrank the nose/mouth forms, softened the cheek tint, and rounded the ear tips.
- 2026-03-20: Revalidated after the face-softening pass. Inline script syntax passes, the updated close-up is in `output/web-game-office-character-front/close-front.png`, the default scene is in `output/web-game-office/shot-0.png`, and the quick regression in `output/web-game-office-character-front/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Flattened the eye geometry further after the user called out that the eyeballs still stuck out too much. Recessed the whole eye assembly deeper into the face and replaced the protruding circular iris/pupil pieces with much thinner in-face layers so the eyes read flatter.
- 2026-03-20: Revalidated after the eye-depth fix. Inline script syntax passes, the updated close-up remains in `output/web-game-office-character-front/close-front.png`, the default scene is in `output/web-game-office/shot-0.png`, and the quick regression in `output/web-game-office-character-front/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Removed the pink spray-can prop from the custom character model by deleting the helper and left-hand attachment. The arm/hand silhouette now renders cleanly with no handheld accessory.
- 2026-03-20: Revalidated after the spray-can removal. Inline script syntax passes, the updated close-up remains in `output/web-game-office-character-front/close-front.png`, the default scene is in `output/web-game-office/shot-0.png`, and the quick regression in `output/web-game-office-character-front/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Lifted and shortened the front fringe so the eyes are no longer covered by the hair band. The center front piece sits higher and the side bangs are shorter and shifted outward to keep the eye area readable.
- 2026-03-20: Revalidated after the fringe adjustment. Inline script syntax passes, the updated close-up remains in `output/web-game-office-character-front/close-front.png`, the default scene is in `output/web-game-office/shot-0.png`, and the quick regression in `output/web-game-office-character-front/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Applied a more anatomy-driven face-structure pass using the provided human-face references. Increased the cranial/forehead volume relative to the lower face, added simple brow/midface/cheek planes, tightened the jaw taper, adjusted the eye line lower into the orbital region, and realigned the nose, mouth, philtrum, and ears to read closer to human landmarks.
- 2026-03-20: Revalidated after the anatomy pass. Inline script syntax passes, the updated close-up remains in `output/web-game-office-character-front/close-front.png`, the default scene is in `output/web-game-office/shot-0.png`, and the quick regression in `output/web-game-office-character-front/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Reverted the last anatomy-driven face-structure pass after the user preferred the earlier softer-face version. Restored the previous skull/jaw landmark placement and removed the added forehead, cheek-plane, jaw-corner, nose-base, and philtrum blocks while keeping the later eye-depth, fringe, and prop-removal fixes.
- 2026-03-20: Revalidated after reverting the anatomy pass. Inline script syntax passes, the restored close-up remains in `output/web-game-office-character-front/close-front.png`, the default scene is in `output/web-game-office/shot-0.png`, and the quick regression in `output/web-game-office-character-front/state.json` still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`.
- 2026-03-20: Fixed the hat function for the rebuilt character. Replaced the undersized floating cap geometry with a larger fitted cap and changed the outfit logic so only cap-covered top/bang hair is hidden while the side/back hair stays visible under the hat.
- 2026-03-20: Revalidated after the hat fix. Inline script syntax passes, the cap-on check is in `output/web-game-office-character-front/hat-check.png`, the default scene is in `output/web-game-office/shot-0.png`, and the quick regression in `output/web-game-office-character-front/state.json` still reports `score: 1`, `ballsRemaining: 2`, `bootsPrompt: true`, and `hat: "CAP"`.
- 2026-03-20: Reworked the jump into a staged human-like sequence based on the user’s reference. The controller now uses a longer gather window, a stronger crouch/compression pose, a clearer propulsion extension, a more deliberate airborne silhouette, and a visible landing compression before returning to idle. Movement speed is also reduced harder during the gather so the player plants before takeoff instead of sliding through the crouch.
- 2026-03-20: Revalidated after the jump update with both the required Playwright client and a focused side-view capture run. `output/web-game-jump-client/state-0.json` shows the jump in flight, and `output/web-game-jump-sequence/state.json` now records the full phase progression (`preparing.grounded: true`, `takeoff.grounded: false`, `airborne.grounded: false`, `landing.grounded: true`) while the gameplay regression still reports `score: 1`, `ballsRemaining: 2`, and `bootsPrompt: true`. The phase screenshots are in `output/web-game-jump-sequence/prep.png`, `takeoff.png`, `air.png`, and `landing.png`.
- 2026-03-20: Added a new `GLOSS BLK` boot color option to the style menu. The boot pieces now use a shinier material path with palette-driven `specular` and `shininess`, so the glossy black option reads differently from the existing matte `OBSIDIAN` pair while the other boot colors continue to work.
- 2026-03-20: Revalidated after the glossy-boot update. The required Playwright client still passes in `output/web-game-boots-client/state-0.json`, and the focused UI capture in `output/web-game-boots-gloss/state.json` confirms the selection changes to `GLOSS BLK`, persists across `resetWorld()`, and the quick score regression still reports `score: 1` and `ballsRemaining: 2`. The visual check is in `output/web-game-boots-gloss/shot.png`.
- 2026-03-20: Switched the default boot style from `OBSIDIAN` to `GLOSS BLK` and centralized the default with `DEFAULT_BOOT_STYLE_INDEX` so the initial outfit, boot creation palette, coffee-boot prompt target, and equip status messaging all stay aligned with the glossy black pair.
- 2026-03-20: Revalidated after changing the default boots. The required Playwright client in `output/web-game-boots-default-client/state-0.json` now reports `outfit.boots: "GLOSS BLK"` on load, and the focused prompt check in `output/web-game-boots-default/state.json` confirms `initialBoots: "GLOSS BLK"` plus `promptTarget: "GLOSS BLK"`. Updated visuals are in `output/web-game-boots-default/default-shot.png` and `output/web-game-boots-default/prompt-shot.png`.
- 2026-03-20: Added a dedicated `TOP` outfit selector to the style menu with three switchable outerwear builds: `DENIM JKT`, `BIKER BLK`, and `LTHR TRENCH`. The character now swaps full torso silhouettes instead of only recoloring the old jacket, and sleeve/cuff colors track the selected top so the arms match each look.
- 2026-03-20: Revalidated after the top-outfit update. The required Playwright client in `output/web-game-top-client/state-0.json` now exports `outfit.top: "BIKER BLK"` on load, and the focused capture set in `output/web-game-top-styles/state.json` confirms all three variants render (`DENIM JKT`, `BIKER BLK`, `LTHR TRENCH`) while the quick regression still reports `score: 1` and `ballsRemaining: 2`. Visual checks are in `output/web-game-top-styles/denim-jkt.png`, `biker-blk.png`, and `lthr-trench.png`.
- 2026-03-20: Added a new `EQSTRN BLK` boot selection for a simple glossy black equestrian style. The boot system now supports per-style geometry variants, so the new option swaps the existing strapped biker/combat boot out for a smoother tall equestrian shaft and sleeker sole/heel profile instead of only recoloring the old mesh.
- 2026-03-20: Revalidated after the equestrian-boot update. The required Playwright client in `output/web-game-equestrian-boots-client/state-0.json` still passes, and the focused check in `output/web-game-equestrian-boots/state.json` confirms `boots: "EQSTRN BLK"` while the quick regression still reports `score: 1` and `ballsRemaining: 2`. The visual check is in `output/web-game-equestrian-boots/shot.png`.
- 2026-03-20: Made `EQSTRN BLK` the default boot style and added a matching brown selection `EQSTRN BRN`. The default boot prompt/copy/status text now consistently refers to the black equestrian pair, and the new brown option reuses the same simple equestrian geometry instead of falling back to the biker/combat boot shape.
- 2026-03-20: Revalidated after the equestrian-default and brown-style update. The required Playwright client in `output/web-game-equestrian-default-client/state-0.json` now reports `outfit.boots: "EQSTRN BLK"` on load, and the focused capture in `output/web-game-equestrian-default/state.json` confirms `initialBoots: "EQSTRN BLK"`, `brownBoots: "EQSTRN BRN"`, and `promptTarget: "EQSTRN BLK"` while the quick regression still reports `score: 1` and `ballsRemaining: 2`. Visual checks are in `output/web-game-equestrian-default/default-shot.png`, `brown-shot.png`, and `prompt-shot.png`.
- 2026-03-20: Replaced the old single-box hand stub with fuller hand geometry on both arms and added white gloves as the default hand finish. Each hand now has a palm, finger block, and thumb shape, with white glove shells and a darker glove cuff layered over the base hand form so the character reads as wearing proper gloves rather than bare block hands.
- 2026-03-20: Revalidated after the hand/glove update. The required Playwright client in `output/web-game-gloves-client/state-0.json` still passes, and the focused waved-hand capture in `output/web-game-gloves/state.json` confirms `action: "wave"` during the close visual check while the quick regression still reports `score: 1` and `ballsRemaining: 2`. The glove visual check is in `output/web-game-gloves/wave-shot.png`.
- 2026-03-20: Added an outfit palette panel that only appears in `SELECT MODE` when the player clicks an outfit target. `TOP`, `SHIRT + SHORTS`, and `BOOTS` now have dedicated pick volumes on the character, the palette exposes swatches for the matching style set, and the current palette/selection state is exported through `render_game_to_text`. Also added `window.projectOutfitPicker(part, occurrence)` for deterministic picker targeting in tests.
- 2026-03-20: Revalidated after the outfit-palette update. The required Playwright client still passes in `output/web-game-outfit-palette-client/state-0.json`. The focused palette run in `output/web-game-outfit-palette/state.json` confirms `TOP`, `SHIRT + SHORTS`, and `BOOTS` all select independently, their palette swatches change the equipped style (`DENIM JKT`, `SAND`, `EQSTRN BRN`), and the quick regression still reports `score: 1` and `ballsRemaining: 2`. Visual captures are in `output/web-game-outfit-palette/top-palette.png`, `shirt-palette.png`, and `boots-palette.png`.
- 2026-03-20: Added automatic seating for the lounge sofa and chairs. The office sofa and both lounge chairs now register as seat targets with touch points, exit points, and seat poses; touching one on the floor snaps the player into a seated state, movement/jump stands them back up, and the active seat collider is ignored while seated so the player can stay on the furniture instead of being pushed off.
- 2026-03-20: Revalidated after the seating update. The required Playwright client still passes in `output/web-game-seating-client/state-0.json`. The focused seating run in `output/web-game-seating/state.json` confirms `afterSofaSit.player.action: "sit"` with `seat: "office-sofa"`, `afterStand.player.seated: false`, and `afterChairSit.player.action: "sit"` with `seat: "office-chair-west"`. The quick regression still reaches `score: 1` and `ballsRemaining: 2`. Visual captures are in `output/web-game-seating/sofa-sit.png` and `output/web-game-seating/chair-sit.png`.
- 2026-03-20: Refined the sit pose using the user’s human-sitting reference. Split sofa vs chair posture so the torso, arm bend, hip angle, knee angle, and leg spread read more like a real seated person instead of a rigid perched pose.
- 2026-03-20: Revalidated after the sit-pose refinement. The required Playwright client still passes in `output/web-game-seating-pose-client/state-0.json`. The focused pose check in `output/web-game-seating-pose/state.json` still reports `action: "sit"` for both the sofa and chair cases and preserves the quick score regression; updated captures are in `output/web-game-seating-pose/sofa-front.png`, `sofa-side.png`, and `chair-side.png`.
- 2026-03-20: Added camera zoom controls for the office scene. Mouse-wheel zoom is now enabled on the canvas, dedicated `－ ZOOM` and `＋ ZOOM` action buttons were added to the mobile controls, and the orbit camera now tracks a clamped radius so zoom can be adjusted without breaking the existing drag-orbit or reset-view behavior. The exported render state now includes camera `radius` / `targetRadius`, and `window.setCameraZoom(radius, snap)` was added for deterministic testing.
- 2026-03-20: Revalidated after the zoom update. The required Playwright client still passes in `output/web-game-zoom-client/state-0.json`, and the focused zoom run in `output/web-game-zoom/state.json` confirms the camera moves from the default `radius: 8.06` to `4.6` / `4.3` when zooming in and out to `9.86` when zooming back out while the gameplay regression still reports `score: 1` and `ballsRemaining: 2`. Visual checks are in `output/web-game-zoom/zoom-in.png` and `zoom-out.png`.
- 2026-03-20: Split the old folded seated pose into two separate behaviors. Automatic chair/sofa contact now uses a lower true-seated root offset and a dedicated seat pose, while the old folded-on-the-ground silhouette was preserved as a manual `POSE` selection with `STAND` / `KNEEL` in the style menu. The runtime outfit export and `window.setOutfit()` now include `pose`, so the kneel selection can be driven and tested directly.
- 2026-03-20: Revalidated after the seat-pose and kneel-selector update. The required Playwright client still passes in `output/web-game-seat-pose-client/state-0.json`. The focused pose run in `output/web-game-seat-pose-fix/state.json` now reports `chairState.player.action: "sit"` with `seat: "office-chair-east"`, `sofaState.player.action: "sit"` with `seat: "office-sofa"`, `kneelState.player.action: "kneel"` with `seat: null`, and the quick regression still reaches `score: 1` with `ballsRemaining: 2`. Visual checks are in `output/web-game-seat-pose-fix/chair-front.png`, `sofa-front.png`, and `kneel-front.png`.
- 2026-03-20: Simplified the character face toward the user’s real-person reference. The head now uses a flatter, cleaner front profile with reduced jaw/chin depth, a much simpler eye assembly, subtler brows/nose/lips/ears, and straighter center-part framing so the face reads less like a layered anime build and more like a simple human face at gameplay distance.
- 2026-03-20: Revalidated after the face simplification. The required Playwright client still passes in `output/web-game-face-simple-client/state-0.json`. The focused face run in `output/web-game-face-simple/state.json` shows the simplified front/side face views while the quick regression still reaches `score: 1` with `ballsRemaining: 2`. Visual checks are in `output/web-game-face-simple/crop-front.png` and `crop-side.png`.
- 2026-03-21: Reverted the face simplification and restored the previous fuller face build. This rollback brings back the earlier eye, brow, nose, lip, cheek, ear, and front-hair geometry rather than the flatter simplified real-person version.
- 2026-03-21: Revalidated after the face rollback. The required Playwright client still passes in `output/web-game-face-revert-client/state-0.json`, and the focused face run in `output/web-game-face-revert/state.json` shows the restored front/side face views while the quick regression still reaches `score: 1` with `ballsRemaining: 2`. Visual checks are in `output/web-game-face-revert/crop-front.png` and `crop-side.png`.
- 2026-03-21: Corrected the sitting posture again by changing the seat target offsets and flipping the sit-leg hinge into a true seated configuration. Sofa seating now places the character slightly farther forward on the cushion with thighs projected outward and calves/boots angled down in front of the sofa instead of dropping straight under the body; chair seating was lowered and tightened to match the same forward-thigh/down-shin logic.
- 2026-03-21: Revalidated after the sitting-posture correction. The required Playwright client still passes in `output/web-game-seat-fix-client/state-0.json`. The focused seat run in `output/web-game-seat-fix/state.json` now reports `sofaFront.player.action: "sit"` with `seat: "office-sofa"`, `sofaLow.player.action: "sit"`, `chairFront.player.action: "sit"` with `seat: "office-chair-east"`, and the quick regression still reaches `score: 1` with `ballsRemaining: 2`. Visual checks are in `output/web-game-seat-fix/sofa-front.png`, `sofa-low.png`, and `chair-front.png`.
- 2026-03-21: Pulled the side-hair pieces inward and forward so they overlap the face more in profile. The side curtain strands are now closer to the cheeks, and slim extra face-framing panels were added on both sides so the side hair covers the face in three-quarter and side views without fully blocking the eyes from the front.
- 2026-03-21: Revalidated after the side-hair update. The required Playwright client still passes in `output/web-game-sidehair-client/state-0.json`. The focused hair run in `output/web-game-sidehair/state.json` shows the updated front/side head crops while the quick regression still reaches `score: 1` with `ballsRemaining: 2`. Visual checks are in `output/web-game-sidehair/crop-front.png` and `crop-side.png`.
- 2026-03-21: Fixed raised-ramp edge clipping by adding a raised-surface footprint constraint for the player, AI, and metal spheres. Bodies now clamp inward to the visible top width when they are on or transitioning off the ramp/plateau, which stops boots and balls from half-hanging through the side while still preserving the existing office-floor movement elsewhere. Added `window.movePlayerToWorld()` and `window.moveBodyToWorld()` to make the edge case reproducible in focused browser checks.
- 2026-03-21: Revalidated after the ramp-edge fix. The required Playwright client still passes in `output/web-game-ramp-edge-client/state-0.json`. The focused edge run in `output/web-game-ramp-edge-fix/state.json` now clamps the player to `x: 3.07` and `steel-1` to `x: 2.87` on the ramp instead of leaving them hanging over the side, and the visual checks are in `output/web-game-ramp-edge-fix/shot.png` and `output/web-game-ramp-edge-fix/close.png`.
- 2026-03-21: Ran a quick post-fix score regression in `output/web-game-ramp-edge-regression/state.json`. Scoring `steel-2` still advances to `score: 1`, clears the ball from play (`ballsRemaining: 2`), and leaves the rest of the level state intact after the new raised-surface clamp logic.
- 2026-03-21: Added a new `CHAR` selector with `HUMAN` and `ROBOT` options. The robot variant reuses the existing animation rig but swaps in a white/blue/black humanoid robot shell on the same head, torso, arm, pelvis, and leg pivots so it can walk, jump, kneel, and sit without a second controller. Also added a matching white/blue robotic arm prop to the coffee desk.
- 2026-03-21: Tweaked the player boot geometry so the bottom sole extends slightly past the upper shoe box, which gives both the biker and equestrian boots a cleaner tread overhang in side view.
- 2026-03-21: Revalidated after the robot-selector and boot-sole update. The required Playwright client still passes in `output/web-game-robot-client/state-0.json`, which now exports `outfit.character: "HUMAN"` by default. The focused run in `output/web-game-robot-select/state.json` confirms the robot selection switches to `outfit.character: "ROBOT"` and the quick score regression still reaches `score: 1` with `ballsRemaining: 2`. Visual checks are in `output/web-game-robot-select/robot-front.png`, `coffee-arm-close.png`, and `boot-close.png`.
- 2026-03-21: Extended the boot sole again after checking the side silhouette. The outsole now projects farther than the upper at the toe on both the biker and equestrian boot variants, and the decorative boot prop was updated to match the same tread overhang.
- 2026-03-21: Revalidated after the boot-sole extension. The required Playwright client still passes in `output/web-game-boot-sole-client/state-0.json`, and the focused side-view check in `output/web-game-boot-sole/state.json` keeps the default `EQSTRN BLK` boots while the updated silhouette is visible in `output/web-game-boot-sole/boot-close.png`.
- 2026-03-21: Kept the restored face version and closed the exposed side head gap with thicker side hair shells plus bridge pieces between the curtain strands and the side panel. This change only affects the hair silhouette around the cheek/ear gap; the eye, brow, nose, mouth, and cheek build remain on the previously restored face.
- 2026-03-21: Revalidated after the side-gap hair fix. The required Playwright client still passes in `output/web-game-face-gap-client/state-0.json`, and the focused head check in `output/web-game-face-gap/state.json` keeps the human character on the restored face while the updated side coverage is visible in `output/web-game-face-gap/head-gap-fix.png` and `head-gap-close.png`.
- 2026-03-21: Reworked the robot feature from a skin toggle into a separate world actor. The style-menu `CHAR` row is now a control selector (`CTRL`), a standalone white/blue robot stands near the pantry, and select-mode clicks on the human or robot switch which actor the movement/camera/controller runtime follows instead of swapping the human mesh into a robot shell.
- 2026-03-21: Added per-actor runtime state handoff for the controllable human and robot. Each actor now keeps its own world position, velocity, facing, timers, and seat state; switching control parks the previous actor in place and loads the new actor into the existing movement/jump/seat logic. Also exported `controller`, `human`, and `robot` in `render_game_to_text`, plus new test hooks `window.switchControlledActor()`, `window.projectActorToCanvas()`, and `window.moveControlledActorToWorld()`.
- 2026-03-21: Fixed select-mode priority so inactive actor picks win over overlapping outfit hitboxes. Without this, reclaiming the human while controlling the robot would select `TOP`/`SHIRT + SHORTS` instead of handing control back.
- 2026-03-21: Revalidated after the separate-robot control update. The required Playwright client still passes in `output/web-game-robot-control-client/state-0.json`. The focused selection run in `output/web-game-robot-control/state.json` now confirms `before.controller: "HUMAN"`, `afterRobot.controller: "ROBOT"` after a select-mode click on the robot, and `afterHuman.controller: "HUMAN"` after a select-mode click back on the human. Visual checks are in `output/web-game-robot-control/robot-selected.png` and `human-selected.png`.
- 2026-03-21: Fixed three outfit regressions introduced by the later robot/control changes. `applyCharacterVariant()` was still force-showing all human head and shin child meshes, which overrode `hat` and `shades` visibility and made both biker/equestrian boot meshes render together. Removed that blanket visibility override, kept only the embedded-robot hide path, and added `focusHumanForOutfitEdit()` so non-character style edits pull control back to the human when the standalone robot is active.
- 2026-03-21: Revalidated the outfit regression fix. The required Playwright client passes again in `output/web-game-outfit-fix-client/state-0.json`. The focused UI run in `output/web-game-outfit-fix/state.json` now confirms `afterHatButton.outfit.hat: "CAP"`, `afterHatOff.outfit.hat: "OFF"`, `afterShadesButton.outfit.shades: "BLACK"`, `brownBoots.outfit.boots: "EQSTRN BRN"`, `blackBoots.outfit.boots: "EQSTRN BLK"`, and `afterHatClickWhileRobot.controller: "HUMAN"` after clicking `HAT` while the robot was selected. Visual checks are in `output/web-game-outfit-fix/human-base.png`, `human-hat.png`, `human-shades.png`, `human-boots-black.png`, `human-boots-brown.png`, and `robot-hat-return-human.png`.
- 2026-03-21: Updated the human face and front hair again using the new portrait reference as the target. The head now uses a slimmer jaw/chin, softer brows, cooler gray-green eyes, subtler lips/blush, and a cleaner center-parted long-hair frame instead of the older flatter fringe. This keeps the same block-built rig but shifts the face toward a softer real-person/anime portrait read.
- 2026-03-21: Corrected the sunglasses styling so `SHADES -> BLACK` now renders as black sunglasses instead of the earlier silver/blue frame-and-lens combination. The frame, bridge, bar, temples, and lenses were all moved to black/dark-smoke values with only a faint cool glint left on the lens faces.
- 2026-03-21: Revalidated after the face + shades pass. The required Playwright client still passes in `output/web-game-face-ref-client/state-0.json`. The focused visual run in `output/web-game-face-ref/state.json` keeps `controller: "HUMAN"` and confirms `blackShades.outfit.shades: "BLACK"`. Visual checks are in `output/web-game-face-ref/face-front.png`, `face-three-quarter.png`, `black-shades.png`, and `face-close.png`.
- 2026-03-21: Tuned the equestrian boots to better match the latest reference. Both `EQSTRN BLK` and `EQSTRN BRN` now use matte black/brown palette values instead of glossy highlights, and the equestrian outsole/toe projection was shortened so the bottom sole reads slimmer from the side. The decorative boot pair by the coffee desk was also updated to the same shorter matte silhouette.
- 2026-03-21: Revalidated after the equestrian boot adjustment. The required Playwright client still passes in `output/web-game-equestrian-matte-client/state-0.json`. The focused comparison run in `output/web-game-equestrian-matte/state.json` keeps the human selected and confirms `black.outfit.boots: "EQSTRN BLK"` and `brown.outfit.boots: "EQSTRN BRN"`. Visual checks are in `output/web-game-equestrian-matte/black-side.png` and `brown-side.png`.
- 2026-03-21: Added a cherry-pink lip tint to the human face by recoloring and slightly enlarging the upper/lower lip blocks and deepening the mouth-shadow tone. This keeps the same face structure but makes the lips read more clearly from the front.
- 2026-03-21: Revalidated after the lip-color tweak. The required Playwright client still passes in `output/web-game-lips-client/state-0.json`, and the focused face check in `output/web-game-lips/state.json` keeps the human selected with the updated mouth visible in `output/web-game-lips/face-close.png`.
- 2026-03-21: Lifted the top front hair band and raised/pulled the side bangs outward so the eyes are less covered in the front view. The center-part top pieces now sit higher on the forehead, and the curtain strands are slightly more open at the eye line.
- 2026-03-21: Revalidated after the fringe/eye visibility tweak. The required Playwright client still passes in `output/web-game-hair-eye-client/state-0.json`, and the focused face check in `output/web-game-hair-eye/state.json` keeps the human selected with the updated eye visibility visible in `output/web-game-hair-eye/face-close.png`.
- 2026-03-21: Closed the remaining side hair gap by enlarging the side cover/shell pieces, moving the bridge pieces farther forward, and adding two smaller temple/face-seal overlap panels so the side profile no longer shows the light ear/face strip between the front strand and the rear hair block.
- 2026-03-21: Revalidated after the side-gap closure. The required Playwright client still passes in `output/web-game-sidegap-client/state-0.json`, and the focused side profile check in `output/web-game-sidegap/state.json` keeps the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual check is in `output/web-game-sidegap/side-close.png`.
- 2026-03-21: Rebuilt the human head using the new voxel-face reference as the base design. The face is now much more square/blocky, the hair uses stepped frontal slabs instead of the softer anime fringe, the eyes are simple tall rectangular panels with pale green centers, and the mouth/lips stay in place as a small pink mouth rather than being removed.
- 2026-03-21: Revalidated after the voxel-face rebuild. The required Playwright client still passes in `output/web-game-face-voxel-client/state-0.json`. Focused checks in `output/web-game-face-voxel/state.json` keep the human selected with the new blocky head visible in `face-front.png`, `face-three-quarter.png`, and `face-close.png`.
- 2026-03-21: Opened the voxel face more by shrinking/lifting the center fringe slabs, shortening the middle bang pieces, and pulling the side curtains/strands outward. The face is now less covered by hair while keeping the same blocky stepped-hair design.
- 2026-03-21: Revalidated after the face-opening tweak. The required Playwright client still passes in `output/web-game-face-open-client/state-0.json`, and the focused face check in `output/web-game-face-open/state.json` keeps the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual check is in `output/web-game-face-open/face-close.png`.
- 2026-03-21: Used the latest voxel-anime reference as a style pass on top of the current black-haired character. The face now borrows the example’s bigger bright-blue eyes, clearer stepped fringe chips over the forehead, and a smaller more expressive mouth while keeping the existing black hair and outfit palette.
- 2026-03-21: Revalidated after the style-reference pass. The required Playwright client still passes in `output/web-game-face-example-client/state-0.json`, and the focused face check in `output/web-game-face-example/state.json` keeps the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual checks are in `output/web-game-face-example/face-close.png` and the tighter crop `face-crop.png`.
- 2026-03-21: Updated the hairstyle to follow the previous sample image more directly. Added a stronger front hair band, shorter stepped forehead pieces, and tied side-tail sections on both sides so the silhouette reads closer to the sample’s blocky anime hairstyle instead of a straight hanging cut.
- 2026-03-21: Revalidated after the hairstyle pass. The required Playwright client still passes in `output/web-game-hair-style-client/state-0.json`, and the focused checks in `output/web-game-hair-style/state.json` keep the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual checks are in `output/web-game-hair-style/front.png`, `profile.png`, and the front head crop `face-crop-front.png`.
- 2026-03-21: Reverted the human head away from the recent voxel/Minecraft-inspired face and tied-tail hair experiments. The head is back to the earlier softer black-haired build with the lifted center-part fringe, detailed eyes, cherry lips, and the later side-gap closure pieces preserved.
- 2026-03-21: Revalidated after restoring the pre-voxel face and hair. The required Playwright client still passes in `output/web-game-face-unvoxel-client/state-0.json`, and the focused checks in `output/web-game-face-unvoxel/state.json` keep the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual checks are in `output/web-game-face-unvoxel/face-front.png` and `face-side.png`.
- 2026-03-21: Applied a restrained style pass from the new voxel-skin references without replacing the restored face. The human keeps the same black-haired softer face, but the hair massing now uses a cleaner squared top, fuller straight side/back volume, a stronger lower hem, and slightly simpler front strands.
- 2026-03-21: Revalidated after the reference-informed hair pass. The required Playwright client still passes in `output/web-game-face-reference-client/state-0.json`, and the focused checks in `output/web-game-face-reference/state.json` keep the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual checks are in `output/web-game-face-reference/face-front.png` and `face-side.png`.
- 2026-03-21: Shortened the hair around the cheeks by trimming and raising the front curtain pieces, the inner cheek strands, and the face-seal/temple fill layers. This keeps the side sealed but exposes more of the lower cheek and jawline.
- 2026-03-21: Revalidated after the cheek-hair trim. The required Playwright client still passes in `output/web-game-cheekhair-client/state-0.json`, and the focused checks in `output/web-game-cheekhair/state.json` keep the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual checks are in `output/web-game-cheekhair/face-front.png` and `face-side.png`.
- 2026-03-21: Shortened the two larger front cheek-framing curtain pieces again so their bottom edges sit higher beside the face. This was a targeted trim of the visible front pair only, without changing the rest of the hair mass.
- 2026-03-21: Revalidated after the front curtain trim. The required Playwright client still passes in `output/web-game-cheekhair-short-client/state-0.json`, and the focused check in `output/web-game-cheekhair-short/state.json` keeps the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual check is in `output/web-game-cheekhair-short/face-front.png`.
- 2026-03-21: Shortened the outer side hair columns at the far left and right edges of the face by trimming the side-cover, side-bridge, and side-blend pieces. This was separate from the inner cheek strands and makes the front silhouette narrower at the lower sides.
- 2026-03-21: Revalidated after the outer side-column trim. The required Playwright client still passes in `output/web-game-sidecolumn-short-client/state-0.json`, and the focused check in `output/web-game-sidecolumn-short/state.json` keeps the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual check is in `output/web-game-sidecolumn-short/face-front.png`.
- 2026-03-21: Reverted the recent hair-shortening experiments after they made the front silhouette worse. The front curtain, inner strand, outer side-column, temple-fill, and face-seal pieces are back to the earlier fuller reference-informed hair version.
- 2026-03-21: Revalidated after reverting the trim experiments. The required Playwright client still passes in `output/web-game-hair-trim-revert-client/state-0.json`, and the focused check in `output/web-game-hair-trim-revert/state.json` keeps the human selected with `hat: "OFF"` and `shades: "OFF"`. Visual check is in `output/web-game-hair-trim-revert/face-front.png`.
- 2026-03-21: Added a second autonomous service robot near the coffee desk. It reuses the robot rig with a green-accent chest light, has its own patrol route/state, is exported as `serviceRobot` in the debug state, and is also selectable in cursor/select mode as `SERVICE BOT`.
- 2026-03-21: Revalidated after adding the service robot. The required Playwright client still passes in `output/web-game-service-robot-client/state-0.json`. The focused patrol check in `output/web-game-service-robot/state.json` shows the service robot moving from `x: -6.4, z: 4` to `x: -5.19, z: 6.65` with `action: "walk"`, and the select-mode probe in `output/web-game-service-robot-select/state.json` confirms `interaction.selection.label: "SERVICE BOT"`. Visual checks are in `output/web-game-service-robot/start.png` and `moved.png`.
- 2026-03-21: Added shared character-to-character collision resolution so robots no longer pass through people. This now covers the patrol service robot, the controllable robot, the human, and the rival through one contact pass instead of only the older player-vs-rival collision case.
- 2026-03-21: Revalidated after the robot/person collision fix. The required Playwright client still passes in `output/web-game-robot-people-collision-client/state-0.json`. Focused overlap checks in `output/web-game-robot-people-collision/state.json` show `service.distance: 0.88` and `robotHuman.distance: 0.84`, which keeps both robot cases at or above the collision radius instead of overlapping. Visual checks are in `output/web-game-robot-people-collision/service-vs-human.png` and `robot-vs-human.png`.
- 2026-03-21: Added select-mode object actions for the coffee-desk robotic arm. The arm is now a selectable `COFFEE ARM` object with its own action panel, `GRAB CUP` and `CLEAN DESK` task buttons, a task-driven articulated rig, cup placement logic, and debug helpers/state export so the interaction can be exercised deterministically.
- 2026-03-21: Revalidated after the coffee-arm interaction update. The required Playwright client still passes in `output/web-game-coffee-arm-client/state-0.json`. Focused select-mode checks in `output/web-game-coffee-arm/state.json` show the arm being selected with `interaction.objectActions.active: true`, then switching to `task: "GRAB CUP"` and `task: "CLEAN DESK"`. A completion pass in `output/web-game-coffee-arm-complete/state.json` shows both tasks returning to `task: "READY"` after finishing. Visual checks are in `output/web-game-coffee-arm/selected.png`, `grab-mid.png`, `clean-mid.png`, and `output/web-game-coffee-arm-complete/grab-complete.png`.
- 2026-03-21: Added a `FACE` expression selector for the human character. The face rig now supports `NORMAL`, `HAPPY`, `SURPRISED`, `SAD`, `ANNOYED`, and `ANGRY` by changing brow tilt/height, eyelid openness, iris position, cheek intensity, tears, and mouth shape instead of keeping one static face.
- 2026-03-21: Revalidated after the facial-expression update. The required Playwright client still passes in `output/web-game-face-expression-client/state-0.json`. Focused expression checks in `output/web-game-face-expression/state.json` confirm the human outfit state switches through the new `face` field, and a direct button test in `output/web-game-face-expression-button/state.json` shows the `FACE` button cycling `NORMAL -> HAPPY -> SURPRISED`. Visual checks are in `output/web-game-face-expression/normal.png`, `happy.png`, `surprised.png`, `sad.png`, `angry.png`, plus tighter crops in `output/web-game-face-expression-crops/`.
- 2026-03-23: Reshaped the human head toward a rounder, more sphere-like silhouette by enlarging/rounding the skull and softening the jaw/chin with sphere meshes instead of blocky lower-face boxes. I also retuned the hair cap, crown, back mass, front curtains, and side seal pieces so the hair wraps the new head more naturally instead of reading like detached slabs.
- 2026-03-23: Revalidated after the round-head and hair-wrap update. The required Playwright client still passes in `output/web-game-head-round-client/state-0.json`. Focused silhouette checks are in `output/web-game-head-round/state.json`, with visual checks in `output/web-game-head-round/front.png`, `side.png`, and tighter crops in `output/web-game-head-round-close/front-close.png` and `side-close.png`.
- 2026-03-23: Simplified the human hairstyle after the round-head pass. I reduced the number of visually dominant side/front hair layers, narrowed the curtains and side covers, collapsed the tiny bridge/blend pieces into subtle seals, and trimmed the back mass so the style reads cleaner and less messy.
- 2026-03-23: Revalidated after simplifying the hair. The required Playwright client still passes in `output/web-game-hair-simple-client/state-0.json`. Focused visual checks are in `output/web-game-hair-simple/front.png` and `side.png`, with the matching state in `output/web-game-hair-simple/state.json`.
- 2026-03-23: Rebuilt the human hairstyle again using the tutorial construction notes: a rounded scalp cap sitting off the spherical head, one clear back mass, two cleaner side masses, a simpler center part, and only a few seam pieces to close the temples and cheek gaps. This keeps the hair attached to the head shape while removing the cluttered layered look.
- 2026-03-23: Revalidated after the tutorial-based hair pass. The required Playwright client still passes in `output/web-game-hair-tutorial-client/state-0.json`. Focused silhouette checks are in `output/web-game-hair-tutorial/front.png` and `side.png`, with the matching state in `output/web-game-hair-tutorial/state.json`.
- 2026-03-23: Rebuilt the human head from a simpler robot-like round shell instead of the old multi-part jaw-heavy face. The new human head now uses a spherical skull plus flatter front face shell, smaller simplified facial features, retuned expression anchors, and a cleaner attached hair build so the face and hair are reconstructed on a stable rounded base.
- 2026-03-23: Revalidated after the round-base head rebuild. The required Playwright client still passes in `output/web-game-head-rebuild-client/state-0.json`. Focused visual checks are in `output/web-game-head-rebuild/front.png`, `three-quarter.png`, and `side.png`, with the matching state in `output/web-game-head-rebuild/state.json`.
- 2026-03-23: Reverted the last round robot-base human head rebuild and restored the earlier squarer human head, face proportions, expression anchors, and attached hair layout that were in place before this turn.
- 2026-03-23: Revalidated after restoring the earlier squared-style head. The required Playwright client still passes in `output/web-game-head-square-revert-client/state-0.json`. Focused visual checks are in `output/web-game-head-square-revert/front.png`, `three-quarter.png`, and `side.png`, with the matching state in `output/web-game-head-square-revert/state.json`.
- 2026-03-23: Retuned the human head toward the supplied portrait reference while keeping the current in-code character system. I slimmed the lower face, softened the jaw/chin proportions, narrowed the brows/eyes, reduced the mouth, shifted the eye palette toward gray-green, and rebuilt the fringe/center part so the hair reads more like long straight black hair framing the face.
- 2026-03-23: Revalidated after the portrait-reference head pass. The required Playwright client still passes in `output/web-game-head-photo-ref-client/state-0.json`. Focused checks are in `output/web-game-head-photo-ref/front.png`, `three-quarter.png`, and `side.png`, with closer checks in `output/web-game-head-photo-ref-close/front-close.png`, `three-quarter-close.png`, and `side-close.png`. Matching state is in `output/web-game-head-photo-ref/state.json`.
- 2026-03-23: Reconstructed the human head, face, and hair to match the supplied voxel/anime skin references instead of the softer portrait look. The human now uses a square pale voxel head, flatter face plate, white block hair masses, blunt front bangs, pink eyes/blush, and a dark side ribbon so the silhouette reads like the example images.
- 2026-03-23: Revalidated after the voxel head reconstruction and follow-up bang/ribbon visibility pass. The required Playwright client still passes in `output/web-game-voxel-head-client/state-0.json`, and focused checks are in `output/web-game-voxel-head/front.png`, `three-quarter.png`, and `side.png`, with closer checks in `output/web-game-voxel-head-close/front-close.png`, `three-quarter-close.png`, and `side-close.png`. Matching state is in `output/web-game-voxel-head/state.json` and `output/web-game-voxel-head-close/state.json`.
- 2026-03-23: Corrected the voxel-reference hair palette after the user pointed out the reference hair should be black, not white. The head/face/hair geometry is unchanged; only the main, shade, and deep hair colors were switched back to a black palette.
- 2026-03-23: Revalidated after the black-hair correction. The required Playwright client still passes in `output/web-game-voxel-head-black-client/state-0.json`, and focused close checks are in `output/web-game-voxel-head-black/front-close.png`, `three-quarter-close.png`, and `side-close.png`, with matching state in `output/web-game-voxel-head-black/state.json`.
- 2026-03-23: Rebuilt the black shades rig for the current voxel head. The old glasses were still using the previous face proportions, so I narrowed the front frame, moved it back onto the eye line, shortened the bridge/top bar, and changed the temple arms to hinge from the frame sides and run back toward the ear instead of projecting forward in profile.
- 2026-03-23: Revalidated after the shades fit fix. The required Playwright client still passes in `output/web-game-shades-fit-client/state-0.json`, and focused shades-on checks are in `output/web-game-shades-fit/front-close.png`, `three-quarter-close.png`, and `side-close.png`, with matching state in `output/web-game-shades-fit/state.json`.
- 2026-03-23: Softened the voxel face using the supplied expression atlas references. I switched the pupils to black, replaced the pink iris palette with layered blue irises, darkened the lid lines, narrowed and softened the brows, reduced the nose/lip mass, and toned down the blush so the default face reads calmer and less creepy while still keeping the blocky style.
- 2026-03-24: Added two more glass meeting rooms to the office shell using the existing room builder. The new west-side rooms are `STUDIO` near the left-front window lane and `POD` near the left-back circulation lane, both rotated to open toward the center of the office.
- 2026-03-24: Revalidated the extra meeting rooms. The required Playwright client passes in `output/web-game-extra-meeting-rooms-client/state-0.json`, and focused visual checks are in `output/web-game-extra-meeting-rooms/rooms-angle.png` and `output/web-game-extra-meeting-rooms/rooms-left.png`.
- 2026-03-24: Moved the two newly added meeting rooms after the previous placement was on the wrong side. `STUDIO` and `POD` now sit in the east/back empty corner of the office (`x/z` around `12.8/15.0` and `18.6/15.2`) so they fill the open corner instead of the west-side circulation lane.
- 2026-03-24: Revalidated the corrected meeting-room placement. The required Playwright client still passes in `output/web-game-extra-meeting-rooms-client/state-0.json`, the default view in `output/web-game-extra-meeting-rooms-client/shot-0.png` now shows the added rooms in the back corner, and the focused angle is `output/web-game-extra-meeting-rooms/rooms-empty-corner-corrected.png`.
- 2026-03-24: Added a bicycle rack with two parked bikes to the open front-right corner of the office. The new rack sits at about `x/z = 20.9 / -9.4` with its own collider so it behaves like a real corner fixture instead of a decal.
- 2026-03-24: Revalidated the bike-rack addition. The required Playwright client passes in `output/web-game-bike-rack-client/state-0.json`, and the focused corner view is `output/web-game-bike-rack/rack-close.png`.
- 2026-03-24: Added a footwear rack next to the bike rack in the same front-right corner. The new display uses four shelves with 12 pairs total: 6 black/brown boot pairs across the lower shelves and 6 white sneaker pairs across the upper shelves, plus a shared collider for the whole fixture.
- 2026-03-24: Revalidated the footwear-rack addition. A clean required Playwright client run passes in `output/web-game-footwear-rack-client-clean/state-0.json` with no page errors, and focused checks are in `output/web-game-footwear-rack/footwear-rack-close.png` and `output/web-game-footwear-rack/footwear-rack-tight.png`.
- 2026-03-24: Increased the vertical spacing between the footwear-rack shelves by raising the overall rack height and redistributing the four shelf levels. This gives each footwear layer more breathing room instead of stacking the rows too tightly.
- 2026-03-24: Revalidated the footwear-rack spacing update. The required Playwright client passes in `output/web-game-footwear-rack-spacing-client/state-0.json` with no page errors, and the updated tight corner view is `output/web-game-footwear-rack-spacing/footwear-rack-tight.png`.
- 2026-03-24: Moved the footwear rack deeper toward the wall in the front-right corner, keeping it beside the bike rack but pushing the shelf unit back to sit closer to the plain wall the player was facing.
- 2026-03-24: Revalidated the footwear-rack move. The required Playwright client passes in `output/web-game-footwear-rack-move-client/state-0.json` with no page errors, and the updated corner view is `output/web-game-footwear-rack-move/footwear-rack-tight.png`.
- 2026-03-24: Pushed the footwear rack fully back so it sits attached to the plain wall segment in that corner while keeping its shelves parallel to the wall line.
- 2026-03-24: Revalidated the wall-attached footwear-rack placement. The required Playwright client passes in `output/web-game-footwear-rack-wall-client/state-0.json` with no page errors, and the updated corner view is `output/web-game-footwear-rack-wall/footwear-rack-tight.png`.
- 2026-03-23: Revalidated after the face softening pass. The required Playwright client still passes in `output/web-game-face-atlas-client/state-0.json`, and focused face checks are in `output/web-game-face-atlas/face-front.png`, `face-three-quarter.png`, and `face-side.png`, with matching state in `output/web-game-face-atlas/state.json`.
- 2026-03-23: Fixed the ear placement for the current voxel head. The ears were sitting too far out and too far back, so I moved the anchors inward/forward, reduced the ear size, and tightened the inner/tip pieces so they sit against the head and stay mostly inside the hair silhouette.
- 2026-03-23: Revalidated after the ear placement fix. The required Playwright client still passes in `output/web-game-ears-fit-client/state-0.json`, and focused ear checks are in `output/web-game-ears-fit/side-close.png`, `three-quarter-close.png`, and `rear-three-quarter.png`, with matching state in `output/web-game-ears-fit/state.json`.
- 2026-03-23: Removed the split vertical bang-bar look from the front hair. I added a continuous front fringe piece and trimmed/repositioned the older narrow bang blocks so the face no longer shows through as multiple vertical pale bars.
- 2026-03-23: Revalidated after the fringe cleanup. The required Playwright client still passes in `output/web-game-fringe-fix-client/state-0.json`, and focused checks are in `output/web-game-fringe-fix/front-close.png` and `three-quarter-close.png`, with matching state in `output/web-game-fringe-fix/state.json`.
- 2026-03-23: Extended the side face-framing hair to cover the convex cheek/face area in profile. I moved the front curtains forward, enlarged the face-seal pieces, and added deeper cheek-cover panels so the side silhouette reads more naturally with hair overlapping the face edge.
- 2026-03-23: Revalidated after the side-cover pass. The required Playwright client still passes in `output/web-game-sidecover-client/state-0.json`, and focused profile checks are in `output/web-game-sidecover/side-close.png` and `three-quarter-close.png`, with matching state in `output/web-game-sidecover/state.json`.
- 2026-03-23: Rebuilt the human head and face toward the supplied portrait reference while staying inside the current in-code character system. I narrowed the skull/jaw/face plate, removed the ribbon, changed the hair to a cleaner long center-part shape, moved the eyes inward into a softer almond layout, and retuned the nose and lips toward a calmer photoreference-style face.
- 2026-03-23: Revalidated after the portrait-style rebuild. The required Playwright client still passes in `output/web-game-photoface-client/state-0.json`, and focused checks are in `output/web-game-photoface/front-close.png`, `three-quarter-close.png`, and `side-close.png`, with matching state in `output/web-game-photoface/state.json`.
- 2026-03-23: Added two more autonomous patrol robots alongside the existing service bot. The new robots reuse the same service-bot rig and movement logic, carry chest badges `01` and `02`, expose their own select-mode pickers/state payloads, and participate in the shared character collision pass so they do not phase through people or each other.
- 2026-03-23: Revalidated after the extra patrol robots were added. The required Playwright client passes in `output/web-game-extra-robots-client/state-0.json`, which now reports `serviceRobot01.chestCode: "01"` and `serviceRobot02.chestCode: "02"`. The focused patrol check in `output/web-game-extra-robots/state.json` shows all three service robots moving with travel distances of `2.7`, `3.29`, and `3.07`, and the visual checks are in `output/web-game-extra-robots/shot.png` and `output/web-game-extra-robots/robots-close.png`.
- 2026-03-23: Rolled the human head and fringe back toward the earlier portrait-reference version after the user rejected the narrower split-bang look. I widened the skull/jaw/face plate, simplified the center fringe, pushed the outer bangs back to a cleaner curtain shape, and widened the eye/brow anchors so the face no longer reads as thin vertical bars.
- 2026-03-23: Revalidated after the head rollback. The required Playwright client still passes in `output/web-game-face-revert-check-client/state-0.json`. Focused visual checks are in `output/web-game-face-revert-check/front-close.png`, `crop.png`, and `output/web-game-face-rollback/shot.png`, with matching state in `output/web-game-face-revert-check/state.json` and `front-state.json`.
- 2026-03-23: Closed the side-view hair gap on the restored portrait-style head. I extended the side hair shell forward, added a dedicated slim profile fill panel, and moved the bridge/temple/face-seal pieces forward so the side silhouette no longer exposes a bright face strip between the front curtain and back hair mass.
- 2026-03-23: Revalidated after the side-gap repair. The required Playwright client still passes in `output/web-game-sidegap-fixed-client/state-0.json`, and the focused side-view check is in `output/web-game-sidegap-fixed/side.png` with matching state in `output/web-game-sidegap-fixed/state.json`.
- 2026-03-23: Enlarged the human eye geometry and softened the neutral face preset for a slightly bigger, cuter look. I widened/tallened the sclera, iris, and pupil meshes, pulled the eye and brow anchors a bit inward, and relaxed the neutral brow/lid settings so the face reads softer instead of sharper.
- 2026-03-23: Revalidated after the eye pass. The required Playwright client still passes in `output/web-game-eyes-cute-client/state-0.json`, and focused face checks are in `output/web-game-eyes-cute-close/face-close.png`, `face-head-crop.png`, and `face-head-tight.png` with matching state in `output/web-game-eyes-cute-close/state.json`.
- 2026-03-23: Removed the thin vertical front bang-bar look from the human hairstyle. I widened the top fringe, shortened the long front bang columns, and turned the two center pieces into small angled fringe chips so the face is no longer split by narrow vertical hair strips.
- 2026-03-23: Revalidated after the fringe cleanup. The required Playwright client still passes in `output/web-game-hair-no-bars-client/state-0.json`, and focused face checks are in `output/web-game-hair-no-bars/face-close.png` and `face-head-tight.png` with matching state in `output/web-game-hair-no-bars/state.json`.
- 2026-03-23: Warmed and darkened the human skin palette so the face no longer reads overbright. I lowered the base skin and shadow colors, replaced the bright face plate with a closer beige tone, and matched the nose pieces to the new palette so the face sits more naturally against the neck and body.
- 2026-03-23: Revalidated after the skin-tone adjustment. The required Playwright client still passes in `output/web-game-skin-natural-client/state-0.json`, and focused face checks are in `output/web-game-skin-natural/face-close.png` and `face-head-tight.png` with matching state in `output/web-game-skin-natural/state.json`.
- 2026-03-23: Simplified the lower face/neck stack after the user called out the redundant extra block under the face. I shrank the chin into a thinner under-face detail and reduced the exposed neck width/height so the center area reads as a normal neck instead of an extra added block.
- 2026-03-23: Revalidated after the lower-face cleanup. The required Playwright client still passes in `output/web-game-neck-clean-client/state-0.json`, and focused face checks are in `output/web-game-neck-clean/face-close.png` and `face-head-tight.png` with matching state in `output/web-game-neck-clean/state.json`.
- 2026-03-23: Removed the visible human neck mesh entirely after the user asked to remove the neck part. The shared `neck` node is now just a hidden transform anchor for the robot neck rig, so the human no longer renders a separate neck block.
- 2026-03-23: Revalidated after removing the human neck mesh. The required Playwright client still passes in `output/web-game-neck-removed-client/state-0.json`, and focused face checks are in `output/web-game-neck-removed/face-close.png` and `face-head-tight.png` with matching state in `output/web-game-neck-removed/state.json`.
- 2026-03-23: Added a dark inner collar bridge on the human torso to visually attach the head back to the body after the neck mesh was removed. This closes the seam without restoring a visible skin neck, and it leaves the shared robot neck anchor untouched.
- 2026-03-23: Revalidated after the head-to-body attachment fix. The required Playwright client still passes in `output/web-game-head-attached-client/state-0.json`, and focused checks are in `output/web-game-head-attached/face-close.png` and `face-head-tight.png` with matching state in `output/web-game-head-attached/state.json`.
- 2026-03-23: Removed the dark inner collar bridge after the user clarified that the center block itself was the unwanted part. This reverts that single attachment mesh and leaves the rest of the torso/head setup unchanged.
- 2026-03-23: Revalidated after removing the collar bridge. The required Playwright client still passes in `output/web-game-collar-removed-client/state-0.json`, and focused checks are in `output/web-game-collar-removed/face-close.png` and `face-head-tight.png` with matching state in `output/web-game-collar-removed/state.json`.
- 2026-03-23: Lowered the shared player head anchor so the human head now sits directly on the torso instead of floating above it. To avoid breaking the player robot variant, I offset the embedded robot head back up by the same amount locally.
- 2026-03-23: Revalidated after the head-anchor adjustment. The required Playwright client still passes in `output/web-game-head-anchor-client/state-0.json`, and focused human/robot checks are in `output/web-game-head-anchor/human-face-close.png`, `human-head-tight.png`, and `robot-face-close.png` with matching state in `output/web-game-head-anchor/state.json`.
- 2026-03-23: Removed the side hair ribbon accessory completely after the user pointed out the remaining brown piece. The ribbon colors and the entire ribbon group were deleted so the accessory cannot come back through outfit visibility updates.
- 2026-03-23: Revalidated after removing the ribbon. The required Playwright client still passes in `output/web-game-ribbon-removed-client/state-0.json`, and focused human checks are in `output/web-game-ribbon-removed/human-face-close.png` and `human-head-tight.png` with matching state in `output/web-game-ribbon-removed/state.json`.
- 2026-03-23: Repositioned the three autonomous patrol robots so they start in distinct office zones: one at the coffee bar, one in the back-right corner, and one at the glass meeting-room frontage. I also rewrote each patrol route to stay around its new local area instead of the old shared pantry-side loop.
- 2026-03-23: Revalidated after the patrol-spawn update. The required Playwright client still passes in `output/web-game-robot-starts-client/state-0.json`, and the focused state check in `output/web-game-robot-starts/state.json` confirms initial spawns at `(-7.2, 5.0)`, `(17.6, 18.6)`, and `(14.2, 9.0)` with all three still advancing along their routes.
- 2026-03-23: Adjusted the human shoulder-to-head proportion toward the new office portrait reference by narrowing the head mass slightly and broadening the torso, jacket tops, and shoulder/arm spread. This keeps the human silhouette from reading top-heavy while preserving the existing outfit system.
- 2026-03-23: Revalidated after the proportion pass. The required Playwright client still passes in `output/web-game-shoulder-ratio-client/state-0.json`, and focused front checks are in `output/web-game-shoulder-ratio/shot.png` and `output/web-game-shoulder-ratio-close/close.png` with matching state in `output/web-game-shoulder-ratio/state.json`.
- 2026-03-23: Refined the human shoulder-to-head ratio again after the user clarified the target rule: one shoulder should read about one head-width. I narrowed the jacket/arm spread back in and restored a little head width so the silhouette is less exaggerated.
- 2026-03-23: Revalidated after the ratio refinement. The required Playwright client still passes in `output/web-game-shoulder-ratio-2-client/state-0.json`, and the updated front check is in `output/web-game-shoulder-ratio-2/shot.png` with matching state in `output/web-game-shoulder-ratio-2/state.json`.
- 2026-03-23: Removed the visible human shoulder-pad block after the user pointed out the side silhouette. The separate top-of-arm shoulder mesh is gone, the upper arm was lifted slightly to keep the sleeve attached cleanly, and the outfit recolor hook now targets the upper arm directly instead of the removed pad.
- 2026-03-23: Revalidated after removing the shoulder pad. The required Playwright client passes in `output/web-game-shoulder-pad-remove-client/state-0.json`, and the focused side check is in `output/web-game-shoulder-pad-remove/side.png` with matching state in `output/web-game-shoulder-pad-remove/state.json`.
- 2026-03-23: Fixed the human ear placement after the user pointed out that the ears were detached from the head. I moved both ears inward and slightly back under the hair shell, and reduced the ear box sizes so they sit against the skull instead of floating outside it.
- 2026-03-23: Revalidated after the ear-placement fix. The required Playwright client passes in `output/web-game-ear-fix-client/state-0.json`, and focused checks are in `output/web-game-ear-fix/head-close.png` and `output/web-game-ear-fix-tight/head-tight.png` with matching state in `output/web-game-ear-fix-tight/state.json`.
- 2026-03-23: Cleaned up the human hat geometry after the user pointed out the oversized side profile and the hovering top piece. I removed the extra top hair crown when the hat is off, then rebuilt the cap into a lower, flatter fitted shape with a much shorter brim and tighter side panels so it no longer reads like a floating box.
- 2026-03-23: Revalidated after the hat cleanup. The required Playwright client passes in `output/web-game-hat-fix-3-client/state-0.json`, and the focused checks are in `output/web-game-hat-fix-3/hat-side.png` and `output/web-game-hat-fix-3/hat-off-front.png` with matching state in `output/web-game-hat-fix-3/state.json`.
- 2026-03-24: Added a collapsible `SPECIAL MOVES` menu to the action cluster with timed `MOONWALK` and `SPIN` options. The panel now expands/collapses in-place, shows the active move plus remaining time, and drives dedicated human and robot performance poses instead of dropping back to idle.
- 2026-03-24: Fixed a special-move starter recursion bug caused by the exported debug helper shadowing the internal starter function. Revalidated with the required Playwright client in `output/web-game-special-moves-client/state-0.json` and focused captures in `output/web-game-special-moves/menu-open.png`, `moonwalk.png`, `spin.png`, and `menu-closed.png`, with matching state in `output/web-game-special-moves/state.json` and no logged console/page errors.
- 2026-03-24: Reworked the human head volume so it reads more like a cube from the side instead of a flat slab. I increased the skull, jaw, and chin depth, pushed the face plate slightly forward, and expanded the surrounding top/back/side hair shell to match the deeper head block.
- 2026-03-24: Revalidated after the head-volume pass. The required Playwright client passes in `output/web-game-head-cube-client/state-0.json`, and the focused front/side checks are in `output/web-game-head-cube/front-close.png` and `side-close.png`, with matching state in `output/web-game-head-cube/state.json` and no logged console/page errors.
- 2026-03-24: Rebuilt the moonwalk into a real heel-toe cycle instead of a symmetric backward walk. The human now alternates a flatter sliding foot with an opposite raised-heel support foot, includes a slower step cadence, and uses a subtler upper-body sway; the robot moonwalk was updated to the same pattern in a simpler form.
- 2026-03-24: Revalidated after the moonwalk rewrite. The required Playwright client passes in `output/web-game-moonwalk-fix-client/state-0.json`, and focused moonwalk checks are in `output/web-game-moonwalk-fix/side-a.png`, `side-b.png`, and `three-quarter.png`, with matching state in `output/web-game-moonwalk-fix/state.json` and no logged console/page errors.
- 2026-03-24: Added a master `PANELS` toggle button that collapses and re-expands the control UI as a group. It hides the controls legend, style panels, d-pad, action rows, and special-moves panel while keeping the master toggle visible so the user can reopen everything.
- 2026-03-24: Revalidated after the control-panels toggle was added. The required Playwright client passes in `output/web-game-control-panels-client/state-0.json`, and focused checks are in `output/web-game-control-panels/expanded.png`, `collapsed.png`, and `expanded-again.png`, with matching state in `output/web-game-control-panels/state.json` and no logged console/page errors.
- 2026-03-24: Turned the footwear rack into a selectable shoe-switch station. Each displayed pair now has its own select-mode picker, the lower rack boot lineup maps to real wearable styles including a new `SADDLE BRN`, and the white sneaker rows switch the player into a new `WHITE SNKR` footwear variant instead of only selecting the shelf collider.
- 2026-03-24: Revalidated after the footwear-rack selection update. The required Playwright client passes in `output/web-game-footwear-select-client/state-0.json`, and the focused rack interaction check in `output/web-game-footwear-rack-select/state.json` confirms a boot-rack click switches the outfit to `SADDLE BRN` and a sneaker-rack click switches it to `WHITE SNKR`, with matching screenshots in `output/web-game-footwear-rack-select/brown-selected.png` and `sneaker-selected.png`.
- 2026-03-24: Added a selectable outfit rack against the empty wall beside the shoe rack. The new rack displays tops, bottoms, and hats as physical rack items, and each item now switches the matching human outfit slot directly in `SELECT MODE`. The bottoms row maps to the existing `shirt` outfit state because that slot still controls the shirt+shorts palette together.
- 2026-03-24: Revalidated after the outfit-rack addition. The required Playwright client passes in `output/web-game-outfit-rack-client/state-0.json`, and the focused rack interaction check in `output/web-game-outfit-rack/state.json` confirms selecting a top changes `top` to `DENIM JKT`, selecting a bottoms stack changes `shirt` to `SAND`, and selecting the hat display changes `hat` to `CAP`, with matching screenshots in `output/web-game-outfit-rack/top-selected.png` and `bottom-hat-selected.png`.
- 2026-03-25: Split the lower outfit into a real `PANTS` slot with three styles: `LT BLUE`, `WHITE`, and `BLACK`. The style menu now has a dedicated pants row, the outfit palette supports pants separately from the shirt, the outfit rack bottoms row now targets the pants slot instead of the old shirt+shorts palette, and the leg overlays switch automatically between tucked and untucked geometry based on the selected footwear variant.
- 2026-03-25: Revalidated after the pants-slot update. The required Playwright client passes in `output/web-game-pants-client/state-0.json`, and the focused interaction check in `output/web-game-pants/state.json` confirms menu cycling from `BLACK -> LT BLUE -> WHITE`, rack selection switching pants to `WHITE`, and footwear-driven tuck behavior for `EQSTRN BLK` versus `WHITE SNKR`, with matching screenshots in `output/web-game-pants/rack-white-selected.png`, `boots-tucked.png`, and `sneakers-untucked.png`.
- 2026-03-25: Added a custom bar chair at the back kitchen island. The new chair uses a dark bar-stool seat, four tall glossy boot legs, a footrest ring, and a monitor as the chair back, and it is parented to the island so it follows the island angle cleanly.
- 2026-03-25: Revalidated after the kitchen bar-chair addition. The required Playwright client passes in `output/web-game-kitchen-bar-chair-client/state-0.json`, and focused kitchen-angle checks in `output/web-game-kitchen-bar-chair/state.json` show the new prop clearly in `angle-a.png`, `angle-b.png`, and `angle-c.png` with no logged console/page errors.
- 2026-03-25: Expanded the seating system so every chair-like prop registered through the shared chair builders can now be sat on, including the lounge chairs, meeting-room chairs, and the kitchen boot bar chair. I also turned those movable chair props into select-mode objects with chair-specific action copy and keyboard nudging, so selecting a chair in `SELECT MODE` lets the user reposition it instead of only highlighting it.
- 2026-03-25: Added client-side user location persistence with per-user localStorage snapshots plus a SQL-shaped export helper for later backend integration. The current controlled actor, both actor coordinates, rotation, and active seat restore on refresh and on user-session switches, and the persistence payload now exposes the target backend table as `user_scene_locations`.
- 2026-03-25: Revalidated after the seating, chair-move, and persistence update. The required Playwright client passes in `output/web-game-chair-persist-client-final/state-0.json`; focused seating and movement checks in `output/web-game-chair-persist-current/state.json` confirm `office-chair-west`, `board-chair-0-1`, and `back-kitchen-boot-chair` all enter `sit`, and the selected boot bar chair moves from `(-11.85, 18.15)` to `(-11.17, 17.81)` in select mode; refresh/session persistence plus the SQL export record are verified in `output/web-game-chair-persist-final/state.json`.
- 2026-03-25: Corrected the kitchen boot bar-chair seating profile after the user pointed out the character was not actually sitting on the stool. The bar chair now registers as a dedicated `stool` seat type with a seat point farther back on the cushion, a much higher sit offset, and a more vertical stool-specific leg pose instead of reusing the normal chair pose.
- 2026-03-25: Revalidated after the stool-seat fix. The required Playwright client passes in `output/web-game-stool-seat-client/state-0.json`, and the focused check in `output/web-game-stool-seat/state.json` confirms the player still enters `sit` on both `back-kitchen-boot-chair` and `office-chair-west`, with updated visuals in `output/web-game-stool-seat/stool-side.png` and `output/web-game-stool-seat/office-chair.png`.
- 2026-03-25: Corrected the glass meeting-room chair seating bug by turning those chairs into a dedicated `meeting-chair` seat type. I enlarged the seat/back geometry, added arm supports, moved the seat point slightly inward, and gave meeting chairs their own sit offsets and leg/arm profile so the avatar no longer perches unrealistically on the edge.
- 2026-03-25: Revalidated after the meeting-chair fix. The required Playwright client passes in `output/web-game-meeting-chair-fix-client/state-0.json`, and the focused check in `output/web-game-meeting-chair-fix/state.json` confirms `board-chair-0-1` and `office-chair-west` still enter `sit`, with updated visuals in `output/web-game-meeting-chair-fix/meeting-chair.png` and `output/web-game-meeting-chair-fix/office-chair.png`.
- 2026-03-25: Refined the kitchen boot bar-chair seat anchor again after the user clarified that the hips, not the torso center, should be the contact point. I moved the stool seat position farther back under the pelvis and reduced the stool-specific forward lean so the body reads as sitting from the hips instead of perching on the front edge.
- 2026-03-25: Revalidated after the stool hip-anchor refinement. The required Playwright client passes in `output/web-game-stool-hip-client/state-0.json`, and the focused stool check in `output/web-game-stool-hip/state.json` still confirms `back-kitchen-boot-chair` enters `sit`, with the updated side render in `output/web-game-stool-hip/stool-side.png`.
- 2026-03-25: Reworked the kitchen boot bar-chair sit pose from the side-view sitting references instead of only tweaking offsets. The stool seat point now sits farther back on the cushion, the seated root is lower, the human and robot torso/head groups reset to clean defaults each frame, and the stool-only pose shifts the body mass rearward with a slightly pitched torso, calmer arm hang, and a hip-led seated chain rather than a stacked perch.
- 2026-03-25: Revalidated after the stool reference-based seating fix. The required Playwright client passes in `output/web-game-stool-reference-fix-client/state-0.json`, the focused stool check in `output/web-game-stool-reference-fix/state.json` still confirms `back-kitchen-boot-chair` enters `sit`, and the tighter side inspection in `output/web-game-stool-reference-fix-tight/state.json` is captured in `output/web-game-stool-reference-fix/stool-side.png`, `output/web-game-stool-reference-fix/office-chair.png`, and `output/web-game-stool-reference-fix-tight/stool-side-tight.png`.
- 2026-03-25: Corrected the stool-pose direction after the user pointed out the previous hip hinge was reversed. I flipped the stool-only torso/head local offsets and pitch so the body now leans the correct way relative to the chair back and island, while keeping the pelvis seated on the stool.
- 2026-03-25: Revalidated after the stool direction correction. The required Playwright client passes in `output/web-game-stool-reference-fix-v2-client/state-0.json`, and the updated focused side view is captured in `output/web-game-stool-reference-fix-v2/stool-side-tight.png` with the matching state in `output/web-game-stool-reference-fix-v2/state.json`.
- 2026-03-25: Raised the stool sit height after the user pointed out the avatar was sinking into the seat. I kept the corrected stool hinge direction, but lifted the stool visual offset so the hips sit on top of the pad instead of intersecting it.
- 2026-03-25: Revalidated after the stool seat-height correction. The required Playwright client passes in `output/web-game-stool-reference-fix-v3-client/state-0.json`, and the updated tight side inspection is in `output/web-game-stool-reference-fix-v3/stool-side-tight.png` with matching state in `output/web-game-stool-reference-fix-v3/state.json`.
- 2026-03-25: Rebuilt the human cap after the user pointed out it looked like a face clamp instead of a hat. I removed the low wraparound band, lifted the cap group higher on the skull, and simplified the shell, brim, and front panel so the hat now sits on top of the head.
- 2026-03-25: Revalidated after the hat rebuild. The required Playwright client passes in `output/web-game-hat-fix-4-client/state-0.json`, and the focused hat checks are in `output/web-game-hat-fix-4/hat-close.png` plus `output/web-game-hat-fix-4-close/hat-front-close.png` and `output/web-game-hat-fix-4-close/hat-side-close.png`.
- 2026-03-25: Reverted the custom boot/monitor bar stool from the back kitchen after the user asked to return to the version without that chair. I removed the custom stool builder and its island placement/seat registration so the kitchen island is clear again.
- 2026-03-25: Revalidated after removing the custom bar stool. The updated kitchen view is in `output/web-game-kitchen-no-barstool/kitchen.png`, and the focused state check in `output/web-game-kitchen-no-barstool/state.json` confirms the scene loads cleanly with the player standing and no active seat.
- 2026-03-25: Fixed the pants-to-boots color inconsistency on the human rig. The exposed leg filler pieces above the boots were still using skin colors, so I retuned the thigh/shin pant overlays to overlap more around the knee and made the thigh, knee, and shin filler meshes inherit the active pants palette instead of showing skin through the clothing stack.
- 2026-03-25: Revalidated after the pants/boot consistency fix. Focused black and white pants checks are in `output/web-game-pants-leg-color-fix/black.png` and `output/web-game-pants-leg-color-fix/white.png`, with matching outfit state in `output/web-game-pants-leg-color-fix/state.json` confirming `EQSTRN BLK` boots now keep the visible leg section consistent with the selected pants color.
- 2026-03-25: Ran the standard Playwright client after the pants-leg fix as a regression check. `output/web-game-pants-leg-color-fix-client/state-0.json` still loads cleanly with the human in `BLACK` pants and `EQSTRN BLK` boots, and the base screenshot is in `output/web-game-pants-leg-color-fix-client/shot-0.png`.
- 2026-03-25: Fixed chair/sofa seat sinking by centralizing per-seat visual offsets instead of relying on deeper hard-coded fallback sinks. I added `defaultSeatVisualOffset(kind)` and raised the registered lounge-chair, sofa, and meeting-chair offsets so the pelvis sits higher on the cushion.
- 2026-03-25: Revalidated the seat-height fix with focused sit checks in `output/web-game-seat-height-fix/state.json`, `output/web-game-seat-height-fix/chair.png`, and `output/web-game-seat-height-fix/sofa.png`, plus alternate angles in `output/web-game-seat-height-fix-clean/chair-clean.png` and `output/web-game-seat-height-fix-clean/sofa-clean.png`. The standard Playwright client still loads cleanly in `output/web-game-seat-sink-fix-client/state-0.json`.
- 2026-03-25: Retuned the lounge seating after the first seat-height pass overshot and made the avatar float above the cushion. The lounge chairs are now at `-0.24`, the sofa at `-0.18`, and the meeting chairs at `-0.20`, with the same values used by both the passive-pose and live animation sit paths.
- 2026-03-25: Revalidated the seating retune with focused checks in `output/web-game-seat-height-fix-v2/state.json`, `output/web-game-seat-height-fix-v2/chair.png`, and `output/web-game-seat-height-fix-v2/sofa.png`, and the standard client still loads cleanly in `output/web-game-seat-height-fix-v2-client/state-0.json`.
- 2026-03-25: Reworked the human seated posture instead of only shifting seat offsets. For sofas, lounge chairs, and meeting chairs, the torso/head groups now lower into a true seated stack and the hip/leg chain uses calmer thigh and shin angles so the body reads as sitting from the pelvis instead of hovering in a near-standing pose.
- 2026-03-25: Revalidated the posture pass with focused chair/sofa checks in `output/web-game-seat-posture-fix/chair.png`, `output/web-game-seat-posture-fix/sofa.png`, and `output/web-game-seat-posture-fix/state.json`. Standard load regression still passes in `output/web-game-seat-posture-fix-client/state-0.json`.
- 2026-03-25: Fixed the human seated shoulder silhouette on chairs and sofas. The seated pose now lowers the arm anchors with the torso instead of leaving them at standing shoulder height, and the seated arm splay/yaw is reduced so the upper shoulders do not bulge outward into a convex shoulder-pad shape.
- 2026-03-25: Revalidated the seat-shoulder fix with the required Playwright client in `output/web-game-seat-shoulder-fix-client/state-0.json` and focused seated captures in `output/web-game-seat-shoulder-fix/chair-front.png`, `output/web-game-seat-shoulder-fix/sofa-front.png`, `output/web-game-seat-shoulder-fix-close/chair-close.png`, and `output/web-game-seat-shoulder-fix-close/sofa-close.png`. The close chair shot shows a flatter shoulder line than the previous convex seated silhouette.
- 2026-03-25: Fixed the lounge floating bug by grounding the lounge chair and sofa meshes with visible lower bases instead of leaving the main seat blocks hovering above the floor. I also moved the lounge chair/sofa seat anchors backward and switched the live player `sit` branch to reuse `applyHumanSeatPose(...)`, so the active seated animation now applies the same hips/legs stack as the passive seated pose instead of a partial standing override.
- 2026-03-25: Revalidated the grounded lounge + seat-contact fix with the required Playwright client in `output/web-game-seat-contact-fix-client/state-0.json`. Focused seated checks are in `output/web-game-seat-contact-fix/chair-front.png`, `output/web-game-seat-contact-fix/sofa-front.png`, plus side/debug views in `output/web-game-seat-contact-fix-debugcam/chair-side.png` and `output/web-game-seat-contact-fix-debugcam/sofa-side.png`; the chair side view now shows the pelvis seated into the chair instead of hovering at the front edge, and both furniture pieces show grounded lower supports.
- 2026-03-25: Lifted the sofa-only sit offset after the user pointed out the avatar was still sunk into the cushion. The chair values stay as-is, but the sofa visual offset now sits higher again so the torso no longer disappears into the sofa front.
- 2026-03-25: Revalidated the sofa-only lift with the required Playwright client in `output/web-game-sofa-seat-lift-client/state-0.json` plus focused sofa checks in `output/web-game-sofa-seat-lift/sofa-front.png` and `output/web-game-sofa-seat-lift-side/sofa-side.png`. The front view now shows the body resting on the cushion instead of buried inside it.
- 2026-03-26: Reworked the lounge chair seat placement after the user pointed out the previous fix cheated by sinking the avatar below the floor. I lifted the chair sit offset to `-0.22` and moved the lounge-chair seat anchor forward from `0.10` to `0.18`, so the seated root stays above the floor and the body sits farther forward on the chair instead of being buried into the cushion.
- 2026-03-26: Revalidated the chair lift/forward shift with the required Playwright client in `output/web-game-chair-seat-lift-client/state-0.json` plus focused chair captures in `output/web-game-chair-seat-lift/chair-front.png` and `output/web-game-chair-seat-lift/chair-side.png`. The recorded chair metrics in `output/web-game-chair-seat-lift/state.json` show `rootY: 0.025` and `seatZ: 1.68`, confirming the seated pivot is above the floor and farther forward than the previous broken version.
- 2026-03-26: Fixed the standing idle grounding regression. The standing pose was using a symmetric vertical sway around the root baseline, and over the idle loop that dropped the boot bottoms to about `-0.012` below the floor. I changed the idle sway to an upward-only offset so the standing animation no longer dips the character into the floor.
- 2026-03-26: Revalidated the standing grounding fix with the required Playwright client in `output/web-game-stand-ground-fix-client/state-0.json` plus a focused standing side capture in `output/web-game-stand-ground-fix/standing-side.png`. The idle-cycle metrics in `output/web-game-stand-ground-fix/state.json` now show `minSeen: 0.0000000025` at the lowest frame instead of a negative floor intersection.
- 2026-03-26: Reworked the human lounge-chair sit pose from the provided proper-sitting references instead of just shifting the seat anchor. The chair-only human pose now uses a more upright trunk, a slightly higher pelvis, calmer arms, and a near-90-degree hip/knee chain so the thighs read as seated on the cushion and the shins drop close to vertical instead of collapsing into a half-squat.
- 2026-03-26: Revalidated the chair reference-based posture fix with the required Playwright client in `output/web-game-chair-posture-ref-v4-client/state-0.json` plus focused front/side chair captures in `output/web-game-chair-posture-ref-v4/chair-front.png` and `output/web-game-chair-posture-ref-v4/chair-east-side.png`. The pose metrics in `output/web-game-chair-posture-ref-v4/state.json` show `characterRootY: 0.0253` with `leftBootMinY/rightBootMinY: -0.00126`, which is effectively on-floor contact instead of the previous deeper below-floor sink.
- 2026-03-26: Enlarged the human eye geometry. I widened the eye anchors slightly and scaled each eye group up on both axes so the face reads larger-eyed while preserving the existing expression presets and brow/mouth system.
- 2026-03-26: Revalidated the bigger-eye update with the required Playwright client in `output/web-game-bigger-eyes-client/state-0.json` plus focused proof captures in `output/web-game-bigger-eyes/eyes-close.png` and `output/web-game-bigger-eyes/eyes-mid.png`.
- 2026-03-26: Fixed the chair-exit standing bug where the boots sank into the floor right after standing up. The active human animation path was only resetting rotations between actions, so after leaving a seat the idle pose inherited seated torso/hip/leg positions; `animateLimbs()` now restores the full standing rig baseline with `resetHumanRigPose()` before applying the current action pose.
- 2026-03-26: Revalidated the seat-exit standing fix with the required Playwright client in `output/web-game-seat-exit-stand-fix-client/state-0.json`, frame-by-frame exit metrics in `output/web-game-seat-exit-stand-fix/metrics.json`, and a focused proof shot in `output/web-game-seat-exit-stand-fix-proof/exit-side-proof.png`. The first idle frame after standing now shows `minY: 0.00745` and equestrian boot minimum `0.02245`, confirming the feet stay above the floor.
- 2026-03-26: Extended the per-user localStorage snapshot to include outfit selections, and added a matching backend export helper for a new `user_scene_outfits` SQL table. New users now reset to the default outfit instead of inheriting the previous user’s clothes, while returning users restore their saved outfit alongside their saved actor/location state.
- 2026-03-26: Revalidated outfit persistence with the required Playwright client in `output/web-game-outfit-persist-client/state-0.json` plus the full multi-user proof in `output/web-game-outfit-persist-v2/state.json` and `output/web-game-outfit-persist-v2/alice-restored-front.png`. That check confirms Alice's custom outfit (`LTHR TRENCH`, `SAND`, `WHITE`, `CAP`, `HAPPY`, `WHITE SNKR`) survives refresh and returns after switching back from a fresh Bob session, and the SQL export now includes both `user_scene_locations` and `user_scene_outfits` rows.
- 2026-03-26: Extended the same per-user localStorage session snapshot to the passive/service robots. User session snapshots now include `passiveRobots`, the passive robot reset path uses the same restore helper as saved sessions, and backend export metadata/helpers now expose a new `user_scene_passive_robot_locations` SQL table alongside the existing location and outfit records.
- 2026-03-26: Revalidated passive robot persistence with the required Playwright client in `output/web-game-passive-robot-persist-client/state-0.json` plus a focused multi-user proof in `output/web-game-passive-robot-persist/state.json`. That check confirms Alice's moved passive robots restore after reload and after switching back from Bob, Bob gets default robot positions instead of Alice's saved route state, and `getUserSessionSqlRecords(...)` now returns `user_scene_passive_robot_locations` rows. Visual proof is in `output/web-game-passive-robot-persist/alice-restored-left.png`, `output/web-game-passive-robot-persist/alice-restored-right.png`, `output/web-game-passive-robot-persist/bob-default-left.png`, and `output/web-game-passive-robot-persist/bob-default-right.png`.
- 2026-03-26: Added an in-world blackjack wall on the east wall of the office. The wall is a live casino-style blackjack board with a rendered felt layout, dealer/player cards, bankroll readouts, and the core house rules wired into game state: 6-deck shoe, dealer stands on soft 17, blackjack pays 3:2, double on any two cards, split up to four hands, and split aces draw one card each.
- 2026-03-26: Wired the blackjack wall into the existing select-mode object action panel instead of adding a separate overlay. Selecting the wall now exposes bet/deal actions during betting and hit/stand/double/split during play, while `render_game_to_text()` exports the full blackjack state for deterministic testing.
- 2026-03-26: Revalidated the blackjack wall with the required Playwright client in `output/web-game-blackjack-wall-client/state-0.json` plus focused rule proofs in `output/web-game-blackjack-wall/state.json`. That focused run confirms the wall starts at `playerCredits: 5000` and `bankCredits: 1000000`, supports an opening split on `8/8`, resolves the split hand sequence to `H1 WIN · H2 LOSE` with bankroll returning to `5000`, and resolves a separate double-down scenario to `playerCredits: 5200` / `bankCredits: 999800`. Screenshots are in `output/web-game-blackjack-wall/wall-installed.png`, `output/web-game-blackjack-wall/split-available.png`, `output/web-game-blackjack-wall/split-resolved.png`, and `output/web-game-blackjack-wall/double-resolved.png`.
- 2026-03-26: Reworked the blackjack interaction panel into a dedicated bottom-center `BLACKJACK CONSOLE` when the wall is selected, while leaving the generic right-side object panel behavior intact for chairs and the coffee arm. The console now uses a casino-terminal style readout with a separate result box, and round-over states no longer bury the winner text inside the generic copy line.
- 2026-03-26: Added a shared blackjack result announcer for both the wall board and the bottom console. When a round ends, the game now computes a clear winner headline (`PLAYER WINS`, `BANKER WINS`, `PLAYER BLACKJACK`, or `ROUND PUSH`), types it out on the wall and the console, and exports the same typed result payload in `render_game_to_text()`. Revalidated with the required smoke client in `output/web-game-blackjack-result-ui-client/state-0.json` and a deterministic proof hand in `output/web-game-blackjack-result-ui/state.json`; that proof shows the console centered at `centerX: 700` in a `1400px` viewport and a natural-blackjack round resolving to `PLAYER BLACKJACK +150` with screenshots in `output/web-game-blackjack-result-ui/result-ui-wide.png` and `output/web-game-blackjack-result-ui/result-ui-board.png`.
- 2026-03-26: Changed the `NEXT HAND` flow so it now clears the previous dealer/player cards and resets the blackjack wall back to a clean betting board instead of carrying the finished round forward. The reset preserves bankrolls and the current wager, clears the typed result banner, restores the `BETTING` console state, and leaves `DEAL HAND` ready for the next round.
- 2026-03-26: Revalidated the next-hand reset flow with the required smoke client in `output/web-game-blackjack-next-hand-reset-client/state-0.json` and a deterministic before/after proof in `output/web-game-blackjack-next-hand-reset/state.json`. That state shows `before.phase: "round-over"` with visible dealer/player cards, and `after.phase: "betting"` with `dealer.cards: []` and `hands: []`. Screenshots are in `output/web-game-blackjack-next-hand-reset/before-next-hand.png` and `output/web-game-blackjack-next-hand-reset/after-next-hand.png`.
- 2026-03-26: Added per-user blackjack persistence to the same localStorage session snapshot used for location, outfit, and passive robots. User snapshots now include blackjack bankroll state plus a capped recent result history, and restoring a user session resets the table to `BETTING` while keeping the saved bankroll and latest result log for that user.
- 2026-03-26: Added backend-facing SQL export helpers for blackjack via `user_scene_blackjack_states` and `user_scene_blackjack_result_history`, and exposed them through `getUserSessionSqlRecords(...)`. The wall console now surfaces the latest saved result in betting mode so restored blackjack history is visible after refresh or user switching.
- 2026-03-26: Revalidated blackjack persistence with the required smoke client in `output/web-game-blackjack-persist-client/state-0.json` and a focused multi-user proof in `output/web-game-blackjack-persist/state.json`. That proof shows Alice restoring `PLAYER BLACKJACK +150` with `playerCredits: 5150` / `bankCredits: 999850` after refresh and after switching back from Bob, while Bob stays at the default `5000` / `1000000` with an empty history. Screenshots are in `output/web-game-blackjack-persist/alice-restored.png`, `output/web-game-blackjack-persist/bob-default.png`, and `output/web-game-blackjack-persist/alice-switch-back.png`.
- 2026-03-27: Lowered the bottom-center `BLACKJACK CONSOLE` so its lower edge aligns with the D-pad down-arrow row instead of floating above it. The desktop and mobile blackjack-console CSS overrides now both use `bottom: 48px`, matching the control baseline used by the D-pad and action stack.
- 2026-03-27: Revalidated the lowered blackjack console with the required smoke client in `output/web-game-blackjack-console-bottom-client/state-0.json` and a focused proof capture in `output/web-game-blackjack-console-bottom/state.json`. The measured layout now reports `panelBottom: 852`, `downBottom: 852`, and `delta: 0`, with screenshots in `output/web-game-blackjack-console-bottom/console-bottom-proof.png` and `output/web-game-blackjack-console-bottom/console-bottom-clean.png`.
- 2026-03-27: Extended the existing `PANELS` collapse behavior to the top HUD blocks as well. `#scoreboard`, `#select-panel`, and `#state-label` now follow the same `body.controls-collapsed` rule as the lower control panels, so one toggle hides and restores the full HUD set.
- 2026-03-27: Revalidated the top-panel collapse update with the required smoke client in `output/web-game-top-panels-collapse-client/state-0.json` and a focused before/after interaction check in `output/web-game-top-panels-collapse/state.json`. That proof shows `scoreboard`, `select-panel`, and `state-label` all switching from visible blocks to `display: none` on collapse, then restoring on the next toggle, with screenshots in `output/web-game-top-panels-collapse/hud-expanded.png`, `output/web-game-top-panels-collapse/hud-collapsed.png`, and `output/web-game-top-panels-collapse/hud-restored.png`.
- 2026-03-27: Expanded the camera zoom range and made the button zoom increments stronger. The orbit camera now supports a closer minimum radius of `2.8`, a farther maximum radius of `16.5`, and uses a larger button step factor of `1.18` so `+ ZOOM` and `- ZOOM` move more per press.
- 2026-03-27: Revalidated the zoom-range update with the required smoke client in `output/web-game-zoom-range-client/state-0.json` and a focused button-driven zoom proof in `output/web-game-zoom-range/state.json`. That proof records the camera moving from the default radius `8.06` to `2.8` at max zoom-in and `16.5` at max zoom-out, with screenshots in `output/web-game-zoom-range/zoom-default.png`, `output/web-game-zoom-range/zoom-in-max.png`, and `output/web-game-zoom-range/zoom-out-max.png`.
- 2026-03-27: Added a dedicated top-right live portrait window and moved the old status card into the top-left HUD stack. The new portrait panel draws a compact front-facing bust for the controlled actor, reflects human face-style changes plus robot control switching, and keeps the style menu below it instead of overlapping.
- 2026-03-27: Revalidated the portrait/status HUD change with the required smoke client in `output/web-game-portrait-window-client/state-0.json` and a focused UI proof in `output/web-game-portrait-window/state.json`. That proof records the moved status card at `left: 16, top: 14`, the portrait panel at `top: 14`, and the style menu safely below it at `top: 258` while the portrait copy changes from `NORMAL FACE · OFFICE FLOOR` to `HAPPY FACE · OFFICE FLOOR`, then to `OFFICE FLOOR · CORE ONLINE` after switching control to the robot. Screenshots are in `output/web-game-portrait-window/portrait-human-default.png`, `output/web-game-portrait-window/portrait-human-happy.png`, and `output/web-game-portrait-window/portrait-robot.png`.
- 2026-03-27: Replaced the stylized portrait drawing with a true live actor render. The portrait canvas now uses a second Three.js renderer and portrait camera on a dedicated layer, so it renders the actual active human or robot rig instead of an approximation; this makes the face, hair, shades, expression, and robot head geometry match the in-world model exactly.
- 2026-03-27: Revalidated the live-render portrait with the required smoke client in `output/web-game-portrait-window-client/state-0.json` and a tighter face-match proof in `output/web-game-portrait-identical/state.json`. The human proof there uses `camera.yaw: 3.14` and `radius: 3.2` to show the front face in-world next to the portrait window, and the portrait now matches the real face instead of the old custom drawing. Screenshots are in `output/web-game-portrait-identical/human-front-happy.png` and `output/web-game-portrait-identical/robot-front.png`.
- 2026-03-27: Removed the portrait-panel text labels so the live face card is image-only. The `LIVE FACE` title, readout chips, and lower copy line now stay hidden, the portrait stage uses the full compact card height, and the style menu is pulled upward to sit directly below the reduced portrait panel.
- 2026-03-27: Revalidated the portrait label removal with the required smoke client in `output/web-game-portrait-label-remove-client/state-0.json` and a focused UI proof in `output/web-game-portrait-label-remove/state.json`. That proof shows `title`, `readout`, and `copy` all at `display: none` with zero height, while the portrait card remains at `top: 14` and the style menu now starts at `top: 178`. Screenshot proof is in `output/web-game-portrait-label-remove/portrait-no-labels.png`.
- 2026-03-27: Moved the visible right-side action controls into the `MOVES` expander. The always-visible action stack now only shows `MOVES` and `PANELS`, while the expanded `MOVES PANEL` contains the full jump, wave, run, view, zoom, and timed performance controls.
- 2026-03-27: Revalidated the consolidated moves drawer with the required smoke client in `output/web-game-moves-toggle-all-actions-client/state-0.json` and a focused DOM/game-state proof in `output/web-game-moves-toggle-all-actions/state.json`. That proof shows the visible rows reduced to `✦ MOVES` and `▣ PANELS`, confirms all eight action IDs live inside `#special-moves-panel`, and records the moved controls still working by toggling `RUN` active and changing camera radius from `8.06` to `6.83` after clicking `+ ZOOM`. Screenshot proof is in `output/web-game-moves-toggle-all-actions/moves-panel-open.png`.
- 2026-03-27: Tightened the moonwalk leg pose so the boots no longer peel away from the legs in side view. The human and robot moonwalk roles now use a shorter stride, less shin fold on the support leg, slight inward yaw, and much smaller whole-boot pitch so the lower leg reads as connected instead of detached.
- 2026-03-27: Revalidated the moonwalk leg fix with the required smoke client in `output/web-game-moonwalk-leg-fix-client/state-0.json` plus focused side-view captures and rig metrics in `output/web-game-moonwalk-leg-fix/state.json`, `output/web-game-moonwalk-leg-fix/moonwalk-side-after.png`, and `output/web-game-moonwalk-leg-fix-close/moonwalk-side-close.png`. The focused proof records the active moonwalk pose with human support-leg `bootX: 0.062` and `shinX: 0.243`, well below the old exaggerated boot-pitch setup that created the detached lower-leg look.
- 2026-03-27: Replaced the portrait window’s live mini-camera with a dedicated 2D canvas portrait renderer. The small face panel now draws a stylized bust directly in canvas instead of rendering the world model, which is the more practical base for matching a reference-image look and for keeping expression states deterministic.
- 2026-03-27: Revalidated the 2D portrait conversion with the required smoke client in `output/web-game-portrait-2d-style-client/state-0.json` plus focused panel captures in `output/web-game-portrait-2d-style/portrait-human-happy-2d.png` and `output/web-game-portrait-2d-style/portrait-robot-2d.png`. The focused proof in `output/web-game-portrait-2d-style/state.json` records the portrait panel still anchored at `top: 14`, `right: 16`, with the canvas at `166x118`, and confirms the human `HAPPY` face plus the robot controller both render through the new 2D path.
- 2026-03-27: Took the no-API/manual route for the portrait panel and refined it into a more explicit hand-authored expression kit. The human portrait now uses larger layered eyes, separate brow/lid controls, a dedicated mouth renderer, softer facial shading, and fuller hair framing so `NORMAL`, `HAPPY`, and `SURPRISED` read as original 2D portrait states instead of a generic live bust render.
- 2026-03-27: Revalidated the manual portrait refinement with the required smoke client in `output/web-game-portrait-manual-style-client/state-0.json` plus focused panel captures in `output/web-game-portrait-manual-style/portrait-normal.png`, `output/web-game-portrait-manual-style/portrait-happy.png`, and `output/web-game-portrait-manual-style/portrait-surprised.png`. The focused proof in `output/web-game-portrait-manual-style/state.json` records the portrait panel still at `top: 14`, `right: 16`, with `166x118` canvas sizing and the three requested human face states rendering through the manual 2D path.
- 2026-03-27: Replaced the manual portrait again with a much stronger style jump toward the provided reference direction instead of iterating inside the old long-hair look. The human portrait now uses a white studio background, a shorter swept hairstyle, a smaller lower-face silhouette, larger bright blue eyes, lighter skin shading, and a head-and-shoulders framing that reads as a different portrait family rather than a tweak of the previous version.
- 2026-03-27: Revalidated the portrait style replacement with the required smoke client in `output/web-game-portrait-style-replace-client/state-0.json` and focused portrait proofs in `output/web-game-portrait-style-replace/portrait-normal.png`, `output/web-game-portrait-style-replace/portrait-happy.png`, and `output/web-game-portrait-style-replace/portrait-surprised.png`. The focused state in `output/web-game-portrait-style-replace/state.json` confirms the portrait panel still holds its `202x154` frame at `top: 14`, `right: 16` while rendering the new short-hair human style.
- 2026-03-27: Reverted the small face panel back to a live 3D monitor render. The portrait canvas now uses a second Three.js renderer again, with portrait-only actor layers and dedicated portrait lights so the panel shows the actual in-world rig instead of the manual 2D portrait kit.
- 2026-03-27: Fixed a follow-up regression from that revert where `PORTRAIT_LAYER` was declared too late and blocked page initialization. Revalidated with the required smoke client in `output/web-game-portrait-3d-monitor-client-clean/state-0.json` and a focused full-page + panel proof in `output/web-game-portrait-3d-monitor-proof/state.json`, `output/web-game-portrait-3d-monitor-proof/portrait-wide.png`, and `output/web-game-portrait-3d-monitor-proof/portrait-panel.png`.
- 2026-03-27: Tightened the live portrait framing so the panel now shows just the head/upper face instead of the shoulders, and removed the forced transparent portrait background override so the window renders the actual office environment behind the face. Softened the portrait-stage chrome too so the frame no longer adds a dark fake backdrop around the scene render.
- 2026-03-27: Revalidated the head-only/environment portrait update with the required smoke client in `output/web-game-portrait-head-env-client/state-0.json` and focused proof captures in `output/web-game-portrait-head-env-proof/state.json`, `output/web-game-portrait-head-env-proof/portrait-wide.png`, and `output/web-game-portrait-head-env-proof/portrait-panel.png`. The focused proof records `errors: []` and shows the portrait background matching the office wall instead of the old dark monitor fill.
- 2026-03-27: Adjusted the portrait again to better match the in-world front-face shot. The portrait camera now renders both the default scene layer and the portrait layer, the custom portrait lights are reduced to subtle fill only, and the face crop is backed off so the full head plus a small shoulder line remain visible instead of the previous over-tight forehead/eyes crop.
- 2026-03-27: Revalidated the live-scene portrait update with the required smoke client in `output/web-game-portrait-livebg-client/state-0.json` and focused proof captures in `output/web-game-portrait-livebg-proof/state.json`, `output/web-game-portrait-livebg-proof/portrait-wide.png`, and `output/web-game-portrait-livebg-proof/portrait-panel.png`. The focused proof records `errors: []` and shows the small window matching the same live wall background seen behind the character in the main view.
- 2026-03-27: Dimmed the live portrait window by cutting the portrait-only ambient/key/fill/rim lights again and lowering the portrait renderer exposure. The portrait now keeps the same framing/background but no longer blows out the face and wall tones as much.
- 2026-03-27: Revalidated the dimmer portrait in `output/web-game-portrait-dim-client/state-0.json` and `output/web-game-portrait-dim-proof/state.json`, with updated screenshots in `output/web-game-portrait-dim-proof/portrait-panel.png` and `output/web-game-portrait-dim-proof/portrait-wide.png`. The focused proof records `errors: []`.
- 2026-03-27: Expanded the live monitor stack from one portrait window to three linked windows on the right side. The head window keeps the existing face monitor, and two new windows now render live cropped views for the arms/hands and the legs/feet using the same secondary Three.js renderer flow, actor switching, environment background, and accent styling as the head monitor.
- 2026-03-27: Revalidated the three-monitor stack with the required smoke client in `output/web-game-body-monitors-client/state-0.json` and focused DOM/screenshot proof in `output/web-game-body-monitors-proof/state.json`, `output/web-game-body-monitors-proof/wide.png`, `output/web-game-body-monitors-proof/head-panel.png`, `output/web-game-body-monitors-proof/arms-panel.png`, and `output/web-game-body-monitors-proof/legs-panel.png`. The focused proof records `errors: []`, shows all three panels visible at `y: 14`, `160`, and `306`, and confirms the style menu starts below them at `y: 452`.
- 2026-03-28: Moved the floating `MOVES` / `PANELS` toggle stack from the bottom-right to the bottom-left side of the screen. The anchor now sits just to the right of the D-pad, and the expanded `MOVES PANEL` also opens from the left edge of that stack instead of the old right-edge anchoring.
- 2026-03-28: Revalidated the left-side action-stack move with the required smoke client in `output/web-game-actions-left-client/state-0.json` and focused proof in `output/web-game-actions-left-proof/state.json`, `output/web-game-actions-left-proof/wide.png`, and `output/web-game-actions-left-proof/actions-left.png`. The focused proof records `errors: []` and shows the action stack at `x: 184` while the D-pad remains at `x: 48`, leaving a clear gap between them.
- 2026-03-28: Decoupled `MOVES` from the panel-collapse state and split the toggles into separate docks. `MOVES` now stays on the left with its own panel even when the rest of the HUD is collapsed, while `PANELS` is isolated on the bottom-right as the only control that shows/hides the HUD blocks.
- 2026-03-28: Revalidated the isolated-toggle fix with the required smoke client in `output/web-game-moves-panels-isolated-client/state-0.json` and a focused one-click proof in `output/web-game-moves-panels-isolated-proof/state.json`, `output/web-game-moves-panels-isolated-proof/wide.png`, `output/web-game-moves-panels-isolated-proof/moves-left.png`, and `output/web-game-moves-panels-isolated-proof/panels-right.png`. That focused proof records `errors: []`, `beforeState.interaction.controlPanels.expanded: false`, and `afterState.interaction.specialMoves.expanded: true` after a single click on `MOVES`, with the left dock at `x: 184` and the right dock at `x: 1245.36`.
- 2026-03-28: Switched the three right-side monitor windows from front-facing crops to actor-relative side views. The head window now uses a wider side-profile camera, and the arm/leg windows also place their cameras on the actor’s side instead of in front so all three monitors match the requested profile-view presentation.
- 2026-03-28: Revalidated the side-view monitor update with the required smoke client in `output/web-game-body-monitors-side-client/state-0.json` and focused proof in `output/web-game-body-monitors-side-proof/state.json`, `output/web-game-body-monitors-side-proof/wide.png`, `output/web-game-body-monitors-side-proof/head-panel.png`, `output/web-game-body-monitors-side-proof/arms-panel.png`, and `output/web-game-body-monitors-side-proof/legs-panel.png`. The focused proof records `errors: []` and confirms all three monitor panels remain visible at `x: 1182` with the updated side-profile crops.
- 2026-03-28: Refined the monitor cameras again so the three small windows use a 30-degree actor-relative view instead of a full profile. The new monitor direction blends the actor forward vector with the side vector, which gives the head window a readable three-quarter angle while keeping the arms and legs in the same angled presentation.
- 2026-03-28: Revalidated the 30-degree monitor view with the required smoke client in `output/web-game-body-monitors-30deg-client/state-0.json` and focused proof in `output/web-game-body-monitors-30deg-proof/state.json`, `output/web-game-body-monitors-30deg-proof/wide.png`, `output/web-game-body-monitors-30deg-proof/head-panel.png`, `output/web-game-body-monitors-30deg-proof/arms-panel.png`, and `output/web-game-body-monitors-30deg-proof/legs-panel.png`. The focused proof records `errors: []` and shows the three-quarter head view plus matching 30-degree arm and leg windows.
- 2026-03-28: Lowered and widened the legs monitor framing so more of the feet remain visible in the small window. The legs camera now looks lower on the shin/foot midpoint, sits slightly farther back, and uses a wider field of view to keep the boot soles and forefoot in frame.
- 2026-03-28: Revalidated the feet-framing change with the required smoke client in `output/web-game-body-monitors-feet-client/state-0.json` and focused proof in `output/web-game-body-monitors-feet-proof/state.json`, `output/web-game-body-monitors-feet-proof/wide.png`, and `output/web-game-body-monitors-feet-proof/legs-panel.png`. The focused proof records `errors: []` and shows the legs monitor now including more of both feet instead of cropping tightly at the ankle area.
- 2026-03-28: Added a new selectable `TABLE TENNIS` corner feature near the empty east-south wall area, including the physical table, net, floor pad, a `PING BOT` expert robot, and a dedicated table-tennis action console in the existing object-actions panel.
- 2026-03-28: Implemented playable table-tennis match logic driven by select-mode entry: selecting the table exposes `START MATCH`, `SERVE`, `RESET MATCH`, and `LEAVE TABLE`; the human is snapped into a bounded player lane, the Ping Bot tracks the far side as an expert opponent, and the ball runs through a self-contained rally/point loop with table bounces, net faults, return detection, and scoring to 11-by-2.
- 2026-03-28: Revalidated the table-tennis feature with the required smoke client in `output/web-game-table-tennis-smoke-client/state-0.json` and focused proof in `output/web-game-table-tennis-proof/state.json`, `output/web-game-table-tennis-proof/table-selected.png`, `output/web-game-table-tennis-proof/rally-live.png`, and `output/web-game-table-tennis-proof/point-scored.png`. The focused proof records `errors: []`, shows the selectable table console, a live rally against `PING BOT`, and a scored-point state with `playerScore: 1` and `robotScore: 1`.
- 2026-03-28: Fixed the first table-tennis bug pass by moving the table deeper into the empty southeast corner, updating the player and Ping Bot station anchors to match the moved table, and relaxing the ready pose so the paddle/serve carry no longer peels away from the hand. The idle/non-rally ball now rides the live paddle transform instead of hovering in a stale world position.
- 2026-03-28: Revalidated the table-tennis bug fix with the required smoke client in `output/web-game-table-tennis-bugfix-client/state-0.json` and focused proof in `output/web-game-table-tennis-bugfix-proof/state.json`, `output/web-game-table-tennis-bugfix-proof/table-wide.png`, `output/web-game-table-tennis-bugfix-proof/serve-ready.png`, `output/web-game-table-tennis-bugfix-proof/serve-close.png`, and `output/web-game-table-tennis-bugfix-proof/rally-wide.png`. The focused proof records `errors: []`, with the player restored at `13.4 / -21.55`, Ping Bot at `13.4 / -13.15`, and the live rally state running at the moved table instead of the old rack-side location.
- 2026-03-28: Added a dedicated table-tennis camera mode. Starting a match now snapshots the current orbit camera, switches into a fixed table-centered gameplay view, and suppresses orbit/zoom overrides while the table-tennis mode is active so the player gets a stable match framing instead of the regular exploration camera.
- 2026-03-28: Exiting table tennis now snaps immediately back to the saved regular orbit view instead of easing through the match framing. Revalidated with the required smoke client in `output/web-game-table-tennis-camera-client/state-0.json` and focused proof in `output/web-game-table-tennis-camera-proof/state.json`, `output/web-game-table-tennis-camera-proof/before-regular.png`, `output/web-game-table-tennis-camera-proof/during-match.png`, and `output/web-game-table-tennis-camera-proof/after-exit.png`. The focused proof records `errors: []`, `before.camera.mode: "orbit"`, `during.camera.mode: "table-tennis"`, and `after.camera.mode: "orbit"` with the post-exit camera back at `x: 13.4, y: 4, z: -28.55`.
- 2026-03-28: Added a new selectable `MAGIC CHESS` board beside the table-tennis corner. The new corner feature includes a raised board/plinth, floating stylized pieces, a dedicated chess console in the object-actions panel, a match-only camera lock, board-cursor controls, and a basic player-vs-PC chess loop where the human plays white and the arcane CPU plays black.
- 2026-03-28: Fixed the first magic-chess render bug by changing the chess piece, cursor, selection, and legal-target overlays to use board-local coordinates instead of world coordinates while they are parented under the board group. Before that fix the pieces were being translated off the board, which made the match look empty after start.
- 2026-03-28: Revalidated the magic-chess feature with the required smoke client in `output/web-game-magic-chess-client/state-0.json` and a focused playable proof in `output/web-game-magic-chess-proof-fixed/state.json`, `output/web-game-magic-chess-proof-fixed/board-selected.png`, `output/web-game-magic-chess-proof-fixed/match-start.png`, and `output/web-game-magic-chess-proof-fixed/pc-response.png`. The focused proof records `errors: []`, the match camera mode as `magic-chess`, and a deterministic opening where white plays `e4` and the CPU answers `...e5`, returning the turn to white.
- 2026-03-28: Replaced the four-button D-pad skin with a joystick-style movement control. The same `dbtn-up/down/left/right` touch bindings remain in place, but the HUD now shows a circular base, grid rings, a moving thumb, and a `MOVE` label closer to the requested joystick reference instead of separate arrow buttons.
- 2026-03-28: Revalidated the joystick HUD update with the required smoke client in `output/web-game-joystick-style-client/state-0.json` and a focused HUD proof screenshot in `output/web-game-joystick-style-proof.png`. The focused proof shows the new joystick at the lower-left and confirms the `MOVES` toggle still has clear spacing to its right.
- 2026-03-28: Reworked `MAGIC CHESS` into a fullscreen regular-chess overlay. Starting a match now opens a full-screen board UI with clickable 2D squares, move highlights, captured-piece trays, move history, reset/leave controls, and a promotion chooser, while the rules layer now tracks `hasMoved`, castling, en passant, promotion, and last-move highlights so the board behaves like a normal playable chess game instead of a cursor-only gimmick.
- 2026-03-28: Revalidated the fullscreen magic-chess flow with the required smoke client in `output/web-game-magic-chess-fullscreen-client/state-0.json` and a focused gameplay proof in `output/web-game-magic-chess-fullscreen-proof/state.json`, `output/web-game-magic-chess-fullscreen-proof/fullscreen-start.png`, `output/web-game-magic-chess-fullscreen-proof/piece-selected.png`, `output/web-game-magic-chess-fullscreen-proof/after-player-move.png`, and `output/web-game-magic-chess-fullscreen-proof/after-cpu-reply.png`. The focused proof records `errors: []`, `overlayDisplay: "flex"`, `squareCount: 64`, a clickable `E2` pawn selection with legal targets `E3/E4`, the committed `WHITE PAWN E2→E4` move, and the CPU reply `B7→B5` before returning the turn to White.
- 2026-03-29: Added a new `KICK` move to the `MOVES` panel and keyboard hotkey `C`. The move is implemented as a short timed action with a dedicated kick pose and a forward foot-strike cone that can impart a strong impulse to nearby physics balls instead of relying on passive body collisions.
- 2026-03-29: Revalidated the kick move with the required smoke client in `output/web-game-kick-move-client/state-0.json` and a focused proof in `output/web-game-kick-move-proof/state.json`, `output/web-game-kick-move-proof/before-kick.png`, `output/web-game-kick-move-proof/during-kick.png`, and `output/web-game-kick-move-proof/after-kick.png`. The focused proof records `errors: []`; `steel-3` starts at `x 0.16 / z 3.08 / vz 0`, reaches `z 3.53 / vz 8.86 / lastTouchedBy "player"` during the strike, and finishes at `z 9.43` after the kick resolves.
- 2026-03-29: Refined the kick animation to read more like the front-kick references: the kick now uses a clearer chamber -> snap -> retract sequence, the strike timing window was tightened, and the visual strike leg/foot center were aligned so the boot actually drives forward into the ball instead of peeling backward in side view.
- 2026-03-29: Revalidated the refined kick with the required smoke client in `output/web-game-kick-refine-client-v3/state-0.json` and a corrected focused proof in `output/web-game-kick-refine-proof-v3/state.json`, `output/web-game-kick-refine-proof-v3/before-kick.png`, `output/web-game-kick-refine-proof-v3/during-kick.png`, and `output/web-game-kick-refine-proof-v3/after-kick.png`. The focused proof records `errors: []`, shows the kick starting from a planted idle stance, contacting `steel-3` with a forward snap-kick silhouette, and returning to idle after the ball is driven away with `lastTouchedBy: "player"`.
- 2026-03-29: Remapped desktop running so holding `R` now feeds the same sprint path as the old modifier-based run check, and updated the on-screen controls legend to show `R — Run`.
- 2026-03-29: Revalidated the `R` run binding with the required smoke client in `output/web-game-run-key-r-client/state-0.json` and a focused proof in `output/web-game-run-key-r-proof/state.json`, `output/web-game-run-key-r-proof/before-r-run.png`, and `output/web-game-run-key-r-proof/running-with-r.png`. The focused proof records `errors: []`, the HUD copy as `R — Run`, and a held `W + R` burst moving the player into `action: "run"` with `vx: -1.7` and `vz: 5.13`.
- 2026-03-29: Added the old kick shape back as a separate `BACK KICK` move instead of replacing the refined front kick. The move now appears as its own button in the `MOVES` panel with hotkey `X`, uses a backward strike cone so it can hit balls behind the player, and has its own human/robot pose branch so the animation reads as a rear kick instead of reusing the front-kick silhouette.
- 2026-03-29: Revalidated `BACK KICK` with the required smoke client in `output/web-game-back-kick-client/state-0.json` and a focused proof in `output/web-game-back-kick-proof/state.json`, `output/web-game-back-kick-proof/moves-panel-back-kick.png`, `output/web-game-back-kick-proof/before-back-kick.png`, `output/web-game-back-kick-proof/during-back-kick.png`, and `output/web-game-back-kick-proof/after-back-kick.png`. The focused proof records `errors: []`, shows `BACK KICK` in the performance move list, and drives `steel-3` from `z: 1.16` to `z: -5.11` with `lastTouchedBy: "player"` after the rear strike lands.
- 2026-03-30: Added a new `FULL KICK` combo move that chains the rear kick into the forward kick in one activation. The combo appears in the `MOVES` panel with hotkey `Z`, reuses the back-kick and front-kick silhouettes in sequence, and now supports two strike stages so one activation can hit a rear ball first and a front ball second.
- 2026-03-30: Revalidated `FULL KICK` with the required smoke client in `output/web-game-full-kick-client/state-0.json` and a focused combo proof in `output/web-game-full-kick-proof/state.json`, `output/web-game-full-kick-proof/moves-panel-full-kick.png`, `output/web-game-full-kick-proof/before-full-kick.png`, `output/web-game-full-kick-proof/back-phase-full-kick.png`, `output/web-game-full-kick-proof/front-phase-full-kick.png`, and `output/web-game-full-kick-proof/after-full-kick.png`. The focused proof records `errors: []`, shows `FULL KICK` in the move list, lands `BACK KICKED STEEL-3` in the first phase, then `KICKED STEEL-2` in the second phase, and ends with both bodies marked `lastTouchedBy: "player"` (`steel-3 z: -6.69`, `steel-2 z: 8.97`).
- 2026-03-30: Replaced the fake joystick/D-pad input path with a real analog 360-degree drag joystick. The lower-left control now reads pointer position from the circular base itself, stores continuous `moveX/moveY` values with a deadzone, drives the thumb by actual offset instead of four hidden direction buttons, and feeds analog movement strength into player and movable-chair motion.
- 2026-03-30: Revalidated the analog joystick conversion with the required smoke client in `output/web-game-analog-joystick-client/state-0.json` and a focused drag proof in `output/web-game-analog-joystick-proof/state.json`, `output/web-game-analog-joystick-proof/upper-right-close.png`, `output/web-game-analog-joystick-proof/lower-left-close.png`, and `output/web-game-analog-joystick-proof/upper-right-wide.png`. The focused proof records `errors: []`, diagonal thumb offsets of `(+25.53, -23.94)` and `(-23.94, +25.53)`, a clean release reset to `0px / 0px`, and live diagonal player motion on the second sample with `vx: 1.44` and `vz: -1.53`.
- 2026-03-30: Made the three right-side monitor windows perspective-aware. The head, arms, and legs monitor cameras no longer stay on one fixed side; they now choose a left or right 30-degree bias from the controlled actor based on the live world camera position, with a small threshold so the stack does not flicker when the main view is near center.
- 2026-03-30: Revalidated the perspective-aware portrait monitors with the required smoke client in `output/web-game-portrait-perspective-switch-client/state-0.json` and a focused side-switch proof in `output/web-game-portrait-perspective-switch-proof/state.json`, `output/web-game-portrait-perspective-switch-proof/camera-right-left-stack.png`, `output/web-game-portrait-perspective-switch-proof/camera-left-right-stack.png`, and `output/web-game-portrait-perspective-switch-proof/camera-right-left-wide.png`. The focused proof records `errors: []`, with the main camera at `x: -5.99` selecting monitor side `left`, and the opposite camera at `x: 5.99` selecting monitor side `right`.
- 2026-03-30: Extended the portrait-monitor perspective logic to front/back as well. The three small windows now resolve the world camera against both the actor’s local right axis and forward axis, so the stack can switch among front-right, front-left, back-right, and back-left instead of only flipping side while always showing the front.
- 2026-03-30: Revalidated the front/back portrait-monitor switch with the required smoke client in `output/web-game-portrait-depth-switch-client/state-0.json` and a focused depth proof in `output/web-game-portrait-depth-switch-proof/state.json`, `output/web-game-portrait-depth-switch-proof/back-right-back-right-stack.png`, `output/web-game-portrait-depth-switch-proof/front-right-front-right-stack.png`, and `output/web-game-portrait-depth-switch-proof/front-right-front-right-wide.png`. The focused proof records `errors: []`, with the same right-side orbit at `camera x: 5.99` resolving to monitor depth `back` when `z: -2.28`, then resolving to monitor depth `front` when the camera moves to `z: 6.28`.
- 2026-03-30: Inverted the portrait-monitor front/back mapping so the small windows now show the opposite depth from the user’s current perspective. Side selection still follows the live camera, but front-view gameplay now yields back-view monitors, and back-view gameplay yields front-view monitors.
- 2026-03-30: Revalidated the inverted portrait depth mapping with the required smoke client in `output/web-game-portrait-depth-invert-client/state-0.json` and updated focused proof in `output/web-game-portrait-depth-switch-proof/state.json`, `output/web-game-portrait-depth-switch-proof/back-right-front-right-stack.png`, `output/web-game-portrait-depth-switch-proof/front-right-back-right-stack.png`, and `output/web-game-portrait-depth-switch-proof/front-right-back-right-wide.png`. The focused proof records `errors: []`, with the same back-right main camera now resolving to monitor depth `front`, while the front-right main camera resolves to monitor depth `back`.
- 2026-03-30: Added hair color as a first-class outfit option. The style menu now includes a `HAIR` row, select mode can target the head for hair changes, and the human hair meshes update live from four new styles: `BLACK`, `WHITE`, `GOLD`, and `PINK`.
- 2026-03-30: Extended the outfit persistence/export path for hair color as well. Hair now rides through localStorage outfit snapshots, the backend-shaped `user_scene_outfits` row includes `hair_name`, `window.setOutfit()` accepts a `hair` field, and `render_game_to_text()` reports the current hair selection.
- 2026-03-30: Revalidated the hair-color feature with the required smoke client in `output/web-game-hair-color-client/state-0.json` and focused proof in `output/web-game-hair-color-proof/state.json`, `output/web-game-hair-color-proof/style-menu-hair.png`, `output/web-game-hair-color-proof/hair-black.png`, `output/web-game-hair-color-proof/hair-white.png`, `output/web-game-hair-color-proof/hair-gold.png`, and `output/web-game-hair-color-proof/hair-pink.png`. The focused proof records `errors: []` and confirms all four requested hair states render correctly.
- 2026-03-30: Added a dedicated `COFFEE BOT` passive robot that loops through a service cycle instead of random patrol: pause at the mug rack, carry an empty mug to the coffee machine, brew on the tray with visible fill/steam progress, then deliver the finished coffee to the lounge coffee table before returning for the next cup. The new robot is registered in the existing passive-robot persistence path, so its location is saved through the same localStorage/SQL-style flow as the other robots.
- 2026-03-30: Added the supporting coffee-service props and state. The coffee machine now exposes a tray cup anchor, the coffee area has a visible mug rack, the service robot carries its own mug prop, and the lounge coffee table now gets timed served-cup slots so the delivery loop leaves an actual coffee on the table before the next cycle continues.
- 2026-03-30: Revalidated the coffee-service loop with the required smoke client in `output/web-game-coffee-robot-client/state-0.json` and focused proof in `output/web-game-coffee-robot-proof/state.json`, `output/web-game-coffee-robot-proof/pickup.png`, `output/web-game-coffee-robot-proof/brew.png`, `output/web-game-coffee-robot-proof/brew-close.png`, `output/web-game-coffee-robot-proof/place.png`, `output/web-game-coffee-robot-proof/served.png`, and `output/web-game-coffee-robot-proof/served-top.png`. The focused proof records `errors: []`, hits `coffeeService.phase: "brew"` at `7200ms` with `brewProgress: 0.52`, reaches `coffeeService.phase: "place"` at `8000ms`, and confirms `tableCupsVisible: 1` once the robot turns to `phase: "return"`.
- 2026-03-30: Fixed the intermittent `MOVES` toggle input bug by converting the toggle controls to real buttons, adding `touch-action: manipulation`, and routing both `MOVES` and `PANELS` through a stable press helper that accepts `pointerup`, `click`, and keyboard activation while suppressing duplicate fires from the same tap.
- 2026-03-30: Revalidated the toggle fix with the required smoke client in `output/web-game-moves-toggle-fix-client/state-0.json` and a focused tap proof in `output/web-game-moves-toggle-fix-proof/state.json`, `output/web-game-moves-toggle-fix-proof/after-first-tap.png`, and `output/web-game-moves-toggle-fix-proof/after-second-tap.png`. The focused proof records `errors: []` and shows one tap opening the panel (`expanded: true`) and the next tap closing it (`expanded: false`).
- 2026-03-30: Moved the coffee mug rack off the floor and onto the coffee counter surface. The rack now uses a dedicated countertop `y` height while the `COFFEE BOT` keeps a separate floor station, so the visual placement matches the scene logic without spawning the robot inside the island.
- 2026-03-30: Revalidated the mug-rack placement with the required smoke client in `output/web-game-mug-rack-table-client/state-0.json` and focused proof in `output/web-game-mug-rack-table-proof/state.json`, `output/web-game-mug-rack-table-proof/rack-close.png`, and `output/web-game-mug-rack-table-proof/rack-wide.png`. The focused proof records `errors: []` and shows the rack resting on the white counter while the coffee bot remains on the floor beside it.
- 2026-03-30: Corrected the human sofa sit posture instead of masking it with a deeper sink. The sofa seat target now sits slightly farther forward, the sofa visual offset is lifted, and the sofa-specific seat pose now uses a higher torso/hips placement plus a fuller hip-knee chain so the avatar sits on the cushion edge with the lower legs dropping toward the floor.
- 2026-03-30: Revalidated the sofa posture fix with the required smoke client in `output/web-game-sofa-posture-client/state-0.json` and focused proof in `output/web-game-sofa-posture-proof/state.json`, `output/web-game-sofa-posture-proof/sofa-front.png`, `output/web-game-sofa-posture-proof/sofa-side.png`, and `output/web-game-sofa-posture-proof/sofa-angle.png`. The focused proof shows the human seated on `office-sofa` with `action: "sit"` and the updated framing confirms the body is no longer buried into the sofa body.
- 2026-03-30: Added a carryable-props system for realistically small objects only. The player can now pick up and drop boots, coffee mugs, and pantry jars through select mode, the `G` key, and object-action buttons. Each prop has an explicit logical weight (`0.4kg` mugs, `1.8kg` spare boots, `2.4–2.7kg` jars) that feeds a carry movement scale, while large fixtures like sofas, tables, and the kitchen island remain non-carryable because they are not registered as carryable props and keep their normal obstacle/object behavior.
- 2026-03-30: Revalidated the carryable-props feature with the required smoke client in `output/web-game-carry-props-client/state-0.json` plus focused carry proof in `output/web-game-carry-boots-study/state.json`, `output/web-game-carry-boots-study/a.png`, `output/web-game-carry-mug-proof/state.json`, `output/web-game-carry-mug-proof/mug-carried.png`, `output/web-game-carry-jar-study/state.json`, and `output/web-game-carry-jar-study/a.png`. The focused states confirm `interaction.carrying` for `SPARE BOOTS`, `COFFEE MUG 2`, and `PANTRY JAR 3`, and the earlier focused restriction proof in `output/web-game-carry-props-proof/state.json` still records `back-kitchen-island` as `kind: "obstacle"` with `objectActions: null`.
- 2026-03-30: Added a fourth jacket and pants palette to the outfit system: `NAVY JKT` for a dark blue jacket and `DK JEANS` for dark blue jeans. The live character outfit arrays, style cycling, persistence, and outfit rack displays all now include the extra top/pants entries.
- 2026-03-30: Revalidated the dark-blue outfit addition with the required smoke client in `output/web-game-dark-blue-outfit-client/state-0.json` and focused proof in `output/web-game-dark-blue-outfit-proof/state.json`, `output/web-game-dark-blue-outfit-proof/navy-outfit-front.png`, and `output/web-game-dark-blue-outfit-proof/style-menu-navy.png`. The focused state confirms `outfit.top: "NAVY JKT"`, `outfit.pants: "DK JEANS"`, and the rack selection payload now includes four top options (`DENIM JKT`, `BIKER BLK`, `LTHR TRENCH`, `NAVY JKT`) and four pants options (`LT BLUE`, `WHITE`, `BLACK`, `DK JEANS`).
- 2026-03-30: Resized the spare corner boots to use the same equestrian boot dimensions as the player’s worn equestrian boots instead of the older oversized decorative boot mesh. The spare-boot pair now sits directly on the floor, uses the live equestrian boot palette, and has a carry picker size updated to the new bounds.
- 2026-03-30: Revalidated the corner-boot size fix with the required smoke client in `output/web-game-corner-boots-size-client/state-0.json` and focused visual proof in `output/web-game-corner-boots-angle-study/d.png` plus the earlier compare capture `output/web-game-corner-boots-size-proof/boots-compare.png`. The smoke state now reports the spare boots at floor level with `carryables[spare-boots].y: 0`, and the focused angle shows the pair reading at the same equestrian scale as the player’s worn boots.
- 2026-03-30: Extended the carryable-props system so release is always available while carrying, even outside select mode. The object-actions panel now shows a `DROP` action whenever a prop is in-hand, the HUD selection line reflects the carried prop while drag mode is active, and the full-reset path now restores carryables to their home/default transforms.
- 2026-03-30: Extended persistence/export for carryable props. Per-user localStorage snapshots now include `carryables`, dropped world placement restores on reload/user switch, `render_game_to_text()` reports the new backend table name `user_scene_carryable_prop_locations`, and `window.getUserCarryablePropSqlRecords()` plus `window.getUserSessionSqlRecords()` now expose backend-shaped carryable rows with `prop_ref`, `prop_label`, `category`, `weight_kg`, `x`, `y`, `z`, `rot`, `is_carried`, `carrier_actor`, and `updated_at`.
- 2026-03-30: Revalidated carry release + drop-location persistence with the required smoke client in `output/web-game-carry-release-client/state-0.json` and focused proof in `output/web-game-carry-drop-persist-proof/state.json`, `output/web-game-carry-drop-persist-proof/carry-drop-panel.png`, `output/web-game-carry-drop-persist-proof/dropped-location.png`, and `output/web-game-carry-drop-persist-proof/restored-after-reload.png`. The focused proof records `errors: []`, confirms the drag-mode object panel exposes `DROP` while carrying `SPARE BOOTS`, shows the boots dropped at `(0, 0.12, 0.88)`, and confirms the same coordinates restore after reload with matching SQL/localStorage records.
- 2026-03-31: Reworked the coffee service loop around a dedicated storage corner instead of the old lounge-table drop. Added a 5-layer `COFFEE STORAGE RACK` plus adjacent `COLD FREEZER`, moved the coffee bot’s delivery logic onto that storage area, and made the rack cap at `100` cups before the bot transfers the full batch into the freezer and resumes the pickup -> brew -> rack loop.
- 2026-03-31: Extended persistence/export for the storage workflow. Per-user localStorage snapshots now include `coffeeStorage` (`rackCupCount`, `freezerCupCount`, `transferCount`, `cycleCount`), the world restores those counts on reload/user switch, and the backend-shaped `user_scene_coffee_storage_states` export now includes rack capacity/layout fields plus freezer batch, transfer, cycle, and service-phase columns for later SQL sync.
- 2026-03-31: Fixed a coffee-bot route bug uncovered during proofing. The storage-lane navigation path is now frozen per phase so the bot cannot shorten its route mid-walk and prematurely enter `freeze-batch` before actually reaching the freezer.
- 2026-03-31: Revalidated the coffee storage feature with the required smoke client in `output/web-game-coffee-storage-client/state-0.json` and focused proof in `output/web-game-coffee-storage-proof/state.json`, `output/web-game-coffee-storage-proof/storage-corner-wide.png`, `output/web-game-coffee-storage-proof/rack-layered.png`, and `output/web-game-coffee-storage-proof/after-transfer.png`. The focused proof records `errors: []`, shows `displayedRackCups: 60` with the rack spread across multiple shelves, then `rackCupCount: 0` / `freezerCupCount: 100` / `transferCount: 1` after the bot reaches the freezer, and confirms the saved localStorage snapshot plus `user_scene_coffee_storage_states` SQL row restore on reload for `coffee-storage-proof-user`.
- 2026-04-01: Added two decorative side coffee racks for the game areas: one beside the blackjack wall and one beside the magic chess board. Both reuse the existing multi-shelf coffee rack build, are scaled down for the entertainment corners, and keep non-selectable solid colliders so they do not hijack `SELECT MODE` from the blackjack or chess boards.
- 2026-04-01: Tuned the decorative rack cup display so the visible cups bias toward the upper shelves instead of only the lowest layer. This makes the blackjack and chess side racks read as active coffee stations from normal camera angles.
- 2026-04-01: Revalidated the entertainment coffee racks with the required smoke client in `output/web-game-entertainment-coffee-racks-client/state-0.json` and focused proof in `output/web-game-entertainment-coffee-racks-proof/state.json`, `output/web-game-entertainment-coffee-racks-proof/blackjack-rack.png`, and `output/web-game-entertainment-coffee-racks-proof/chess-rack-clean.png`. The focused proof records `errors: []`, confirms the main selections still resolve to `BLACKJACK WALL` and `MAGIC CHESS`, and records the new rack anchors at `(24.88, -9.54)` and `(22.96, -17.02)`.
- 2026-04-02: Reworked the live human boot base from a flat slab into a segmented gait-style foot profile. The biker, equestrian, and sneaker variants now use stepped heel/arch/forefoot/toe-rocker sole groups, the visible toe shells tip upward to match the rocker, sneaker boot pitch follows the same motion path, and the spare decorative boots reuse the same equestrian gait base so they stay visually consistent with the player boots.
- 2026-04-02: Revalidated the gait-style foot fix with the required smoke client in `output/web-game-gait-foot-client/state-0.json` plus focused standing/moonwalk proof in `output/web-game-gait-foot-proof/state.json`, `output/web-game-gait-foot-proof/standing-side.png`, `output/web-game-gait-foot-proof/moonwalk-side.png`, `output/web-game-gait-foot-proof-side/standing-profile.png`, and `output/web-game-gait-foot-proof-side/moonwalk-profile.png`. The focused profile captures show the sole no longer reads as a single flat plane and the moonwalk toe-off now lifts on the rocker instead of peeling off a flat slab.
- 2026-04-02: Rolled back the gait-style boot experiment at user request. The live human boots and spare decorative boots are back on the previous flat-sole structure, and moonwalk foot pitch is restored to the prior values.
- 2026-04-02: Fixed the remaining human boot pose bugs in `BACK KICK` and `MOONWALK` without reintroducing the gait-style sole. The human boot pitch now rotates around new ankle-level pivot wrappers for biker, equestrian, and sneaker footwear instead of rotating the full boot around the upper shin, which removes the visible boot/leg separation during kick and moonwalk poses while keeping the old flat-sole geometry.
- 2026-04-02: Revalidated the back-kick/moonwalk boot fix with the required smoke client in `output/web-game-boot-pivot-fix-client/state-0.json` and focused close-up proof in `output/web-game-boot-pivot-fix-proof/state.json`, `output/web-game-boot-pivot-fix-proof/idle-close.png`, `output/web-game-boot-pivot-fix-proof/back-kick-close.png`, and `output/web-game-boot-pivot-fix-proof/moonwalk-close.png`. The focused captures use white pants for contrast and show the boot tops staying attached to the lower leg during both moves instead of opening a large ankle gap.
- 2026-04-02: Extended the same ankle-pivot fix into the forward `KICK` path and the front half of `FULL KICK`. Boot pitch reset now restores each footwear pivot to its stored base position, forward kick uses a smaller ankle pitch on the striking leg, and the support leg returns to a neutral boot pitch so the boot cuff stays visually connected during the forward strike phase.
- 2026-04-02: Revalidated the forward-kick/full-kick boot fix with the required smoke client in `output/web-game-front-kick-boot-fix-client/state-0.json` and focused close-up proof in `output/web-game-front-kick-boot-fix-proof/state.json`, `output/web-game-front-kick-boot-fix-proof/front-kick-left.png`, `output/web-game-front-kick-boot-fix-proof/front-kick-right.png`, `output/web-game-front-kick-boot-fix-proof/full-kick-front-left.png`, and `output/web-game-front-kick-boot-fix-proof/full-kick-front-right.png`. The proof captures use white pants plus opposite side views and show the front kick plus the front phase of `FULL KICK` holding the ankle/boot join instead of opening the earlier cuff gap.
- 2026-04-02: Fixed the actual rear-boot structure bug. The tall boot shaft and cuff are now separated from the rotating foot section, so kick and moonwalk-style foot pitch no longer hinge the whole boot backward and open the rear seam under the calf.
- 2026-04-02: Revalidated the rear-boot structure fix with the required smoke client in `output/web-game-boot-backfix-client/state-0.json` and focused rear-angle proof in `output/web-game-boot-backfix-proof/state.json`, `output/web-game-boot-backfix-proof/kick-rear-left.png`, `output/web-game-boot-backfix-proof/back-kick-rear-left.png`, and `output/web-game-boot-backfix-proof/full-kick-front-rear-left.png`. The proof captures show the foot rotating independently while the rear boot shaft stays upright and attached instead of folding backward with the sole.
- 2026-04-02: Cleaned up the human thigh/knee/shin silhouette after the user pointed out the leg stack looked wrong. The thigh and shin volumes are slightly slimmer and less boxy, the knee cap is smaller and better centered, and the raised pant seam geometry/opacity is reduced so white pants no longer read like an extra rectangular leg piece in oblique views.
- 2026-04-02: Revalidated the leg-structure cleanup with the required smoke client in `output/web-game-leg-structure-fix-client/state-0.json` and focused oblique proof in `output/web-game-leg-structure-fix-proof/state.json`, `output/web-game-leg-structure-fix-proof/standing-oblique.png`, and `output/web-game-leg-structure-fix-proof/kick-oblique.png`. The proof captures show the same white-pants angle with a cleaner thigh-knee-shin stack and a reduced seam silhouette during both standing and kick poses.
- 2026-04-02: Fixed the remaining white block artifact at the top of the boots by tucking the lower pant cylinder and cuff higher inside the boot shaft. The tucked shin geometry is shorter and the cuff is thinner/higher, so the white pant edge no longer hangs below the black boot shell in the oblique standing/kick views.
- 2026-04-02: Revalidated the tucked-pants/boot seam fix with the required smoke client in `output/web-game-leg-tuck-fix-client/state-0.json` and focused oblique proof in `output/web-game-leg-tuck-fix-proof/state.json`, `output/web-game-leg-tuck-fix-proof/standing-oblique.png`, and `output/web-game-leg-tuck-fix-proof/kick-oblique.png`. The latest proof removes the visible white blocks that were still showing at the boot tops in the previous oblique screenshots.
- 2026-04-02: Rebuilt the human knee/leg layout to behave more like the robot’s cleaner joint split. The upper pant now ends above the knee, the knee joint sits in a clearer gap, and the human shin is a full lower-leg segment instead of a tiny top block with most of the leg implied by pant filler.
- 2026-04-02: Revalidated the human knee/leg joint rebuild with the required smoke client in `output/web-game-leg-joint-fix-client/state-0.json` and focused oblique proof in `output/web-game-leg-joint-fix-proof/state.json`, `output/web-game-leg-joint-fix-proof/standing-oblique.png`, and `output/web-game-leg-joint-fix-proof/kick-oblique.png`. The updated proof shows a clearer upper-leg/knee/lower-leg stack in both standing and kick poses.
- 2026-04-02: Fixed the human lower-leg alignment after the user pointed out the shin was still sitting forward of the knee line. The whole leg root now sits on the center plane instead of a forward offset, the knee center is pulled back to the same plane, and the shin group plus pant seam/cuff depths are shifted backward so the standing/kick side profile aligns as a single vertical leg column.
- 2026-04-02: Revalidated the human knee/leg alignment fix with the required smoke client in `output/web-game-knee-align-fix-client/state-0.json` and focused side/oblique proof in `output/web-game-knee-align-fix-proof/state.json`, `output/web-game-knee-align-fix-proof/standing-side.png`, `output/web-game-knee-align-fix-proof/standing-oblique.png`, `output/web-game-knee-align-fix-proof/kick-side.png`, and `output/web-game-knee-align-fix-proof/kick-oblique.png`. The exact side proof now shows the shin directly under the knee instead of shifted forward.
- 2026-04-03: Added a real hinged `COFFEE FREEZER` door for the storage corner. The freezer now has a hollow shell interior, an animated door pivot, selectable `OPEN DOOR` / `CLOSE DOOR` actions in `SELECT MODE`, and a visible shelf display for stored coffee batches. The coffee-storage snapshot and future SQL export now also carry `freezerDoorOpen` / `freezer_door_open`.
- 2026-04-03: Revalidated the freezer-door change with the required smoke client in `output/web-game-freezer-door-client/state-0.json` and focused proof in `output/web-game-freezer-door-proof/state.json`, `output/web-game-freezer-door-proof/state-close.json`, `output/web-game-freezer-door-proof/door-closed-close.png`, and `output/web-game-freezer-door-proof/door-open-close.png`. The focused proof records `errors: []`, `freezerCupCount: 300`, `displayedFreezerBatches: 3`, and a live door state change from `doorOpen: false` to `doorOpen: true` with the cups visible on the open freezer shelves.
- 2026-04-03: Fixed the freezer-door interaction bug in the real UI path. The `OPEN DOOR` button had a stack-overflow bug because the exported `window.toggleCoffeeFreezerDoor` helper shadowed the internal toggle function; the internal handler is now separate again, so the actual button click opens and closes the door correctly.
- 2026-04-03: Revalidated the real freezer button click with focused proof in `output/web-game-freezer-door-click-proof/state.json`, `output/web-game-freezer-door-click-proof/before-click.png`, and `output/web-game-freezer-door-click-proof/after-click.png`. That proof records `errors: []` and shows the actual button click changing `freezerDoorOpen` from `false` to `true` with the panel switching from `OPEN DOOR` to `CLOSE DOOR`.
- 2026-04-03: Fixed the remaining freezer-door tap bug on touch input. The shared `bindStablePressAction()` helper was firing once on `pointerup` and again on the synthetic follow-up `click`, so a single tap could immediately undo the first toggle. The helper now suppresses the click that follows a handled pointer activation, and the freezer object-action button also uses the same touch-safe press path plus `touch-action: manipulation`.
- 2026-04-03: Revalidated the touch freezer-door fix with focused tap proof in `output/web-game-freezer-door-tap-proof/state.json`, `output/web-game-freezer-door-tap-proof/before-tap.png`, and `output/web-game-freezer-door-tap-proof/after-tap.png`, where one tap changes `freezerDoorOpen` from `false` to `true` with `errors: []`. Also reran the required smoke client in `output/web-game-freezer-door-touchfix-client/state-0.json`.
- 2026-04-03: Added a robot auto-close path for the coffee freezer door. If the user leaves the door open, `COFFEE BOT` now notices during its idle return/pickup window, routes to the freezer with a dedicated `to-close-freezer` phase, closes the door in `close-freezer`, persists the closed state, and then resumes the normal coffee cycle. The freezer panel also now explains that the bot will close the door later if the user leaves it open.
- 2026-04-03: Revalidated the robot freezer auto-close with focused proof in `output/web-game-freezer-robot-close-proof/state.json`, `output/web-game-freezer-robot-close-proof/state-after-loop.json`, `output/web-game-freezer-robot-close-proof/door-left-open.png`, `output/web-game-freezer-robot-close-proof/robot-closing.png`, `output/web-game-freezer-robot-close-proof/after-robot-close.png`, and `output/web-game-freezer-robot-close-proof/after-return-loop.png`. That proof records `openedRequested: true`, then `closingPhaseSeen: true`, then `finalRequestedOpen: false`, `finalDoorOpen: false`, and `finalPhase: "return"`. Also reran the required smoke client in `output/web-game-freezer-robot-close-client/state-0.json`.
- 2026-04-22: Added the first pass of the forward wall browser screen in `character.html`: a huge selectable north-wall display, `BROWSER SCREEN` object actions, a full-page iframe browser overlay with URL/load/refresh/open-tab/close controls, default URL `https://www.dp0.io/`, and `browserScreen` state/debug hooks. Inline script syntax passed via `node --input-type=module -e`; Playwright visual and interaction validation still pending.
- 2026-04-22: Revalidated the browser screen. Required smoke client passed in `output/web-game-browser-screen-client/state-0.json` / `shot-0.png` with `browserScreen.url: "https://www.dp0.io/"` and the huge forward wall screen visible. Focused proofs in `output/web-game-browser-screen-proof/`, `output/web-game-browser-screen-www-long/`, and `output/web-game-browser-screen-button-proof/` confirm selection/object actions, the real `OPEN BROWSER` button, iframe loading, and close behavior. Note: Chromium reports one external `ERR_CERT_COMMON_NAME_INVALID` resource warning from the dp0 page itself, but the app state reaches `frameLoaded: true` and the dp0 page renders in the overlay.
- 2026-04-22: Corrected the browser screen placement after the user clarified it belongs next to the ping-pong table, not on the +Z back wall. `WORLD_BROWSER_SCREEN` now sits at the front-right/ping-pong corner near `x: 21.25, z: -18.4`, rotated to face the table. Revalidated with the required client in `output/web-game-browser-pingpong-client/` and focused proof in `output/web-game-browser-pingpong-proof/`; screenshots show the screen beside the table, selection actions still work, and `OPEN BROWSER` still opens `https://www.dp0.io/`.
- 2026-04-22: Moved the browser screen again to use the open player-side floor space near ping-pong instead of the compact side slot. It is now centered beyond the ping-pong table at `x: 13.4, z: -24.25`, with a wider `8.8 x 3.7` display facing back into the table/play area. Revalidated with the required client in `output/web-game-browser-open-area-client/` and focused proof in `output/web-game-browser-open-area-proof/`; screenshots show the screen in the open area, selection actions work, and `OPEN BROWSER` loads `https://www.dp0.io/`.
- 2026-04-23: Added a daily-accessible `DAILY PORTAL` floor node that seeds its location from the local calendar date and moves among curated open-floor positions once per day. The portal exposes a proximity prompt plus `SELECT MODE` actions (`ENTER PORTAL`, `OPEN TAB`), opens the in-world browser to `https://www.dp0.to/asd/`, and exports its date/location/url state through `render_game_to_text()` and new window hooks. Fixed two portal regressions during proofing: the exported `window.enterDailyPortal()` helper now calls a separate internal action instead of recursing, and switching into `SELECT MODE` dismisses the touch prompt so the object-action panel stays readable.
- 2026-04-23: Revalidated the daily portal with the required smoke client in `output/web-game-daily-portal-client/state-0.json` / `shot-0.png` and focused proof in `output/web-game-daily-portal-proof/state.json`, `output/web-game-daily-portal-proof/portal-view.png`, `output/web-game-daily-portal-proof/portal-prompt.png`, `output/web-game-daily-portal-proof/portal-selected.png`, and `output/web-game-daily-portal-proof/portal-opened.png`. The proof records `errors: []`, confirms today (`2026-04-23`) seeds the portal to `FRONT EAST FLOOR` at `(20.6, 0, -23.4)`, shows the prompt when the player stands on it, shows clean select-mode actions without the prompt overlap, and confirms `ENTER PORTAL` routes the world browser to `https://www.dp0.to/asd/`.
- 2026-04-24: Fixed the daily portal spawn pool after the user pointed out the portal had landed on an inaccessible raised surface. Replaced the bad candidates that sat under the giant browser screen and on the magic chess board with verified walkable-floor positions, and added a runtime clearance check so the seeded date hash now skips any candidate that overlaps static colliders or non-floor terrain before choosing the day's portal location.
- 2026-04-24: Revalidated the floor-only portal fix with the required smoke client in `output/web-game-daily-portal-floorfix-client/state-0.json` / `shot-0.png` and focused proof in `output/web-game-daily-portal-floorfix-proof/state.json`, `output/web-game-daily-portal-floorfix-proof/portal-initial.png`, `output/web-game-daily-portal-floorfix-proof/portal-prompt.png`, and `output/web-game-daily-portal-floorfix-proof/portal-selected.png`. The updated proof records `errors: []`, confirms today (`2026-04-24`) now seeds the portal to `EAST MID FLOOR` at `(24.2, 0, -19.8)`, shows the portal on accessible office floor next to the east shelving/wall area, and confirms the touch prompt plus select-mode actions still work.
- 2026-04-30: Fixed the macOS WebView keyboard-beep issue by making the scene canvas focusable/focused, restoring canvas focus when the world browser closes, and consuming the game's control keys (`WASD`, arrows, space, prompt keys, hotkeys) at the page level so they do not fall through as unhandled WebView key input. Text entry remains allowed inside the in-world browser URL field, while `Escape` / `Enter` there still keep their custom close/load behavior.
- 2026-04-30: Revalidated the WebView keyboard fix with the required smoke client in `output/web-game-keyboard-beep-client/state-0.json` / `shot-0.png` and focused proof in `output/web-game-keyboard-beep-proof/state.json` / `keyboard-proof.png`. The focused proof confirms the active element is the scene `CANVAS` on load, `ArrowRight` and `KeyW` are default-prevented at the window level, the browser URL input still accepts `KeyW` when focused, `Escape` there still closes the overlay, and the smoke client still moves the player (`player.x: -0.26`, `action: "walk"`). Note: opening `https://www.dp0.io/` still logs the same external `ERR_CERT_COMMON_NAME_INVALID` resource warning from the remote page itself.
- 2026-04-30: Changed the daily portal transit flow to redirect the current page directly to `https://www.dp0.to/asd/` instead of opening the in-world browser overlay. Updated both the touch prompt copy and the select-mode portal panel copy so they now describe leaving the current page, while keeping `OPEN TAB` as the secondary option.
- 2026-04-30: Revalidated the direct portal redirect with the required smoke client in `output/web-game-portal-redirect-client/state-0.json` / `shot-0.png` and focused proof in `output/web-game-portal-redirect-proof/state.json`, `output/web-game-portal-redirect-proof/portal-before-redirect.png`, and `output/web-game-portal-redirect-proof/portal-after-redirect.png`. The focused proof records `errors: []`, confirms the prompt was active on `FRONT EAST FLOOR`, and shows the real `ENTER PORTAL` button driving the page URL to `https://www.dp0.to/asd/`.
- 2026-04-30: Added a one-time daily-portal return guard so coming back from the external URL no longer drops the restored actor directly back onto the portal trigger. Entering the portal now writes a same-tab `sessionStorage` marker and persists the current user state before redirect; on the next load of the office scene, the marker is consumed once and the restored controlled actor is pushed a safe distance away from the portal before prompts or repeat transfers can fire.
- 2026-04-30: Revalidated the portal return guard with the required smoke client in `output/web-game-portal-return-guard-client/state-0.json` / `shot-0.png` and focused proof in `output/web-game-portal-return-guard-proof/state.json`, `output/web-game-portal-return-guard-proof/before-redirect.png`, and `output/web-game-portal-return-guard-proof/after-return.png`. The focused proof records `errors: []`, confirms the player left from `FRONT EAST FLOOR` while touching the portal, then after reloading the office scene in the same tab the player is restored at `z: -21.5` with `portalDistanceAfterReturn: 1.9`, `touchingAfterReturn: false`, and `promptAfterReturn: false`.
- 2026-07-15: Added the male VRM avatar (`player.vrm`) from the mochikick soccer-field project as a new third playable character (`assets/models/player.vrm`, loaded via three r128 `GLTFLoader` + `@pixiv/three-vrm@0.6.11`). Added a `VRM` entry to the `STYLE MENU` `CTRL` selector alongside `HUMAN`/`ROBOT`; it spawns at `VRM_STATION` (x:2.4, z:2, next to the human spawn) as a fully independent third actor (own `controlledActorStates.vrm` entry, own station, selectable/controllable like the robot). Reused `animateRobotRig(rig, actorState)` verbatim for the VRM by building a duck-typed `vrmRig` whose `torsoGroup`/`headGroup`/`leftArm`/`rightArm`/`leftLeg`/`rightLeg` are proxy objects that forward `.rotation.x/y/z` writes onto the VRM's real humanoid bone nodes (with a per-bone sign/rest correction, e.g. arms rest at `z: ±1.16` to hang naturally instead of T-pose) — this gives the VRM character the same walk/run/jump/sit/kneel/wave/kick/spin/moonwalk pose set as the robot for free, no animation code duplicated.
- 2026-07-15: Fixed VRM materials failing to render: `@pixiv/three-vrm@0.6.11`'s MToon shader does not compile under this page's WebGL2 context (`punctualLightIntensityToIrradianceFactor` overload error, all 153 meshes affected). Added `fixVrmMaterials()`, which swaps every `ShaderMaterial` on the loaded VRM scene for a plain `MeshStandardMaterial` (reusing the original texture map, skinning enabled) immediately after load and before adding it to the scene.
- 2026-07-15: Initially validated by serving the folder over `python3 -m http.server 8893` since `fetch()` of the local `.vrm` fails under `file://`. Confirmed via the Browser pane: default `HUMAN` load is unaffected (no regressions), switching `CTRL` to `VRM` loads and displays the avatar at the correct scale/position/facing, and driving it with `W` produces a correct walk cycle (forward knee bend, opposing arm swing, `WALK 2.4 m/s` in the HUD) with the camera following normally. Known gaps not yet addressed: the security-camera "portrait" mirror view and per-bone foot-pitch still assume the procedural human/robot rigs when VRM is active (cosmetic only), and only walking was verified beyond the reused pose functions — sit/kneel/jump/kick/spin/moonwalk should look right (same code path as ROBOT) but were not individually screenshotted this session.
- 2026-07-15: Removed the localhost dependency so the page still opens by plain double-click (`file://`), matching how this project has always been used. `player.vrm` is no longer a separate fetched file; it's base64-embedded directly in `character.html` inside a `<script type="text/plain" id="vrm-player-base64">` tag (injected via a one-off Python script — a 24.6M-character line can't go through a normal text edit), and `loadVrmAsset()` now reads that tag, decodes it to an `ArrayBuffer`, and calls `GLTFLoader.parse()` instead of `loader.load(url)`. This raises `character.html` from ~56KB to ~25.4MB (single file, same tradeoff the `3d_objects` viewer project made for the same reason — see the embedded-VRM technique used there). Deleted the now-unused external `assets/models/player.vrm` copy and the temporary `.claude/launch.json`/http-server test scaffold. Re-verified the embedded path in the browser: the VRM's world-space bounding box and visual appearance are identical to the pre-embed version, confirming the swap didn't change loading behavior, only where the bytes come from.
- 2026-07-15: Made the VRM character auto-walk on its own, matching the passive service-robot patrol behavior. Added `VRM_PATROL_ROUTE` (a small 5-point loop around `VRM_STATION`) and `updatePassiveVrm(dt)` — a direct adaptation of `updatePassiveRobot` (route-follow via `moveActor`, pause 0.32s at each waypoint, `action` flips to `walk`/`idle` off actual speed). Called from `applyScene()` only when `activeControlledActor !== 'vrm'`, so the avatar patrols the loop by itself whenever the player isn't driving it, and driving it (via the `CTRL: VRM` style-menu option) still overrides normal WASD control as before; releasing control resumes the patrol from wherever it was left. Verified in the browser (temporary local server, for testing only — the shipped file no longer needs one): the avatar's position advances on its own frame-to-frame with `action: "walk"`, a mid-stride screenshot shows a natural walking pose, and manually switching control to/from `vrm` doesn't crash or desync its position.
- 2026-07-15: Added a second, distinct VRM character: same base model (`player.vrm`, re-parsed independently — no second copy embedded), but with the real tall-riding-boot mesh transplant from the mochikick soccer-field project and a yellow top, auto-patrolling a wider loop across the open office floor. Ported the transplant pipeline from `mochikick/soccer3d.html` (`hideOwnShoes`, `removeOwnFeet`, `splitGeometryByX`, `indexedBounds`, `filterTriangles`, `legBasis`, `addKeeperBootTransplant`) verbatim except for one required API adaptation: that project uses `@pixiv/three-vrm@3.5.4` (ESM, `humanoid.getRawBoneNode`), this page uses the older `0.6.11` UMD build embedded here (`humanoid.getBoneNode` — 0.6.x has no raw/normalized bone split, so it's a direct swap) — confirmed via live inspection of both a fresh `keeper.vrm` parse and the existing `player.vrm` rig before porting, rather than assuming API parity. The transplant bakes `keeper.vrm`'s own sculpted shoe mesh and lower-leg skin geometry (the "boot" look is actually her painted leg + separate shoe mesh) out of its rest pose, splits it left/right, and remounts it on this model's leg bones at the correct scale; `addProceduralRidingBoots` (lathe-turned shaft, ported from the same source file) is kept as an automatic fallback if the transplant throws. Embedded `keeper.vrm` as a second `<script type="text/plain" id="vrm-keeper-base64">` payload (same injection technique as the player model) — `character.html` is now ~44.5MB. Added `recolorVrmMaterials(scene, /Tops/i, hex)` for the yellow top (VRoid material naming, e.g. `M00_006_01_Tops_01_CLOTH`, confirmed by inspecting the live model's actual material names before writing the regex, not guessed) and generalized `makeVrmCharacterRig` to take a `decorate(vrm)` hook so the two characters share all loading/rig-building code. Fixed a latent bug found while wiring this up: `fixVrmMaterials` was dropping each material's `.name` when swapping MToon → `MeshStandardMaterial`, which would have silently broken every name-regex-based lookup (boot transplant, recolor) — now the name is preserved.
- 2026-07-15: Picked the second character's patrol route (`VRM2_PATROL_ROUTE`, `VRM2_STATION` at x:-1,z:-3) empirically rather than guessing office bounds: probed candidate points and segment midpoints against the real `moveActor` collision resolution (teleport a scratch actor state to a point, call `moveActor` with zero input, check how far it gets pushed) to confirm a ~6×7.5-unit tour of the open floor near the human/robot spawn is actually clear — this caught a real obstacle (something around x:7-10, z:2-3, likely pantry/desk clutter) that a straight-line first-guess loop would have driven straight into, and the route was reshaped to go around it before ever loading it into the visible scene. Refactored the single-character `updatePassiveVrm` into a shared `updatePatrolActorState(state, route, dt, speedScale)` used by both characters. Verified via `window.advanceTime()`: `routeIndex` cycles through all 5 waypoints over a simulated 44s with `action: "walk"` throughout, confirming the loop completes rather than snagging on the obstacle it was routed around. Visual verification (temporary local server, for testing only): screenshots confirm a yellow hoodie and symmetric tall black boots on both legs, and mesh-count accounting (159 vs. the plain character's 153) matches exactly 3 new meshes × 2 legs, confirming the real mesh transplant succeeded rather than silently falling back to the procedural boots.
- 2026-07-15: User reported the yellow character's boots were missing. Root cause: `THREE.CapsuleGeometry` — used in three call sites ported from `mochikick/soccer3d.html` (the sole in the real transplant, plus the instep/sole in the procedural fallback) — does not exist in three.js r128 (this page's version; `CapsuleGeometry` wasn't added to three.js until well after r128, and `soccer3d.html` gets it for free via a modern unpinned import). Confirmed live (`new THREE.CapsuleGeometry(...)` threw `"THREE.CapsuleGeometry is not a constructor"`, `typeof THREE.CapsuleGeometry === 'undefined'`) rather than assumed. This meant the real transplant threw partway through the first leg (after hiding the model's own shoes and deleting its own foot geometry, so the failure mode was worse than a no-op — bare feet with nothing covering them), the automatic procedural fallback then *also* hit the same missing constructor and threw too, and both errors were silently swallowed by the existing try/catch layers, leaving no boots at all. This also means the "159 meshes, looks like boots" verification from the initial build was a false positive — the mesh count coincidentally matched (partial real transplant + partial procedural fallback pieces summed to the same count as a full success), and the screenshot at the time happened to be angled such that the partial/asymmetric result wasn't obviously wrong; geometry *type* (not just count) is what actually distinguishes success from partial failure here. Fix: added `makeCapsuleGeometryCompat(radius, length, capSegments, radialSegments)`, a same-signature drop-in replacement built from `THREE.LatheGeometry` (hemisphere-cap + straight-side profile revolved around Y) — r128 has always had `LatheGeometry`. Swapped all three call sites. Re-verified with the stricter check this time (geometry `.type` and vertex count per extra mesh, not just a total): now 8 extra meshes on `vrmRig2` — 6 `BufferGeometry` (the real transplanted shoe + leg-shaft pieces, ~6648 verts each, both legs symmetric) + 2 `LatheGeometry` (the sole caps) — confirming the real transplant now completes fully on both legs with no fallback needed, and a close-up screenshot shows two matching tall black boots with visible sculpted surface detail.
- 2026-07-15: Added a dedicated "🧍 WHITE MALE" button (`#btn-ctrl-white-male`, in the MOVES panel's `CORE ACTIONS` grid) that switches control directly to the original VRM avatar (`setControlledActor('vrm', true, true)`) in one click, instead of needing to cycle the `CTRL` style-menu button through `HUMAN → ROBOT → VRM`. Wired with the existing `bindStablePressAction` debounced-click helper, same pattern as the other one-shot action buttons.
- 2026-07-15: Fixed the three "LIVE FACE" portrait mini-windows (face/arms/legs close-up canvases, top-right) so they show the actually-active character instead of always falling back to the procedural human rig. `activePortraitRig()` previously only branched on `activeControlledActor === 'robot'` vs. a hardcoded human default — driving the VRM character left these windows silently showing the wrong body. Added a `vrm` branch that anchors the three views to the VRM's *real* humanoid bone nodes (`head`, `leftHand`/`rightHand`, `leftFoot`/`rightFoot` via a new `rig.getBone` accessor exposed off `makeVrmCharacterRig`) rather than the bone-rotation proxy objects used for animation, since the portrait code needs real `Object3D.getWorldQuaternion`/`localToWorld`, which the proxies don't implement. Added matching local offset vectors (`portraitVrmFaceLocal/HandLocal/FootLocal`) tuned by eye against a live screenshot (VRM raw bone-local axis conventions aren't guaranteed to match the procedural rig's, per the known VRM0 bone-sign landmine — this needed empirical checking, not derivation). Guards against the pre-load window (bones null before the async VRM parse resolves) by falling through to the human default if any anchor bone is missing. Camera distance/FOV/lift constants are unchanged (still keyed by `rig.type === 'robot' ? robotValue : humanValue`, and `'vrm'` naturally takes the human branch since it's roughly human-scale) — verified this was an adequate starting point via screenshot, no separate `'vrm'` tuning needed. Verified via the browser: clicking the new WHITE MALE button switches control and all three portrait windows immediately update to show the VRM's face/torso/legs correctly framed; switching back to HUMAN and ROBOT still renders their own portraits correctly (no regression).
- 2026-07-15: Added matching "🧑 HUMAN" and "🤖 ROBOT" quick-switch buttons (`#btn-ctrl-human`, `#btn-ctrl-robot`) next to WHITE MALE in the same `CORE ACTIONS` grid, so control can be switched directly to any of the three actors and back, not just one-way toward the VRM. Same `bindStablePressAction` + `setControlledActor(key, true, true)` pattern as WHITE MALE. Verified the full round trip via real UI clicks in the browser: WHITE MALE → `activeControlledActor: "vrm"` → ROBOT → `"robot"` → HUMAN → `"human"`, with the `CTRL` style-menu label and all three portrait mini-windows updating correctly at each step and no regression on the final HUMAN state.
- 2026-07-15: Made the VRM's position/rotation persist across reloads via the same localStorage session mechanism already used for the HUMAN character ("the female", per the 2026-03-20 rebuild — see the character-model-replacement entries above). That mechanism (`buildUserSessionSnapshot`/`persistUserSession`/`restoreUserSession`, auto-saved on a timer and on demand) previously only had `human`/`robot` entries in its `actors` map and a hardcoded `restoredActorKey = snapshot.controlledActor === 'robot' ? 'robot' : 'human'` binary — driving the VRM and reloading always snapped it back to `VRM_STATION` instead of remembering where it was. Fixed by: (1) adding `vrm: actorSessionSnapshot(controlledActorStates.vrm)` to `buildUserSessionSnapshot()`'s `actors` map (no other change needed there — `userSessionSignature` already serializes `actors` wholesale, so change-detection picked it up automatically); (2) generalizing the restore-target check to `CHARACTER_INDEX_TO_ACTOR_KEY.includes(snapshot.controlledActor) ? snapshot.controlledActor : 'human'` (reusing the `['human','robot','vrm']` list added earlier for the CTRL cycle button) and adding `applySnapshotToActorState('vrm', snapshot.actors?.vrm)` — that apply function was already actor-agnostic (looks up `controlledActorStates[actorKey]` generically), so no changes were needed there; (3) for consistency, added the same "reset VRM to its station" block already present for human/robot in both `applyDefaultUserRuntime()` (first-ever visit, no saved snapshot) and the separate `resetGameState()` world-reset path, so the VRM doesn't become the one actor left stranded out of position when everything else resets. Verified end-to-end in the browser: switched control to `vrm`, set its live position/rotation directly (`pos.set(7.77,0,-4.44)`, `charYRot=1.23`), force-persisted, did a full page navigate (fresh JS context, real reload not a soft reset), and confirmed both `controlledActorStates.vrm` and the live runtime position matched exactly on load, with a screenshot showing the avatar rendered at the restored spot and `CTRL: VRM` correctly re-selected.
- 2026-07-15: Added a `#loading-overlay` splash screen (spinner + bouncing-dots + live "N / 2 avatars ready" counter) that covers the whole page from first paint and only fades away (0.65s opacity+scale transition) once both embedded VRM avatars have actually finished loading, instead of the playground being visible immediately while the two large base64 payloads are still being decoded/parsed/boot-transplanted in the background. Implementation: `makeVrmCharacterRig()` now stores its internal load-and-decorate promise chain on the returned rig as `rig.ready` (already internally caught, so it always *resolves*, never rejects — a failed VRM load still counts as "done" instead of hanging the loader forever); a small IIFE right after `vrmRig`/`vrmRig2` are constructed watches `[vrmRig.ready, vrmRig2.ready]`, updates the progress counter as each settles, and reveals the page once both are in; a 20s fallback timer force-reveals regardless, so a stuck load can never leave the page permanently blank. The overlay markup sits as the very first element in `<body>` (before the huge embedded `<script type="text/plain">` payloads) so it paints as soon as that much of the document is parsed, rather than waiting on the rest of the ~44MB file. Verified in the browser across two separate fresh loads: a screenshot taken immediately after navigation caught the overlay mid-load showing "1 / 2 avatars ready" with the spinner animating (proving it gates on real load state, not a fixed timer), and after both finish the overlay's computed style settles to `display: none` (fully non-interactive, not just visually hidden) with the complete scene — all four characters, HUD, and portrait windows — visible underneath.
- 2026-07-15: Added a fifth character: "Vita", the white-haired VRoid figure from the user's `3d_objects` gallery project (`coffee_mug_3d.html`, "Original Figure" entry), identified by cross-referencing a screenshot the user provided against that project's own gallery labels and page text ("The same Vita VRoid model in her original outfit") rather than guessing from memory. No new asset import was needed — she's `keeper.vrm`, which this project already has fully embedded as `vrm-keeper-base64` (previously only used as the boot-mesh donor for the yellow-top character). Confirmed the identification directly in this project before wiring anything up: loaded `vrm-keeper-base64` standalone via the existing `loadVrmAsset`/`fixVrmMaterials` helpers, dropped her raw scene into the live scene at a throwaway spot, and screenshotted her — matching the user's reference image exactly (white/silver hair with blue cat-ear-shaped ornaments, green eyes, black/cyan/purple geometric bodysuit). Added her as a fifth rig via the now-fully-generalized `makeVrmCharacterRig({ scriptId: 'vrm-keeper-base64', debugName: 'vitaRig' })` — no `decorate` hook, since she should keep her own original outfit — following the same pattern as `vrmRig`/`vrmRig2` (bone-rotation-proxy rig reusing `animateRobotRig`, same T-pose arm-rest correction, which turned out to apply correctly to her too without changes). Gave her a small patrol loop (`VITA_STATION`/`VITA_PATROL_ROUTE`, a southwest-quadrant rectangle around x:-9..-5, z:-7..-3.5) picked with the same empirical collision-probing technique used for the yellow-top character's route, in an unused part of the map so her loop doesn't cross the others'. She's NPC-only (not added to `CHARACTER_INDEX_TO_ACTOR_KEY`/`CTRL`), matching the precedent set by the yellow-top character rather than the controllable white-male VRM. Extended the loading overlay to track her too (`[vrmRig.ready, vrmRig2.ready, vitaRig.ready]`, counter text now "N / 3 avatars ready"). Verified in the browser: she renders with a natural arms-down idle pose (not T-posed, confirming the existing rest-pose correction generalizes to a second donor model), and `window.advanceTime()` shows her patrol `routeIndex` cycling through all waypoints continuously with `action: "walk"` — with no regressions on the other four characters (all still visible, no console errors).
- 2026-07-15: Gave Vita the tall black leather boots from the `3d_objects` gallery's "Anime Figure" variant (as opposed to "Original Figure", which is what got added above). Located and ported the dedicated boot-builder code from `coffee_mug_3d.html` — `createBootShaftGeometry` (a custom hand-built `BufferGeometry` lathe-like column, not `LatheGeometry`/`CapsuleGeometry`, so no r128 compatibility concerns), `createBootFootMesh`/`createBootSoleMesh` (via a ported `centeredExtrude` helper over `THREE.Shape`+`ExtrudeGeometry`), and the black-leather/sole/buckle materials — as a new `addFigureStyleBoots(vrm)`, wired in as `vitaRig`'s `decorate` hook. The source project's own placement code (`addFigureBoots`) couldn't be ported as-is: it positions boot pieces using an absolute `groundY` constant tuned for that project's fixed pedestal-display scale (models forced to exactly 2.3 units tall), which doesn't hold in this project (VRMs rendered at their natural scale, matching the other characters) — so placement was rewritten from scratch using this project's existing bone-relative technique (ankle/knee/toe positions in the `lowerLeg` bone's local space, same pattern as `addProceduralRidingBoots`/`addKeeperBootTransplant`) while reusing the ported geometry/material code verbatim. Two real placement bugs were caught and fixed by iterating on close-up screenshots (not assumed correct from the port): (1) the shaft initially pointed *down into the floor* instead of up the shin — `ankleLocal` (knee→ankle, i.e. downward) was used directly as the "up" direction instead of negated; (2) the foot/sole piece left her bare toes sticking out — using the raw ankle→toes vector for orientation tilted the foot down into the ground (VRM toe bones commonly sit lower than the ankle in rest pose), fixed by flattening it against the shin's up-axis with the existing `legBasis()` helper (already used elsewhere in this file) so the foot sits level, plus a small forward nudge to close the last sliver of visible toe. Verified via a sequence of close-up screenshots through each fix (each one caught and visibly resolved the specific defect it targeted, not just "looks fine now") and a final normal-gameplay-distance screenshot confirming the boots read correctly as tall black leather, matching the gallery reference — with all five characters and no console errors on a final regression pass.
- 2026-07-15: Switched the yellow-top male's boots from the real mesh-transplant technique (`addKeeperBootTransplant`, donor geometry baked out of `keeper.vrm`) to the same `addFigureStyleBoots()` now used on Vita, at the user's request ("replace his boots by hers"). Confirmed it generalizes cleanly to a different donor model (different leg proportions, no per-model tuning needed) and correctly follows live walk animation, not just the rest pose — verified via a mid-stride screenshot (froze his patrol update mid-walk rather than only checking idle) showing both boots properly shaped and positioned through the animated leg swing, matching the tall-black-leather look with no bare skin visible. This left `addKeeperBootTransplant`, `addProceduralRidingBoots`, and `addTallRidingBoots` (their dispatcher) with zero remaining callers, so removed all three along with their now-orphaned exclusive helpers (`removeOwnFeet`, `splitGeometryByX`, `indexedBounds`, `filterTriangles`, `bootLeatherMaterial`) — about 330 lines — while keeping `hideOwnShoes` and `legBasis`, which `addFigureStyleBoots` still uses. Re-verified after the deletion with a fresh full reload: no console errors, all five characters still visible, loading overlay still resolves correctly, and a side-by-side screenshot confirms both the yellow male and Vita now share the identical boot style.
- 2026-07-15: Made Vita ("the female") player-controllable, matching the full treatment the white-male VRM got earlier rather than a half-measure — since `CHARACTER_STYLES`/`setControlledActor`/session persistence/portrait system all key off `controlledActorStates[actorKey]`, selectability required actually integrating her into that architecture, not just a button. Merged the standalone `vitaState` into `controlledActorStates.vita`; added `'vita'` to `CHARACTER_INDEX_TO_ACTOR_KEY` and a `VITA` entry to `CHARACTER_STYLES` (CTRL now cycles human → robot → vrm → vita → human); added a guard (`if (activeControlledActor !== 'vita') updatePassiveVita(dt);`) so her patrol pauses while controlled and resumes from wherever she was left once released, mirroring the white male; added a "🧝 VITA" quick-switch button next to WHITE MALE; extended `activePortraitRig()` with a `vita` branch (reusing the `vrm` type's tuned camera offsets and `rig.type === 'robot' ? … : …` fallback, since she's roughly human-scale — confirmed via screenshot rather than assumed); extended session persistence the same way it was extended for the white male (`actors.vita` in `buildUserSessionSnapshot`, `applySnapshotToActorState('vita', …)` in restore, and matching "reset to station" blocks in both `applyDefaultUserRuntime` and `resetGameState`). Verified in the browser: the CTRL cycle button visits all four actors in order; the dedicated VITA button switches control and updates all three portrait windows to her face/torso/legs; she stops drifting while controlled (`moved: 0` over a 3s check) and resumes patrolling immediately on release; a full page-reload persistence round-trip restored her exact saved position/rotation. One false alarm during testing: an early persistence check appeared to show her teleporting ~15 units on reload, but this was because that particular test used an arbitrary coordinate that was never collision/bounds-checked (unlike the actual patrol route, which was); a repeat test with a known-safe coordinate held stable across 8 real seconds, confirming the restore logic itself has no bug — the existing out-of-bounds recovery (which applies to any actor, not something introduced here) had simply done its job.
- 2026-07-15: Added phone remote-control: a per-session QR code on the desktop that a phone scans to drive the joystick, moves, and avatar switch of the *same live session*. Clarified the architecture with the user before building anything — this page is a static file (works via `file://` or GitHub Pages, no backend), and a literal shared JSON file can't give two separate devices a live, bidirectional channel once deployed (GitHub Pages is read-only). Went with WebRTC via PeerJS's free public signaling broker (no account/server needed): the desktop creates `new Peer('officedp-session-' + <6-char code>)`, renders a QR code (via the `qrcode-generator` CDN library) encoding `phoneEnd.html?session=<code>`, and the phone (`new Peer()` with an auto-generated ID) calls `peer.connect('officedp-session-' + code)`; both sides exchange plain JSON objects over the resulting `DataConnection` (`serialization: 'json'`). Message schema: phone→desktop `{type:'joystick',x,y}` / `{type:'input',key:'jump'|'wave',pressed}` / `{type:'toggle',key:'run'}` / `{type:'move',name:'kick'|'back-kick'|'full-kick'|'moonwalk'|'spin'}` / `{type:'switch',actor:'human'|'robot'|'vrm'|'vita'}`; desktop→phone a 1s heartbeat `{type:'state',controller,action}` so the phone can show "DRIVING: VITA · WALK" and highlight the active actor. Deliberately fed phone input into the *exact same* entry points the existing keyboard/on-screen controls already use (`setJoystickVector()`, the shared `vpad.jump`/`vpad.wave`/`runToggle` state, `startSpecialMove()`, `setControlledActor()`) rather than building a parallel input path, so nothing about the core game logic needed to change. Added a new collapsible "📱 PHONE" panel (QR + session code + connection status) next to the MOVES panel on the desktop, and a new lightweight mobile-first file, `phoneEnd.html` (touch joystick, avatar-switch row, hold buttons for jump/wave, toggle for run, one-shot buttons for the five performance moves, tap-to-reconnect status banner) — kept deliberately small (no embedded VRM data) so it loads fast on mobile data. Guards against the unhosted case: if `location.protocol === 'file:'`, the panel shows "host this page online" instead of trying to connect. Verified end-to-end for real over the public PeerJS broker (not mocked) using two live browser tabs — one as the desktop, one resized to a 375×812 mobile viewport loaded at `phoneEnd.html?session=<the real code from the desktop tab>`: confirmed the WebRTC connection actually established (both sides showed "connected"), then verified every message type actually reached and changed the correct desktop state (not just "no errors") — `vpad.moveX/moveY` from a real dispatched `PointerEvent` drag on the phone's joystick, `activeControlledActor` from a real tap on the phone's VITA button, `vpad.wave` toggling from the hold button, `specialMoveType` from the KICK button, `runToggle` from the RUN button — and confirmed the reverse direction too (the phone's "DRIVING: VITA · WALK" label and highlighted VITA button updating live from the desktop's heartbeat). Known scope limits, not addressed since not asked for: only one phone can actively drive a session at a time (a second connection replaces the first rather than merging inputs), and phone control requires the page to be hosted online — it's inert when opened via plain `file://` double-click.
- 2026-07-15: Fixed a real, user-reported bug in `addFigureStyleBoots()` (the shared boot style used by both the yellow-top male and Vita): from a low, close camera angle a visible sliver of bare foot/leg poked out around the boot, on both characters but at different edges (heel on the male, toe on Vita). Root-caused in stages rather than trial-and-error scaling forever: (1) the very first fix attempt (a `+0.045` forward position-nudge, added earlier this session to close a small toe gap on Vita only) turned out to have shortened the heel-side coverage by the same amount it lengthened the toe-side — translating a mesh moves both edges together, it doesn't grow either one — which is what caused the *new* heel gap on the male; replaced the translate with a symmetric scale-up from the same anchor point (both edges grow together) — this fixed the male cleanly, but (2) Vita still showed a gap even at a very generous 1.55× scale, and going from 1.34× to 1.55× barely changed the visible gap size at all, which was the tell that this was never actually a scale problem for her. Traced it to the actual cause: unlike the earlier mesh-transplant boot code (which explicitly cut away foot triangles from the donor mesh), `addFigureStyleBoots` never removed or hid anything from the character's own body — and Vita's whole body (skin, painted socks, everything) is one continuous `Body_SKIN`-material mesh with no separate foot/shoe geometry to hide, so her bare foot was always fully present underneath the boot regardless of how big the boot mesh was scaled. Fixed by re-adding `removeOwnFeet()` — bone-weight-based triangle deletion from any `_Body_.*SKIN$`-material mesh, keyed off the foot/toe bones — which this project already had and had proven working earlier in the session, but which got deleted as "no longer used" when the mesh-transplant boot path was replaced; it turns out the *technique* (not just the specific function call site) was still needed. Called it from `addFigureStyleBoots()` right after the existing `hideOwnShoes()`, and dialed the foot-mesh scale back down from 1.55× to a more naturally-proportioned 1.3× now that the boot no longer has to be oversized just to visually bury a persistent bare foot. Verified by reproducing the exact bug first (matched the user's reported low-angle framing on the male, confirmed the visible skin), then re-checking that same exact camera framing after each change on *both* characters — not just re-checking the character that prompted the report — since the two earlier scale-only attempts had each fixed one character while leaving (or in one case, barely touching) the other. Final state: both characters clean from the original bug angle, the opposite (heel-focused) angle, and a normal gameplay-distance view, boots reading as naturally sized rather than oversized, all five characters still visible with no console errors on a final regression pass.
- 2026-07-16: Added camera zoom and orbit ("360 view") controls to `phoneEnd.html`, alongside the existing move joystick. Reused the desktop's own camera functions rather than reimplementing anything: `rotateCameraByDelta(dx, dy)` (the exact function the desktop's own mouse-drag-to-orbit handler already calls) for the view stick, and `applyCameraZoomTarget(radius)` + the existing `CAM_MIN_RADIUS`/`CAM_MAX_RADIUS` constants for the zoom bar, both followed by `syncCameraInstant()` + `renderScene()` to match the desktop's own zero-lag, direct-update feel (not the eased/lerped path). Added two new phone→desktop message types (`{type:'orbit', dx, dy}`, `{type:'zoom', value}` normalized 0–1) and extended the existing `{type:'state'}` heartbeat with the desktop's current normalized zoom so the phone's slider thumb passively tracks the real camera zoom whenever the user isn't actively dragging it (avoids a jarring snap the moment a phone connects mid-session). The view stick is deliberately *rate-based*, not position-based, despite looking identical to the move stick: holding it deflected continuously sends scaled `dx/dy` deltas via a `setInterval(…, 33)` loop for as long as it's held (simulating a continuously-held mouse drag), then springs back to center and stops sending on release — matches the literal ask ("simulate the cursor dragging") while still reading visually as "a joystick" like the user called it, rather than a raw one-shot drag gesture that would be awkward on a small touchscreen. Restructured the phone layout to a 3-across control row (MOVE / ZOOM / VIEW) with the jump/wave/run/performance-move buttons moved to a new section below rather than beside, to fit the extra controls without cramping the existing ones. Verified for real over a live PeerJS connection: after finding that fabricated `PointerEvent` dispatches (with a synthetic pointerId, no real active touch) produced silently-wrong results for the zoom track specifically — traced to the drag math itself being correct (a direct function call reproduced the hand-calculated expected value exactly) and the discrepancy being isolated to the synthetic-event path, most likely `setPointerCapture` behaving oddly for a pointerId that was never real — switched to the `computer` tool's genuine OS-level clicks for both new controls, which is also how the pre-existing controls were proven working in the first phone-control session. With real input: a click near the bottom of the zoom track produced `targetCamRadius: 15.909`, matching `CAM_MIN_RADIUS + t·(CAM_MAX_RADIUS−CAM_MIN_RADIUS)` computed by hand to 3 decimal places; holding the view stick deflected for ~1s moved `camYaw` from `0` to `-1.098`, correctly signed and roughly proportional to hold time. Mid-verification, the desktop tab became unresponsive to both `screenshot` and `javascript_exec` calls (a recurring pattern with this page's heavy WebGL/44MB-parse load after extended interaction, not new to this session) — rather than fight it, opened a fresh tab, reconnected the phone to the new session code, and completed verification there; also re-confirmed avatar-switching (a pre-existing, unmodified control) still works via a real tap, to check the layout restructuring didn't regress anything already working. No console errors on either side throughout.
- 2026-07-16: Fixed a real layout bug the user flagged: the zoom bar wasn't level with the MOVE/VIEW joysticks. Root cause was a unit mismatch, not a spacing tweak — the joysticks were sized `width: min(34vw, 170px)` with `aspect-ratio: 1/1` (so height tracks that same value), while the zoom track's height used a completely unrelated formula, `min(38vh, 200px)`, which evaluates larger than the joystick diameter on typical phone viewports (e.g. 200px vs 127.5px on a 375×812 screen) — so the track visibly stuck out above and below the circles no matter how the flex alignment was tuned. Fixed by giving the track the exact same height formula as the joysticks, and switched `#main-area` from `align-items: center` to `align-items: flex-start` (with the stick/track element as the first flex child in each column) so all three now share identical top and bottom edges, not just an approximate visual center — confirmed via `getBoundingClientRect()` on all three elements returning byte-identical `top`/`bottom`/`height`, not just eyeballed. Also took the opportunity to address "more user-friendly" concretely rather than guessing at vague polish: removed `flex:1` from `#main-area`, which had been forcing it to fill all leftover vertical space and pushing the action buttons down behind a large, purposeless empty gap in the middle of the screen — the whole layout now flows compactly from the top instead. Added "AVATAR" and "ACTIONS" section labels above the actor-switch row and the button area for clearer grouping (matching the existing MOVE/ZOOM/VIEW per-control labels). Since removing `flex:1` means content is no longer guaranteed to exactly fill the viewport height, also made the page scroll-safe as a defensive fix for shorter phones: changed `overflow:hidden`/`touch-action:none` on `html,body` (previously a hard requirement so the custom touch controls wouldn't fight page scroll/bounce) to `overflow-y:auto`/`touch-action:pan-y`, so the page can scroll as a fallback if content ever doesn't fit, while the individual controls' own `touch-action:none` still takes precedence during actual drag interaction and blocks scroll-interference there. Verified in the browser: alignment confirmed via matching bounding-rect coordinates (not just a screenshot), a fresh screenshot shows the new compact layout with both section labels, and a live-connected re-test confirmed the zoom bar still correctly drives the desktop's camera radius after the height-formula change (click near the new, shorter track's bottom edge produced `targetCamRadius: 16.3`, near max as expected) — no console errors.
- 2026-07-17: Reworked the `phoneEnd.html` control layout per user request: moved the zoom bar out of the 3-across MOVE/ZOOM/VIEW row and placed it directly below the purple VIEW stick (new `#orbit-column` flex wrapper containing `#orbit-zone` then `#zoom-zone` stacked vertically), and rotated it from a vertical slider to a horizontal one so it reads left-to-right under the stick it's paired with. The action/performance buttons (jump/wave/run/kick/back-kick/full-kick/moonwalk/spin) were pulled out of the always-visible layout and put behind a new collapsible `☰ ACTIONS ▼` toggle button, collapsed by default, so the default view is just the two sticks plus the zoom bar with no button clutter; expanding it reveals the full grid, matching the desktop's own panel-toggle idiom rather than inventing a new one. Locked the page down per explicit request: reverted the previous round's scroll-safety fallback (`overflow-y:auto`/`touch-action:pan-y` on `html,body`) back to `overflow:hidden`/`touch-action:none` (plus `overscroll-behavior:none`) now that the compact toggle-based layout comfortably fits typical phone viewports without needing a scroll fallback, added `minimum-scale=1.0` to the viewport meta (alongside the existing `maximum-scale=1.0, user-scalable=no`) to close a gap where some mobile browsers still allow pinch-zoom-out even with `user-scalable=no`, and added explicit JS gesture guards — `gesturestart` and `dblclick` preventDefault, plus a 300ms-window `touchend` timestamp check that preventDefaults the second tap of a double-tap before the browser's built-in double-tap-to-zoom can trigger. The horizontal zoom math keeps the exact same `zoomValue` semantics the desktop protocol already expects (0 = zoomed in / `CAM_MIN_RADIUS`, 1 = zoomed out / `CAM_MAX_RADIUS`) but maps it to left-to-right screen position instead of bottom-to-top, so no desktop-side code needed to change. Verified for real against a live connected desktop tab (fresh session, not reused): a genuine `computer`-tool click near the track's left ("−") edge, using coordinates taken directly from a fresh `getBoundingClientRect()` call rather than eyeballed off a screenshot (screenshots render at 2x device-pixel-ratio in this harness — clicking at visually-estimated coordinates landed out of the 0–375 CSS-pixel viewport entirely on the first attempt and produced a bogus mid-track result; switched to rect-derived coordinates for the corrected attempt), produced `targetCamRadius: 16.5` (exactly `CAM_MAX_RADIUS`, zoomed out); a click near the right ("+") edge produced exactly `2.8` (`CAM_MIN_RADIUS`, zoomed in) — confirming the left/right convention is correct end to end. Re-verified the two controls whose code was untouched by this restructure still work after it: a real click-drag on the VIEW stick changed the live desktop's `camYaw`/`camPitch` away from their baseline values; the MOVE joystick was harder to verify by position-delta because this test harness's tabs report `document.hidden: true` regardless of which is "active" (so the desktop's `requestAnimationFrame` game loop — which is what actually integrates joystick input into position — is throttled/paused throughout), so instead temporarily monkey-patched `setJoystickVector` on the live desktop tab to log its call arguments, repeated the same real click-drag, and confirmed a clean deflection curve (`[0.004, 0.145] → [0.004, -0.639] → [0, 0]` on release) reached the handler correctly — isolating the "no position change" to the harness's rAF throttling rather than a pipeline defect. No console errors on either side. The collapsible toggle itself was also verified with a real click (not just CSS inspection): collapsed → expanded showed all eight action buttons fit the viewport with no scroll needed, and a second real click collapsed it back. Not yet pushed to GitHub — pending user confirmation.
- 2026-07-17: Pushed the zoom-bar/actions-toggle/scroll-lockdown round to GitHub (`ca119cf`) after the user confirmed. Excluded an unrelated, pre-existing bulk deletion under `output/` (old blackjack/sofa test-artifact files, not touched this session) from the commit by staging only `phoneEnd.html` and `progress.md` explicitly rather than `git add -A`.
- 2026-07-17: Added a landscape-specific layout to `phoneEnd.html` via a single `@media (orientation: landscape)` block — no HTML or JS changes needed, since it only re-lays-out the existing elements. Per the user's request, the 3 header labels (title/status/driving) go from 3 stacked lines to one row (`#header` becomes `display:flex`), and the whole control area switches from vertical stacking to a single 3-column grid (`avatar | move+view+zoom | actions`) so the constrained landscape height goes to the controllers rather than labels: `#app` becomes `display:grid` with `grid-template-areas` placing `#header` across the top and `#actor-row` (now a vertical button stack)/`#main-area`/`#actions-toggle-row` side by side below. Two elements that only added clutter in the cramped orientation are hidden outright (`.section-label` "AVATAR" text, `#retry-hint`) since the buttons/status banner are self-explanatory without them. The expanded actions panel (`#bottom-area.expanded`) switches from pushing layout (portrait's approach) to a `position:absolute` floating overlay anchored bottom-right, so opening it never resizes or shoves the joysticks — it just floats over the corner of the screen. The long-error-message case (`#status` can contain text like "Connection error (peer-unavailable) — tap to reconnect") is handled with `text-overflow:ellipsis` + `max-width:42vw` on `#status` specifically (title/driving stay `flex-shrink:0`, always fully visible) rather than letting it wrap or blow out the one-line header. Sized the MOVE/VIEW circles and zoom track with a landscape-specific `min(20vw, 140px)` (replacing portrait's `min(34vw, 170px)`, which would have simply hit its flat 170px cap on any real landscape viewport and — worked through by hand for the narrowest realistic case, a 568px-wide landscape phone — actually been too wide to fit next to the 76px/96px side columns); verified this wasn't just a theoretical concern by checking the real computed width at 568×320, which came back exactly `113.59375px` (matching `20vw` of 568 by hand), with the avatar/main/actions column edges confirmed non-overlapping via `getBoundingClientRect()` on all three (86 → 98 → 450 → 462, i.e. the intended 12px gaps on both sides, not eyeballed). Verified in the browser at three sizes: 844×390 (iPhone 12-class) and 568×320 (smallest realistic landscape phone) both show the one-line header — including a real long error message correctly ellipsizing instead of wrapping — with all controls fitting and the actions overlay opening/closing via real clicks with no viewport overflow in either dimension (`getBoundingClientRect()` checked against `innerWidth`/`innerHeight` directly, not assumed from a screenshot); portrait at 375×812 re-checked afterward and confirmed pixel-identical to the pre-existing verified layout (unaffected, since the new rules are entirely inside the landscape media query). No console errors at any size. Not yet pushed — pending user confirmation.
- 2026-07-17: Reworked the landscape layout again per user follow-up: pin MOVE to the left edge and VIEW+ZOOM to the right edge (natural two-thumb reach when holding the phone with both hands) instead of clustering both sticks together in a shared center column, and move the actions toggle to the top-left corner. The previous round's 3-column grid (`avatar | move+view+zoom | actions`) is replaced with `auto 1fr auto` columns and a 4-row template — `header` across the top, then `avatar`/`toggle`/`move` stacked in the left column with an empty `1fr` spacer column, and `view` (the VIEW+ZOOM stack) spanning all 3 body rows in the right column — so MOVE and VIEW sit at the true left/right screen edges with nothing but empty space between them. Achieved without touching the HTML or JS: `#joystick-zone` and `#orbit-column` are nested inside `#main-area` in the DOM, so `#main-area` gets `display:contents` in the landscape block, which removes its own box and promotes those two children to be direct grid items of `#app` — each independently placeable via `grid-area` even though the markup nesting is unchanged. The avatar switcher changed from a 4-row vertical stack to a 2×2 grid (`repeat(2, 1fr)`) to keep the top-left corner cluster (avatar buttons + actions toggle) short enough to leave real room for the MOVE joystick below it in the same column — this was the tight dimension: hand-computed the smallest realistic case (568×320) at roughly 4px of margin between the joystick's bottom label and the viewport edge, then confirmed empirically via `getBoundingClientRect()` that it does NOT overflow (`joystick-zone` bottom `316` vs viewport height `320`) and a screenshot at that exact size shows no visible clipping. Circle/zoom-track sizing tightened slightly to `min(20vw, 130px)` (from `140px`) to help that margin. Moved the expanded actions overlay's anchor from bottom-right to top-left (`left`/`top` instead of `right`/`bottom`) to open from beside its (relocated) toggle button; confirmed via rect that it doesn't overflow the viewport at either landscape size tested. Known, accepted tradeoff: because the toggle button now sits directly above the MOVE joystick in the same column, the expanded overlay briefly covers roughly the top half of the MOVE circle while open (measured overlap zone ~151–200px of the circle's 151–265px vertical span at 568×320) — not fixed, since doing so would require either shrinking the 8 action buttons to fit a much shorter panel or adding internal scrolling to a panel that's open for only a few seconds at a time; the same toggle tap that opens it closes it, restoring full joystick access immediately. Verified in the browser at 844×390 and 568×320: real `getBoundingClientRect()` checks confirmed no overlap between the corner cluster and the MOVE joystick and no overflow in any direction at both sizes; the actions overlay was opened and closed via real dispatched pointer events at both sizes with correct expand/collapse state each time; portrait at 375×812 re-checked and confirmed pixel-identical to the already-verified layout (all landscape rules stay inside the `orientation: landscape` media query). No console errors. Not yet pushed — pending user confirmation.
- 2026-07-17: Fixed a real bug the previous round's top-left move introduced and the user caught: opening the landscape actions panel completely hid its own `☰ ACTIONS ▼` toggle button, since the expanded `#bottom-area` overlay is anchored at the same top-left corner the toggle button lives in and fully covers it (opaque background, `z-index:8`) — meaning once open, there was no visible way to dismiss it (the accepted MOVE-joystick overlap from the prior entry was fine since that control isn't needed to close the menu; the toggle button being the *one* control that closes the menu made this a real dead-end, not a cosmetic tradeoff). Fixed with `#actions-toggle-row { position: relative; z-index: 9; }` (landscape-only) so the button's own stacking context renders above the overlay's `z-index: 8` regardless of the exact pixel geometry, rather than trying to hand-tune the overlay's offset to dodge it. Verified in the browser at both 844×390 and 568×390: a screenshot after expanding shows `☰ ACTIONS ▲` clearly floating on top of the RUN button underneath it, and — the actual point of the fix — a real `computer`-tool click on that now-visible button (coordinates taken from a fresh `getBoundingClientRect()`, not eyeballed) collapsed the panel at both sizes. Confirmed the landscape-only rule doesn't leak into portrait (`getComputedStyle` on the toggle row at 375×812 shows `position: static; z-index: auto`, i.e. the base rule, unaffected). No console errors. Not yet pushed — pending user confirmation.
- 2026-07-18: Added a sixth character: a second Vita with pink boots, following the exact precedent set by the yellow-top male (`vrmRig2`) — a duplicate NPC that reuses the same embedded VRM asset and re-parses it independently rather than adding a new asset, patrols its own route, and isn't added to `CHARACTER_INDEX_TO_ACTOR_KEY`/`CTRL` (not player-controllable, matching the yellow-top male rather than the original Vita, since the ask was for "a new character," not a controllable variant). This required first making `addFigureStyleBoots()`'s boot color actually overridable: previously `figureLeatherMat` was a single module-level `MeshPhysicalMaterial` shared by every character that calls the function (both the yellow-top male and the original Vita), so directly recoloring it would have turned *all* boots pink at once. Added an `options.leatherColor` parameter that, when set, clones `figureLeatherMat` once per `addFigureStyleBoots()` call (not per leg) and recolors the clone, leaving the shared default material — and every other character using it — untouched; threaded that resolved material through the boot shaft, strap, and `createBootFootMesh()` (which gained a `leatherMat` parameter, defaulting to the shared material for existing callers). Added `vita2Rig` (`makeVrmCharacterRig({ scriptId: 'vrm-keeper-base64', decorate: vrm => addFigureStyleBoots(vrm, { leatherColor: 0xff4fa8 }) })`), `vita2State`, `VITA2_STATION`/`VITA2_PATROL_ROUTE`/`VITA2_GROUND_OFFSET`, and `updatePassiveVita2()`, wired into `applyScene()` unconditionally (never player-controlled, so no active-actor guard needed) right after the original Vita's block; added `vita2Rig.ready` to the loading-overlay tracker (now "N / 4 avatars ready", including updating the static HTML fallback text so there's no flash of the old "0 / 3" before the tracking script runs). Picked an unused patrol zone (`x: 9–12, z: -6…-3`, southeast of the map, clear of every existing station/route: human spawn, robot, both VRM males, the original Vita's southwest loop, and both service robots) and verified it empirically rather than by eye — the same collision-probe technique used for every prior patrol route in this project: teleported a scratch actor state to each of the 4 waypoints *and* each segment midpoint and called `moveActor` with zero input, confirming zero pushback at all 8 points before ever loading it into the visible scene. Verified in the browser: `4 / 4 avatars ready` on load with no console errors; `window.advanceTime(30000)` showed `vita2State.routeIndex` cycling and position moving substantially, confirming the patrol loop actually runs; a close-up screenshot (human character temporarily hidden and the camera manually repositioned next to her, since the normal follow-camera couldn't be aimed at a non-controllable NPC directly) shows unmistakably pink boots; confirmed via scene traversal that exactly 6 meshes on `vita2Rig` use the new pink material (`ff4fa8`) — 3 per leg × 2 legs, matching the shaft/strap/foot count — while the original Vita's boots are still exactly her original black (`050505`) and the yellow-top male's are also still black, proving the shared-material fix actually isolated the new color instead of leaking it onto the other two boot-wearing characters. All 6 characters confirmed visible after restoring the human's visibility. Pushed to GitHub per explicit request.
- 2026-07-18: Put Vita's boots — both the black original and the new pink variant — on display at the existing footwear rack (`addFootwearRack`, the shoe-shop rack at x:23.6, z:-12.7). Rather than modeling lookalike display props from boxes (the technique the rack's existing display boots use), rebuilt the *actual* boot geometry the VRM characters wear as a static standing pair: a new `makeVitaBootDisplayPair(leatherColor)` inside `addFootwearRack` reuses `createBootShaftGeometry`/`createBootFootMesh`/`createBootSoleMesh` plus the same strap torus and buckle box with identical dimensions as `addFigureStyleBoots`, minus the bone-relative framing (shaft base at a fixed ankle height of 0.065, foot group at ground level with the same 1.3× scale, toe toward the rack front) — so the shelf pairs are true miniatures of what the characters have on. Black uses the shared `figureLeatherMat` directly; pink clones-and-recolors it per pair via the same `leatherColor` pattern added for the vita2 character (same hex, `0xff4fa8`, for consistency). Placement: converted the rack's top shelf — previously 3 identical filler sneaker pairs, the second of two such shelves — to black-Vita-pair / sneaker / pink-Vita-pair, with the whole pair scaled 1.25× so the VRM-leg-scale geometry sits at comparable size beside the rack's chunkier box-built display boots. The Vita pairs are decorative only (not part of the human's `BOOT_STYLES` click-to-switch system, which the rack's other displays feed via `registerFootwearSelectionPicker`) — deliberately NOT registered as pickers since these boots have no corresponding human boot style to switch to; the one remaining top-shelf sneaker pair keeps its picker registration (added explicitly in the new branch), so the sneaker style stays clickable in 4 places instead of the previous 6 — acceptable since all 6 were duplicate pickers for the same single style. Verified in the browser: `4 / 4 avatars ready`, no console errors; screenshots aimed at the rack's top shelf (this took several attempts — learned that `syncCameraInstant()` copies `targetCamYaw` over any directly-set `camYaw`, so aiming the follow camera requires writing the `target*` variables, and manually setting `camera.position` gets recomputed away by `renderScene()`) clearly show the pink pair and the black pair standing on the top shelf flanking the sneakers; scene-traversal counts confirm exactly 12 pink-material meshes in the whole scene (6 on the vita2 character + 6 on the rack pair — shaft/strap/foot × 2 boots) and exactly 18 meshes on the shared black `figureLeatherMat` (6 original-Vita + 6 yellow-top-male + 6 rack pair), proving the display pairs use the real materials without leaking color anywhere; `footwearSelectionEntries.length` is 10 (6 boot styles + 4 sneaker pickers), matching the intended picker layout. Pushed to GitHub per explicit request.
- 2026-07-19: Added three more Vita variants per user request — brown outfit, red outfit, and a fifth "red boots" variant — following the same NPC-duplicate pattern as vita2/vrm2 (own patrol route, not player-controllable, `scriptId: 'vrm-keeper-base64'` re-parsed independently). The boots half was easy (`addFigureStyleBoots(vrm, { leatherColor: 0xb02020 })`, the exact mechanism already proven for vita2's pink boots — vita5). The outfit-color half took a long, genuinely instructive debugging detour: the obvious approach — reuse `recolorVrmMaterials(vrm.scene, /Tops/i, hex)`, the same one-liner that gave the yellow-top male his hoodie color — visibly did nothing to Vita's dress no matter what hex was passed. Root-caused in stages rather than guessing: (1) direct pixel sampling of the `Tops` and `Body_00_SKIN` texture atlases showed the "black" dress is not black cloth-with-shading in the texture — it's *unpainted* atlas space, literal `RGB(0,0,0)`, rendered opaque because the material isn't using alpha blending; `mat.color` is a *multiplicative* tint, and `0 × anything = 0`, so no tint value could ever make those texels visible — this is exactly why the male's Tops recolor worked (his texture's base is light-colored, so tinting shows) and Vita's doesn't (hers is black-because-empty). (2) Built a canvas-based fix that rewrote those near-black texels to a real hue-shifted color (preserving relative lightness so shading/folds would still read, and explicitly excluding skin-tone and cyan-accent-trim pixel ranges so only the "fabric" fill would change) and reassigned it as a new `CanvasTexture` — the canvas itself was verified correct at every step (`getImageData` readback, and literally appending the processed canvas to the page DOM to see it directly, bypassing Three.js entirely) — brown/red pixels were unmistakably present in the source data. (3) Yet the rendered mesh stayed black regardless, through an extensive elimination pass: ruled out wrong-mesh-instance (Vita's `Body_00_SKIN` material name is shared across 4 separate `MeshStandardMaterial` instances on 4 separate mesh primitives — `Bodybaked` through `Bodybaked_3` — each independently confirmed via solid per-mesh flat-color tests, e.g. one showing color only in a small neck sliver, another on a hand), ruled out the toon-outline shell (tested its material directly, `side: THREE.BackSide`, not the culprit), ruled out vertex colors/emissive/aoMap/lightMap/envMap (all absent or neutral), ruled out stale per-material GPU-program caching (replaced the *entire* material object with a brand-new `MeshStandardMaterial` instance carrying the verified-correct canvas map — identical result), and ruled out alpha-premultiply upload behavior (forced full opacity across the whole canvas — no change). What finally worked: setting `mat.map = null` and using a flat `mat.color` instead of any texture at all — confirmed first with an "everything in the rig, no maps, magenta" sweep (the entire figure, dress included, turned magenta), then narrowed to just the `Tops` material specifically. The real, still-not-fully-explained finding is that `Tops` alone (not `Body_00_SKIN`) is what actually renders as the visible dress/cloak surface — the `Body_00_SKIN` material's dark regions turned out to cover only tiny slivers (neck, a hip patch) — so the fix only ever needed to target `Tops`, and since `Tops` is a fabric-only mesh with no skin pixels mixed in, dropping its texture map costs nothing (no skin-tone loss) while guaranteeing the color renders correctly, unlike the texture-preserving approach that never worked despite the source data being provably right. Added this as a new named helper, `recolorVrmOutfitFlat(vrmScene, nameRegex, hexColor)`, next to the existing `recolorVrmMaterials`, documented with why it exists rather than just re-tinting. Applied via `vita3Rig`/`vita4Rig` (`recolorVrmOutfitFlat(vrm.scene, /Tops/i, 0x8b4513)` / `0xb02020`, both with default black boots) and `vita5Rig` (default outfit, `addFigureStyleBoots(vrm, { leatherColor: 0xb02020 })`). Picked three more open patrol zones south of the existing vita2/vita/vrm2 territory (`x:9–12,z:-13…-10` / `x:-9…-5,z:-14…-11` / `x:2–5,z:-13…-10`) and verified each collision-clear at every waypoint and segment midpoint before wiring them in, same technique as every prior route in this project. Extended the loading overlay to `[…, vita3Rig.ready, vita4Rig.ready, vita5Rig.ready]` (now "N / 7 avatars ready", static HTML fallback updated to match) and added the matching `VITA3/4/5_STATION/GROUND_OFFSET/PATROL_ROUTE` constants, `vita3/4/5State`, `updatePassiveVita3/4/5()`, and unconditional `applyScene()` wiring (never player-controlled, same as vita2). Verified in the browser: fresh load shows `7 / 7 avatars ready` with no console errors; `window.advanceTime(20000)` showed all three route indices cycling with `action: "walk"`; close-up screenshots (human hidden, camera manually aimed at each character in turn) confirm a clean solid-brown dress, a clean solid-red dress, and clean red boots respectively, all still showing Vita's normal face/hair/cyan-accent-trim/animation; confirmed no regression on the original Vita or the yellow-top male by scene-traversal color checks (`050505` black leather still the only boot color on both, `f5c518` yellow still intact on the male's top) — the new recolor path never touched the shared default materials. All 7 characters confirmed simultaneously visible. Pushed to GitHub per explicit request.
- 2026-07-22: Added a seventh Vita variant: a "Matrix movie style" tight black leather jumpsuit, black boots, and black shades. The jumpsuit reused `recolorVrmOutfitFlat` (from the previous round's brown/red outfits) with a new second capability added alongside it — an optional `{ roughness, metalness }` override — since a flat-matte black wouldn't read as "leather"; called as `recolorVrmOutfitFlat(vrm.scene, /Tops/i, 0x0a0a0a, { roughness: 0.2, metalness: 0.15 })` for a glossy, second-skin sheen instead of the diffuse-cloth look the other color variants use. Boots use `addFigureStyleBoots(vrm)` with no color override (the shared default black, per "with the black boots"). Shades are new: no existing accessory system to reuse, so added procedural geometry (`createShadesLensShape` + `addBlackShades`) following the same pattern as the boot builder — two rounded-rectangle lenses (via `centeredExtrude` over a `THREE.Shape`, the same helper the boot foot/sole pieces use), a bridge box, and two short temple-arm stubs, all dark and glossy (`roughness: 0.1, metalness: 0.35` lenses, more metallic frame), attached directly to `vrm.humanoid.getBoneNode('head')` — the real bone, not the animation-proxy rig, so the shades track actual head rotation. Position needed empirical tuning exactly like the boot placement did (documented in-code as such): the first attempt placed the shades group at local `(0, -0.02, +0.085)` on the head bone, which landed *behind* her head entirely — confirmed by comparing the shades' and head's world positions after attaching (`getWorldPosition` on both showed the shades moved in −Z world space for a +Z local offset), a clean sign inversion, so this project's VRM head-bone-local Z axis points opposite to what was assumed. Flipped to `-0.085` and iterated the Y offset twice more (first attempt sat over her mouth, not her eyes) before arriving at `(0, 0.018, -0.085)`, which was then hardcoded as the function's default (all three offsets still overridable via `options` for future reuse on the male VRM models if ever wanted, though their head-bone axis conventions aren't guaranteed to match and would need their own empirical check). Verifying this also fought the follow-camera itself harder than the character work: direct `camera.position.set()`+`lookAt()` calls got silently overwritten by the next `renderScene()` call (contradicting what worked in an earlier session for a different close-up shot — not fully explained, possibly dependent on some render-loop state that differed between sessions), and framing a specific patrol NPC's face required accounting for her *arbitrary* current facing direction (patrol characters rotate to face their walk direction, so freezing one mid-patrol leaves her facing whatever way she last walked) — worked around by explicitly setting `vita6State.rot` to a known value before aiming the camera, rather than fighting to detect her current facing. Picked a sixth open patrol zone (`x:-2…1, z:-13…-10`, filling the gap between the existing vita5 and vita4 zones) and verified collision-clear at all waypoints and segment midpoints before wiring it in. Added `VITA6_STATION`/`GROUND_OFFSET`/`PATROL_ROUTE`, `vita6State`, `updatePassiveVita6()`, `applyScene()` wiring (unconditional, NPC-only, same as the other Vita duplicates), and extended the loading overlay to `[…, vita6Rig.ready]` (now "N / 8 avatars ready", static fallback text updated to match). Verified in the browser: fresh load shows `8 / 8 avatars ready` with no console errors; a close-up front-on screenshot (camera aimed via the position/rotation workaround above) clearly shows a symmetric pair of black shades sitting over both eyes with no bare eye visible above or below the lenses, the glossy black jumpsuit reading distinctly different (visible specular sheen) from the other variants' matte outfits, and black boots matching the original Vita; `window.advanceTime(15000)` showed her patrol route index advancing with `action: "walk"`; confirmed no regression on the original Vita, the yellow-top male, or the red-boots variant via scene-traversal color checks (each still shows only its own expected material colors). All 8 characters load and coexist without error. Not yet pushed — pending user confirmation.
- 2026-07-22: Changed the Matrix-style Vita's outfit from a recolored dress to an actual two-piece leather jacket + pants, per user follow-up after confirming they wanted real geometry built rather than a two-tone recolor of the existing dress silhouette (asked first, since the dress mesh has no separable jacket/pants pieces — this was a meaningful scope jump from every other Vita variant so far, all of which only recolored the existing dress). Added `hideVrmMeshesByMaterial()` (generic mesh-visibility toggle by material-name match, used to hide the `Tops` dress mesh instead of recoloring it) and `addLeatherJacketAndPants(vrm)`, which builds 7 separate pieces from primitives and attaches each to the real humanoid bone it should follow: one `makeCapsuleGeometryCompat` torso capsule on `chest`, two upper-arm sleeve tubes and two forearm sleeve tubes (`createBootShaftGeometry`, the same lathe-tube profile the boot shafts use) on the arm bones, and two thigh tubes on the upper-leg bones — all sized and oriented via `boneSpan()`, a small helper that reads the real local-space vector from each attachment bone to its child bone (chest→neck, upperArm→lowerArm, lowerArm→hand, upperLeg→lowerLeg) so every piece's length and "which way is down the limb" rotation comes from the actual skeleton, the same principle as the boot shaft's `shinDir`/`shinLen` computation, generalized to more bone pairs. Two real bugs, both caught and fixed by comparing against a definitive ground truth rather than guessing again: (1) the first version was invisible — traced to `mesh.scale.set(radiusTop, 1, radiusTop)` being applied on top of `createBootShaftGeometry`'s own already-realistic profile radius (~0.086, confirmed via `computeBoundingBox()`), so a desired radius like `0.075` was being used as a *scale factor* on an already-correctly-sized mesh, shrinking every limb tube to a few millimeters; fixed by scaling relative to the profile's real base radius (`scale = desiredRadius / 0.086`) instead of using the desired radius directly as the scale. (2) even after that fix the pieces were barely visible — confirmed they existed with correct world-space size and position (`Box3.setFromObject` on the mesh gave a sane bounding box) by temporarily forcing the shared material to bright green, which showed thin slivers rather than a proper wrap, meaning the chosen radii were simply smaller than Vita's actual limb thickness so the tubes sat mostly *inside* the opaque body mesh; fixed by iterating the radius values upward (arms 0.042→0.082, forearms 0.036→0.088, thighs 0.075→0.17, torso 0.095→0.115 with 3x more length) against real screenshots until they read as a proper jacket/sleeve/pant-leg wrap instead of a stripe — the first attempt at the bigger torso overshot into a distinct black balloon/egg shape, corrected down once more. Reused the exact same follow-camera workaround from the shades work (`vita6State.rot` forced to a known value before aiming, since a frozen patrol NPC's facing direction is otherwise whatever she last happened to be walking toward). Verified in the browser: `8 / 8 avatars ready`, no console errors; a sequence of close-up screenshots through each fix shows the progression from invisible → thin slivers → a properly-proportioned black jacket with sleeves and separate pant legs merging into the existing boots, with a shoulder/waist gap closed by extending the torso capsule further down; a walking mid-stride screenshot confirms the thigh tubes correctly follow the upper-leg bone's rotation during the walk cycle (one leg forward, one back, no stretching or clipping through the body); scene-traversal color checks on the original Vita, the yellow-top male, and the red-outfit variant confirm no regression (each still shows only its own expected material colors — the new jacket material and mesh-hiding logic are scoped entirely to vita6's own independently-parsed VRM instance, same isolation guarantee as every other per-character customization this session). Not yet pushed — pending user confirmation.
- 2026-07-22: Removed the red recolor from the "red coat" Vita (vita4) per user request — dropped the `recolorVrmOutfitFlat(vrm.scene, /Tops/i, 0xb02020)` call from her `decorate` hook, leaving just `addFigureStyleBoots(vrm)`, so she reverts to the default black outfit while remaining as a character on her own patrol route (the ask was interpreted as "take the red coat off her," not "delete the character," since the request's own wording was "vita's red coat," keeping the character as the subject and the coat as the object being removed — confirmed via a clarifying question before touching anything, given deleting a whole character outright isn't reversible-by-re-adding-a-line the way an outfit revert is). Verified in the browser: scene-traversal colors on `vita4Rig` now show exactly the same set as the plain default Vita (`ffffff`, `050505`, `b6bcc4`, `040404` — no `b02020` anywhere), a close-up screenshot confirms the plain black dress, patrol still cycles correctly (`window.advanceTime(15000)` advanced her route index with `action: "walk"`), and no console errors. Not yet pushed — pending user confirmation.
- 2026-07-22: Removed vita4's dress entirely per user follow-up (clarified via question after "remove the scope" turned out to mean "remove the dress," not a delete-the-character request) — added `hideVrmMeshesByMaterial(vrm.scene, /Tops/i)` to her `decorate` hook (reusing the helper built for the leather-jacket work) ahead of `addFigureStyleBoots(vrm)`, with no replacement geometry this time — just bare removal. This reveals the same painted-on geometric bodysuit pattern discovered while debugging the jacket build (baked into the `Body_00_SKIN` texture, full torso/arm/leg coverage — confirmed by inspecting the actual render, not assumed, before treating this as done, since "remove the dress" could plausibly have exposed something inappropriate if her base body texture had been a normal skin texture instead of a printed catsuit). Verified in the browser: no console errors, a screenshot confirms the dress is gone and the cyan/purple bodysuit pattern + black boots are showing correctly, her patrol still cycles (`routeIndex` advanced with `action: "walk"`), and the original Vita's own Tops mesh is confirmed still `visible: true` (the change only touches vita4's independently-parsed VRM instance). Not yet pushed — pending user confirmation.
